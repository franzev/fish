---
phase: quick-260708-nr2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0015_reactions_any_emoji.sql
  - apps/web/package.json
  - pnpm-lock.yaml
  - apps/web/components/chat/emoji-picker/emoji-picker.tsx
  - apps/web/components/chat/emoji-picker/index.ts
  - apps/web/components/chat/emoji-picker/emoji-picker.stories.tsx
  - apps/web/components/chat/index.ts
  - apps/web/components/chat/reactions/reactions.tsx
  - apps/web/components/chat/reactions/reactions.stories.tsx
  - apps/web/components/chat/story-data.ts
  - apps/web/app/globals.css
  - apps/web/app/(authenticated)/chat/chat-client.tsx
  - apps/web/app/(authenticated)/chat/chat-client.test.tsx
autonomous: false
requirements: [QUICK-260708-nr2]
user_setup: []

must_haves:
  truths:
    - "A reaction pill shows an emoji plus its count and reads as a quiet monochrome chip; the viewer's own reaction is marked by tone/border, never by hue"
    - "A trailing add-reaction pill (smiley) sits after the reaction pills and opens the emoji picker"
    - "The message hover bar opens the same emoji picker from a smiley trigger instead of a hardcoded thumbs-up"
    - "The picker is grouped by category and searchable, monochrome, and keyboard-operable (Escape closes, focus-visible rings)"
    - "Any emoji (including multi-codepoint ZWJ/skin-tone sequences up to 16 chars) can be toggled and persists to message_reactions — no 4-emoji whitelist remains"
    - "Every button and role=button is cursor-pointer via a single globals base-layer rule"
  artifacts:
    - path: "supabase/migrations/0015_reactions_any_emoji.sql"
      provides: "Drops the 4-emoji check constraint and RPC whitelist; adds generic length-bound validation"
      contains: "char_length"
    - path: "apps/web/components/chat/emoji-picker/emoji-picker.tsx"
      provides: "Grouped + searchable monochrome EmojiPicker panel and EmojiPickerButton popover trigger"
      min_lines: 60
    - path: "apps/web/components/chat/reactions/reactions.tsx"
      provides: "Screenshot-geometry reaction pills plus trailing add-reaction smiley pill"
    - path: "apps/web/app/globals.css"
      provides: "Base-layer cursor-pointer rule for buttons and role=button"
      contains: "cursor: pointer"
  key_links:
    - from: "apps/web/components/chat/reactions/reactions.tsx"
      to: "EmojiPickerButton"
      via: "trailing add-pill onSelect -> onToggle"
      pattern: "EmojiPickerButton"
    - from: "apps/web/app/(authenticated)/chat/chat-client.tsx"
      to: "handleToggleReaction"
      via: "picker onSelect in hover bar and reactions row"
      pattern: "handleToggleReaction"
    - from: "supabase/migrations/0015_reactions_any_emoji.sql"
      to: "public.toggle_message_reaction"
      via: "create or replace function without whitelist"
      pattern: "toggle_message_reaction"
---

<objective>
Redesign chat emoji reactions to match the reference screenshot geometry (a monochrome pill with emoji + count, plus an adjacent add-reaction smiley pill that opens a grouped, searchable emoji picker) using only FISH design tokens, and remove the 4-emoji whitelist end-to-end so any emoji can be toggled and persisted.

Purpose: The current reactions are limited to 4 hardcoded emojis and the hover bar hardcodes a thumbs-up. Clients and coaches should be able to react with any emoji through a calm, choice-removing picker that still reads as a positive reward, not a graded control.

Output: A new additive migration relaxing the DB/RPC validation, a reusable monochrome EmojiPicker (panel + popover-button), redesigned Reactions pills with a trailing add-pill, a single globals cursor-pointer rule, and updated stories/tests.
</objective>

<execution_context>
@/Users/franz/Library/Application Support/Claude/local-agent-mode-sessions/614fd504-8566-46b2-8256-66ded77bb604/43ed32e3-053c-413f-843e-d49e1f00f5e1/rpm/plugin_019FPGEfUV8XSdonegN1mWDs/workflows/execute-plan.md
@/Users/franz/Library/Application Support/Claude/local-agent-mode-sessions/614fd504-8566-46b2-8256-66ded77bb604/43ed32e3-053c-413f-843e-d49e1f00f5e1/rpm/plugin_019FPGEfUV8XSdonegN1mWDs/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@apps/web/app/globals.css
@apps/web/components/chat/reactions/reactions.tsx
@apps/web/components/chat/reactions/reactions.stories.tsx
@apps/web/components/chat/types.ts
@apps/web/components/chat/index.ts
@apps/web/app/(authenticated)/chat/chat-client.tsx
@apps/web/app/(authenticated)/chat/actions.ts
@supabase/functions/chat-command/index.ts
@supabase/migrations/0013_realtime_chat_features.sql

Before touching any client-facing screen, load the `.claude/skills/sketch-findings-fish` skill so the settled monochrome design direction (reactions are a positive reward, never color in structural UI, calm copy) is applied.

<interfaces>
<!-- Extracted from the codebase. Use directly — do not re-explore. -->

Reaction shape (apps/web/components/chat/types.ts):
  interface Reaction { emoji: string; count: number; byMe: boolean }

Reactions component today (apps/web/components/chat/reactions/reactions.tsx):
  interface ReactionsProps extends Omit<HTMLAttributes<HTMLDivElement>, "onToggle"> {
    reactions?: Reaction[];
    onToggle?: (emoji: string) => void;
  }
  - Returns null for empty/missing list.
  - Pill classes: "animate-reaction-pop inline-flex items-center gap-2xs rounded-pill border px-xs py-2xs transition-colors"
    byMe -> "border-border-strong bg-surface-2 text-foreground"
    else -> "border-transparent bg-surface-2 text-body hover:border-border"
  - Emoji span: text-ui-sm leading-none ; count span: text-ui-xs font-medium leading-none

Input component export (apps/web/components/ui/input/input.tsx):
  export const Input = forwardRef<HTMLInputElement, InputProps>(...)  // reuse for picker search

chat-client.tsx hover bar (around lines 438-483) currently renders, inside `showMessageActions`:
  - "Reply to message" button (IconMessageReply)
  - "React with thumbs up" button (IconThumbUp) -> onClick={() => void handleToggleReaction(message, "👍")}   <-- REPLACE
  - mine-only Edit/Delete buttons
Reactions is rendered at ~line 484:
  <Reactions reactions={message.reactions} onToggle={(emoji) => void handleToggleReaction(message, emoji)} className="mt-2xs" />

Design tokens available (apps/web/app/globals.css @theme):
  colors: bg / surface / surface-2 / border / border-strong / foreground / body / muted / primary / on-primary
  radius: rounded-card rounded-control rounded-pill
  spacing tokens: gap-nudge gap-2xs gap-xs px-xs py-2xs mt-2xs etc. (NO one-off numeric utilities)
  --size-control: 56px

toggle_message_reaction RPC (supabase/migrations/0013 lines 517-581): security-definer plpgsql, returns public.messages.
  Whitelist to remove is line 535: `if p_emoji not in ('👍', '❤️', '🎉', '🙏') then raise exception 'reaction is not supported'; end if;`
  message_reactions table constraint to remove is 0013 line 35:
    `constraint message_reactions_emoji_supported check (emoji in ('👍', '❤️', '🎉', '🙏'))`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive migration — remove the emoji whitelist from constraint and RPC</name>
  <files>supabase/migrations/0015_reactions_any_emoji.sql</files>
  <action>
Create a NEW migration file `supabase/migrations/0015_reactions_any_emoji.sql` (never edit 0013). It must do two things:

1. Relax the table check constraint. Drop `message_reactions_emoji_supported` and re-add it as a generic bound that allows any emoji including ZWJ/skin-tone sequences:
   - `alter table public.message_reactions drop constraint message_reactions_emoji_supported;`
   - `alter table public.message_reactions add constraint message_reactions_emoji_supported check (char_length(btrim(emoji)) between 1 and 16);`

2. `create or replace function public.toggle_message_reaction(p_message_id uuid, p_emoji text)` copying the EXACT body from migration 0013 (lines 517-581 — same signature, `returns public.messages`, `security definer`, `set search_path = ''`, the auth check, member assertion, deleted check, and the exists/update/insert toggle logic) with ONE change: replace the whitelist guard (`if p_emoji not in ('👍', '❤️', '🎉', '🙏') then raise exception 'reaction is not supported'; end if;`) with generic validation:
   `if p_emoji is null or btrim(p_emoji) = '' or char_length(p_emoji) > 16 then raise exception 'reaction is not supported'; end if;`
   Keep the exact `'reaction is not supported'` exception text so the edge function's calm error mapping ("That reaction is not available.") still triggers unchanged.

Do not change actions.ts (zod already `.max(16)`) or the chat-command edge function (already trims + checks non-empty, no whitelist there). Add a short leading `--` comment explaining WHY (any-emoji support, length-bounded to allow multi-codepoint sequences).
  </action>
  <verify>
    <automated>test -f supabase/migrations/0015_reactions_any_emoji.sql && grep -v '^--' supabase/migrations/0015_reactions_any_emoji.sql | grep -c "not in ('👍'" | grep -qx 0 && grep -q "char_length" supabase/migrations/0015_reactions_any_emoji.sql && grep -q "create or replace function public.toggle_message_reaction" supabase/migrations/0015_reactions_any_emoji.sql && echo OK</automated>
  </verify>
  <done>Migration 0015 exists, contains no 4-emoji whitelist, re-creates the constraint and RPC with length-bounded generic validation, and preserves the `reaction is not supported` exception text.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Task 3 needs the `unicode-emoji-json` npm package to supply grouped emoji data (`unicode-emoji-json/data-by-group.json`) for the searchable picker. No RESEARCH.md legitimacy audit exists for this quick task, so the package is treated as [ASSUMED] and must be verified before install.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/unicode-emoji-json
    2. Confirm it is the widely-used emoji dataset package (healthy weekly downloads, a public source repo, recent-enough maintenance, MIT-style license) and not a typosquat.
    3. Confirm the export path `unicode-emoji-json/data-by-group.json` exists (array of groups, each `{ name, slug, emojis: [{ emoji, name, slug, ... }] }`).
    If legitimate, approve. If it looks abandoned or wrong, reply with a preferred alternative (e.g. bundle a local dataset another way) and Task 3 will adapt.
  </how-to-verify>
  <resume-signal>Type "approved" to allow installing unicode-emoji-json, or name an alternative dataset.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Monochrome emoji picker, redesigned reaction pills, cursor rule, and chat wiring</name>
  <files>apps/web/package.json, pnpm-lock.yaml, apps/web/components/chat/emoji-picker/emoji-picker.tsx, apps/web/components/chat/emoji-picker/index.ts, apps/web/components/chat/emoji-picker/emoji-picker.stories.tsx, apps/web/components/chat/index.ts, apps/web/components/chat/reactions/reactions.tsx, apps/web/components/chat/reactions/reactions.stories.tsx, apps/web/components/chat/story-data.ts, apps/web/app/globals.css, apps/web/app/(authenticated)/chat/chat-client.tsx, apps/web/app/(authenticated)/chat/chat-client.test.tsx</files>
  <action>
Install and build the picker, redesign the pills, add the cursor rule, and wire everything through the existing reaction path. Use ONLY design tokens (bg-surface / bg-surface-2 / border / border-strong / text-foreground / text-body / text-muted, rounded-pill / rounded-control / rounded-card) and named spacing utilities (gap-*, px-*, py-*, p-*, mt-*) — no raw hex, no one-off numeric spacing (`p-2`, `gap-5`, etc.), no `tailwind.config.js`. Use `cn()` from `@/lib/utils`. Tabler icons via `@tabler/icons-react`.

1. Install dependency (only after the checkpoint approves): `pnpm --filter @fish/web add unicode-emoji-json`. Import grouped data with `import groups from "unicode-emoji-json/data-by-group.json"` (resolveJsonModule is already enabled). Each group is `{ name: string; slug: string; emojis: Array<{ emoji: string; name: string; slug: string }> }`.

2. globals.css — inside the existing `@layer base { ... }` block, add ONE rule so all clickables show a pointer (Tailwind v4 preflight leaves buttons as default cursor):
   `button:not(:disabled), [role="button"]:not(:disabled) { cursor: pointer; }`
   Add a WHY comment. Do not sprinkle `cursor-pointer` classes anywhere else.

3. Create `apps/web/components/chat/emoji-picker/emoji-picker.tsx` ("use client") exporting TWO named components:
   - `EmojiPicker` — the panel. Props: `{ onSelect: (emoji: string) => void; className?: string }`. Layout: a monochrome surface panel (`bg-surface border border-border rounded-card`), a sticky search row at the top reusing the existing `Input` component (from `@/components/ui/input/input`) with an `IconSearch` affordance and a calm placeholder ("Search emoji"), then a scrollable region grouped by category. When the search box is empty, render each group with a small `text-ui-xs text-muted` heading (the group `name`) followed by its emoji buttons; when there is a query, filter across all groups by matching the query (lowercased) against each emoji's `name`/`slug` and render a single flat "Results" list (calm empty state text via `text-muted` when nothing matches — e.g. "No emoji match that yet."). Each emoji is a `<button type="button">` with `aria-label={emoji.name}`, containing the emoji char in a `text-ui-lg leading-none` (or nearest existing size token) span with `aria-hidden="true"`, sized around `size-10` for a comfortable tap target, `rounded-control`, `hover:bg-surface-2`, `focus-visible` handled globally. Clicking calls `onSelect(emoji.emoji)`. Keep the panel keyboard-operable: it is composed of native buttons + Input, so tab order already works.
   - `EmojiPickerButton` — a self-contained popover trigger. Props: `{ onSelect: (emoji: string) => void; label: string; className?: string; children?: React.ReactNode }`. Renders a `relative`-wrapped trigger `<button type="button" aria-haspopup="dialog" aria-expanded={open} aria-label={label}>` (default children = `<IconMoodSmile size={18} stroke={1.75} aria-hidden="true" />`) plus, when `open`, an absolutely-positioned `EmojiPicker` panel. Manage `open` with `useState`. Close on: selecting an emoji (call `onSelect` then close), pressing Escape, and clicking outside (a `useEffect` pointerdown listener on `document` scoped to a container `ref`). Apply `className` to the trigger button so callers can style it as either a pill or an icon button. Respect prefers-reduced-motion (already global) — keep any open/close transition token-based and subtle, or none.
   Do NOT fetch from any CDN — all data is the bundled `unicode-emoji-json` import.

4. Create `apps/web/components/chat/emoji-picker/index.ts` re-exporting `./emoji-picker`, and add `export * from "./emoji-picker";` to `apps/web/components/chat/index.ts` barrel.

5. Redesign `apps/web/components/chat/reactions/reactions.tsx`:
   - Keep the `Reaction`-count pills exactly as the reference screenshot geometry — reuse the current pill classes (rounded-pill, byMe = border-border-strong bg-surface-2 text-foreground; others = border-transparent bg-surface-2 text-body hover:border-border; emoji span text-ui-sm, count span text-ui-xs font-medium). Keep `aria-pressed`/`aria-label` behavior.
   - Add a trailing add-reaction pill: when `onToggle` is provided AND there is at least one reaction, render an `EmojiPickerButton` styled as a circular monochrome pill (`inline-flex items-center justify-center rounded-pill border border-transparent bg-surface-2 text-muted hover:border-border hover:text-body px-xs py-2xs`) with `label="Add a reaction"` and `onSelect={(emoji) => onToggle(emoji)}`. This is the screenshot's second (smiley) pill.
   - Preserve "renders nothing for an empty list" (still return null when there are zero reactions — the hover-bar smiley in chat-client handles first-reaction entry).

6. Wire `apps/web/app/(authenticated)/chat/chat-client.tsx`:
   - Remove the `IconThumbUp` import; add `IconMoodSmile` to the `@tabler/icons-react` import. Import `EmojiPickerButton` from `@/components/chat`.
   - Replace the hardcoded "React with thumbs up" `<button>` (the one calling `handleToggleReaction(message, "👍")`) with an `<EmojiPickerButton label="Add a reaction" onSelect={(emoji) => void handleToggleReaction(message, emoji)} className="inline-flex size-10 items-center justify-center rounded-control text-muted hover:bg-surface-2 hover:text-body" />` sitting in the same hover-bar position (keep it inside the `showMessageActions` block so pending messages still hide it). Default children (IconMoodSmile) render the smiley.
   - Leave the `<Reactions ... onToggle={...}>` call as-is — the new add-pill lives inside Reactions.

7. Stories + mock data:
   - Create `apps/web/components/chat/emoji-picker/emoji-picker.stories.tsx` (title "Chat/EmojiPicker") with a story rendering the open `EmojiPicker` panel and one rendering `EmojiPickerButton` (log selection via an action/no-op onSelect).
   - Update `apps/web/components/chat/reactions/reactions.stories.tsx` so the existing stories pass an `onToggle` (so the add-pill shows) — e.g. `args: { onToggle: () => {} }` on the meta; keep the WithMine / WithoutMine / Empty stories.
   - In `apps/web/components/chat/story-data.ts`, extend the `reactions` mock to include a non-legacy emoji (e.g. add `{ emoji: "🔥", count: 4, byMe: false }`) to prove the design handles arbitrary emoji. Keep it small.

8. Update `apps/web/app/(authenticated)/chat/chat-client.test.tsx` so the suite stays green with the new picker flow:
   - The pending-item assertion (~line 496) `queryByRole("button", { name: "React with thumbs up" })` -> change the expected name to `"Add a reaction"` (still expecting null on the pending item).
   - The "toggles reactions..." test (~line 892) currently does `fireEvent.click(screen.getAllByRole("button", { name: "React with thumbs up" })[0])` and expects `emoji: "👍"`. Rework it to exercise the picker: click the first `"Add a reaction"` trigger to open the panel, type a query into the picker search (`getByPlaceholderText` / role searchbox — e.g. "thumbs up") to filter to a single result, then click the emoji button by its `aria-label` (the emoji name, e.g. `"thumbs up"`), and assert `toggleReactionAction` was called with `{ messageId: "message-1", emoji: "👍" }`. Searching first keeps the rendered DOM small and deterministic in jsdom. Do not assert on realtime read-receipt behavior differently than today — keep the rest of that test intact.
   - Do NOT change `actions.test.ts` (backend contract unchanged; emoji "👍" still valid under max(16)).

Verify the whole app typechecks and builds. `pnpm build` must pass (it also typechecks shared packages).
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- --run app/\(authenticated\)/chat/chat-client.test.tsx app/\(authenticated\)/chat/actions.test.ts && grep -q "cursor: pointer" app/globals.css && grep -q "EmojiPickerButton" components/chat/reactions/reactions.tsx && grep -q "IconMoodSmile" "app/(authenticated)/chat/chat-client.tsx" && ! grep -q "IconThumbUp" "app/(authenticated)/chat/chat-client.tsx"</automated>
  </verify>
  <done>`pnpm build` passes; chat-client.test.tsx and actions.test.ts are green; the hover bar opens the picker from a smiley (no IconThumbUp); the reactions row shows a trailing add-pill; the picker is grouped, searchable, monochrome, and keyboard-operable; the single globals cursor rule is present; any emoji selection routes through handleToggleReaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → chat-command edge / toggle_message_reaction RPC | Untrusted emoji string crosses into persistence |
| npm registry → repo | Third-party emoji dataset enters the build (supply chain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-nr2-01 | Denial of Service | emoji field (oversized payload) | mitigate | Length bound `char_length(p_emoji) > 16` in RPC + DB check constraint `between 1 and 16` + zod `.max(16)` already in actions.ts |
| T-nr2-02 | Tampering | emoji value (empty/whitespace) | mitigate | RPC rejects null/blank via `btrim(p_emoji) = ''`; edge function already trims |
| T-nr2-03 | Elevation/Injection | RPC search_path | accept | Existing RPC already runs `security definer set search_path = ''` with fully-qualified names; unchanged |
| T-nr2-04 | Information disclosure (XSS) | emoji string rendered in Reactions/picker | mitigate | Rendered as React text children (auto-escaped); no dangerouslySetInnerHTML |
| T-nr2-SC | Tampering | `unicode-emoji-json` npm install | mitigate | slopcheck + blocking human checkpoint (Task 2) verifying legitimacy on npmjs.com before install |
</threat_model>

<verification>
- `pnpm build` passes from repo root (typechecks web + shared packages).
- Targeted vitest run for chat-client.test.tsx and actions.test.ts is green.
- Migration 0015 exists, additive, no 4-emoji whitelist in constraint or RPC, preserves `reaction is not supported` exception text.
- globals.css has exactly one base-layer cursor-pointer rule; no scattered `cursor-pointer` utility classes were added.
- Grep confirms `IconThumbUp` removed from chat-client and `IconMoodSmile` + `EmojiPickerButton` wired.
- Manual (optional): run `pnpm storybook` and open Chat/EmojiPicker + Chat/Reactions to confirm the monochrome pill + add-pill geometry matches the reference and no color hue appears in structural UI.
</verification>

<success_criteria>
- Reaction pills copy the screenshot geometry (emoji + count pill, adjacent smiley add-pill) using only FISH monochrome tokens.
- The add-pill and the hover-bar smiley both open a grouped, searchable, keyboard-operable monochrome emoji picker.
- Any emoji (including multi-codepoint sequences ≤16 chars) can be toggled and persists; the 4-emoji whitelist is gone from both the DB check constraint and the RPC.
- All clickables are cursor-pointer via a single globals base rule.
- Stories reflect the new design; chat-client.test.tsx and actions.test.ts still pass.
</success_criteria>

<output>
Create `.planning/quick/260708-nr2-redesign-chat-emoji-reactions-screenshot/260708-nr2-SUMMARY.md` when done.
</output>
