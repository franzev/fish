---
quick_id: 260708-n53
description: Make the chat occupy the full shell pane width and height, simplify its tailwind classes, and remove redundant wrapper divs
date: 2026-07-08
status: complete
---

# Summary — 260708-n53

## What changed

1. **Immersive shell for /chat** — `apps/web/components/shell/app-shell.tsx`
   derives `immersive = isActivePath(pathname, "/chat")` (it already reads
   usePathname for nav state). Immersive: root `h-dvh` (else `min-h-dvh`),
   main `flex min-h-0 w-full flex-1 flex-col pb-mobile-nav-offset md:pb-0` —
   no `max-w-content`, no page padding. Other routes keep the centered D-10
   column untouched. New test asserts the /chat variant; the D-10 test
   still covers the default.

2. **Simplified chat section** —
   `apps/web/app/(authenticated)/chat/chat-client.tsx` section is now
   `flex min-h-0 w-full flex-1 flex-col` (drops `min-h-chat-container-demo`
   and redundant `bg-bg`). With the bounded pane, the existing
   `flex-1 overflow-y-auto` log scrolls internally and the composer stays
   pinned — Discord behavior.

3. **Wrapper removal** — the notice `<div className="border-t ...">` around
   `Alert` is gone; `Alert` (accepts HTMLAttributes) carries `mx-md mb-xs`
   directly. Kit `ChatContainer` dropped its redundant `bg-bg`.

## Verification

- vitest: 49 files, 376 tests passed (includes new immersive-shell test).
- `pnpm build` passes (core, supabase, web).
- /kit/chat production preview renders clean, no console errors. The real
  /chat shell behavior is covered by the unit test (auth wall prevents
  browser verification; .env access declined).
