---
quick_id: 260704-dn2
status: planned
created: 2026-07-04
description: go with option 1: implement native Android static Compose UI preview for current web auth screens, no auth wiring yet
---

# Quick Task 260704-dn2 Plan

Build a native Android Compose UI-only preview that mirrors the current web auth surface closely enough to review on a phone. Auth behavior stays out of scope.

## Task 1: Enable Compose

- Files: `apps/android/gradle/libs.versions.toml`, `apps/android/build.gradle.kts`, `apps/android/app/build.gradle.kts`, Android manifest/theme resources.
- Action: Add Kotlin, Compose compiler, Activity Compose, Compose BOM, Material 3, and preview tooling. Switch the app theme to a no-action-bar launcher-safe theme.
- Verify: Gradle can resolve and compile the app module.

## Task 2: Add Design System Wrappers

- Files: `apps/android/app/src/main/java/space/fishhub/app/designsystem/**`.
- Action: Add unprefixed `Theme`, `Tokens`, `Button`, `TextField`, `Card`, `Alert`, and `Progress` wrappers. Mirror web token roles and 56dp controls.
- Verify: Product screens import wrappers rather than raw Material components for core UI.

## Task 3: Add UI Preview Screens

- Files: `apps/android/app/src/main/java/space/fishhub/app/MainActivity.kt`, `apps/android/app/src/main/java/space/fishhub/app/ui/**`.
- Action: Add a static local preview shell with tabs for Log in, Create account, Check inbox, Reset password, and Signed in.
- Verify: `./gradlew app:assembleDebug` passes with Android Studio's bundled JBR.
