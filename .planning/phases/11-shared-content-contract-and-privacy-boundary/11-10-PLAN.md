---
phase: 11-shared-content-contract-and-privacy-boundary
plan: 10
type: execute
wave: 5
depends_on:
  - 11-04
  - 11-08
files_modified:
  - apps/ios/FishKit/Sources/ChatCore/SharedContent/SharedContentState.swift
  - apps/ios/FishKit/Sources/TestSupport/Fixtures/SharedContentVectors.swift
  - apps/ios/FishKit/Sources/TestSupport/Resources/shared-content-vectors.json
  - apps/ios/FishKit/Tests/ChatCoreTests/SharedContentContractTests.swift
autonomous: true
gap_closure: true
skip_ui: true
scope_exclusions:
  - "Phase 12 data/cache/recovery, delivery URLs, and offline behavior"
  - "Phase 13 gallery UI and navigation"
  - "Phase 14 preview/export/source navigation and user-facing deletion"
  - "Phase 15 accessibility, native UI parity, and release surfaces"
requirements:
  - PAGE-02
  - PAR-01
must_haves:
  truths:
    - "Per D-04, Swift rejects another conversation's page/realtime payload and applies only the completion matching the active request ID, cursor, and replace mode."
    - "Per D-07, Swift resolves every fixture item with a throwing lookup and compares complete normalized items, pages, cursors, capabilities, statuses, purge, and deletion projections."
    - "The intended iOS simulator test target—not unavailable host SwiftPM—is automated and green before the separate human gate."
    - "The committed SwiftPM fixture remains byte-identical to the updated canonical TypeScript corpus."
  artifacts:
    - path: "apps/ios/FishKit/Sources/ChatCore/SharedContent/SharedContentState.swift"
      provides: "Codable request-sequenced Swift shared-content reducer"
    - path: "apps/ios/FishKit/Sources/TestSupport/Fixtures/SharedContentVectors.swift"
      provides: "Strict decoding for canonical request/sequencing vectors"
    - path: "apps/ios/FishKit/Tests/ChatCoreTests/SharedContentContractTests.swift"
      provides: "Non-tautological complete Swift parity assertions"
  key_links:
    - from: "packages/core/src/shared-content/fixtures/shared-content-vectors.json"
      to: "Sources/TestSupport/Resources/shared-content-vectors.json"
      via: "existing byte-copy/check script"
      pattern: "shared-content-vectors"
    - from: "SharedContentEvent.requestStarted"
      to: "SharedContentState.pendingPageRequest"
      via: "Codable request token consumed once by matching completion"
      pattern: "requestStarted|pendingPageRequest"
---

<objective>
Port the corrected D-04/D-07 ownership and request sequencing to Swift and replace weak parity assertions with complete evidence.

Purpose: PAR-01 is blocked by silent `compactMap`, partial page/cursor comparisons, a tautological status assertion, and the same stale/cross-conversation reducer weakness.
Output: Updated pure Swift contract, strict fixture decoder/test, byte-synced fixture, and a green iOS simulator suite.
</objective>

<execution_context>
@/Users/franz/.codex/gsd-core/workflows/execute-plan.md
@/Users/franz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-CONTEXT.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-VERIFICATION.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-04-SUMMARY.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-08-SUMMARY.md
@packages/core/src/shared-content/types.ts
@packages/core/src/shared-content/state.ts
@packages/core/src/shared-content/fixtures/shared-content-vectors.json
@apps/ios/FishKit/Sources/ChatCore/SharedContent/SharedContentState.swift
@apps/ios/FishKit/Sources/TestSupport/Fixtures/SharedContentVectors.swift
@apps/ios/FishKit/Tests/ChatCoreTests/SharedContentContractTests.swift
@scripts/sync-ios-chat-vectors.mjs
@package.json

<interfaces>
- Swift wire values must match TypeScript `requestStarted`, `requestId`, `requestedCursor`, `replace`, and `pendingPageRequest` exactly.
- The existing root `pnpm ios:test` invokes `xcodebuild test -scheme FishKit-Package` against `${FISH_IOS_SIM:-iPhone 17 Pro}` and is the intended UIKit-capable target.
- Host `swift test` remains non-authoritative because the package graph imports UIKit; do not weaken imports or duplicate the target to make host SwiftPM pass.
- `SharedContentState.swift` remains Foundation-only, `Codable`, `Sendable`, `Equatable`, and free of SwiftUI/provider/persistence/network dependencies.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce strict Swift parity and request-owned continuation</name>
  <files>apps/ios/FishKit/Sources/ChatCore/SharedContent/SharedContentState.swift, apps/ios/FishKit/Sources/TestSupport/Fixtures/SharedContentVectors.swift, apps/ios/FishKit/Sources/TestSupport/Resources/shared-content-vectors.json, apps/ios/FishKit/Tests/ChatCoreTests/SharedContentContractTests.swift</files>
  <read_first>
    - `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-08-SUMMARY.md` — final portable request symbols, semantics, and fixture count
    - `packages/core/src/shared-content/types.ts` — canonical JSON field names
    - `packages/core/src/shared-content/state.ts` — canonical reducer rejection/mutation order
    - `packages/core/src/shared-content/fixtures/shared-content-vectors.json` — sole expected semantics
    - `apps/ios/FishKit/Sources/ChatCore/SharedContent/SharedContentState.swift` — current Swift model/reducer
    - `apps/ios/FishKit/Sources/TestSupport/Fixtures/SharedContentVectors.swift` — current fixture Codable schema
    - `apps/ios/FishKit/Tests/ChatCoreTests/SharedContentContractTests.swift` — compactMap, partial projection, and tautological assertions to replace
    - `scripts/sync-ios-chat-vectors.mjs` — existing byte-copy/check behavior
    - `package.json` — authoritative `ios:test` simulator command
  </read_first>
  <behavior>
    - Missing/unknown item IDs and unsupported event/projection keys throw or record a test issue.
    - Full item/page/cursor/capability/status values equal canonical expectations; status is never compared to itself.
    - Cross-conversation initial/page/realtime payloads are no-ops.
    - Only the matching pending request completion updates continuation once; stale/duplicate/wrong-cursor/wrong-mode completions are no-ops.
  </behavior>
  <action>
Run `pnpm ios:chat-vectors` to copy the updated canonical bytes, then update `SharedContentVectors.swift` Codable structures for the new request and expected state fields without defaults that hide missing required data. In `SharedContentContractTests.swift`, replace both `compactMap` materializers with throwing `requireFixtureItem(id, vectorName)` helpers using `#require`; make unknown event/projection keys fail. Compare full `SharedContentItem` values, complete `SharedContentPage` arrays (nested items, `hasMore`, cursor), exact permissions/capabilities, expected gallery status raw values, pending request, `nextCursor`, `hasMore`, categories, references, errors, and tombstones. Remove the tautological `status.rawValue == status.rawValue` and boolean-only empty/nonempty page checks.

Add `SharedContentPageRequest`, `pendingPageRequest`, `.requestStarted`, and request fields on initial/page completion to the Swift Codable model with exact keys/discriminators. Port the TypeScript reducer sequence exactly: envelope ownership, pending request ID/cursor/mode equality, all-item conversation validation, whole-event rejection, accepted merge, one-time request consumption, continuation update, tombstone guard, and identity purge. Preserve decoded order; no locale sorting or partial acceptance.

Run byte drift check and `pnpm ios:test` against the iOS simulator, plus canonical TypeScript replay and workspace build. Do not use host `swift test` as substitute evidence and do not add UI, persistence, networking, or a second fixture corpus.
  </action>
  <verify>
    <automated>pnpm ios:chat-vectors:check &amp;&amp; pnpm ios:test &amp;&amp; node --experimental-strip-types --test packages/core/src/shared-content/shared-content.test.ts &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - The copied resource is byte-identical and the expected fixture count/version matches TypeScript.
    - No `compactMap` or silent missing-item path remains in shared-content parity materialization.
    - Status, capabilities, items, pages, cursor, pending request, `hasMore`, and every declared state projection compare expected fixture data to actual values.
    - Cross-conversation and every stale/duplicate/out-of-order completion case preserves exact expected state.
    - `SharedContentState.swift` remains pure Foundation and retains `Codable, Sendable, Equatable` public values.
    - `pnpm ios:test` passes the intended simulator target; fixture drift, canonical TypeScript replay, and build also pass.
  </acceptance_criteria>
  <done>Swift provides strict complete parity evidence and the same conversation/request safety as TypeScript and Kotlin.</done>
</task>

</tasks>

## Artifacts this phase produces

- New Swift type: `SharedContentPageRequest`.
- New Swift state field: `SharedContentState.pendingPageRequest`.
- New Swift event case: `SharedContentEvent.requestStarted`.
- Modified Swift event associated values: request ID/requested cursor on initial/page completion.
- Modified Swift behavior: `SharedContentReducer.reduce` and `SharedContentReducer.apply`; no new reducer entry point.
- Modified files: `apps/ios/FishKit/Sources/ChatCore/SharedContent/SharedContentState.swift`, `apps/ios/FishKit/Sources/TestSupport/Fixtures/SharedContentVectors.swift`, `apps/ios/FishKit/Sources/TestSupport/Resources/shared-content-vectors.json`, `apps/ios/FishKit/Tests/ChatCoreTests/SharedContentContractTests.swift`.
- CLI flags: none; existing `FISH_IOS_SIM` environment override remains supported by `pnpm ios:test`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canonical JSON -> Swift Codable | Defaults or compactMap can silently erase missing privacy cases. |
| event envelope -> item payload | A correct envelope can contain another conversation's content. |
| iOS async request -> completion | Duplicate/reordered callbacks can regress pagination. |
| host test -> intended iOS target | A skipped/unavailable host target cannot be treated as simulator evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-10-01 | Information disclosure | Swift page/realtime merge | mitigate | Validate all item conversations and reject whole event before mutation. |
| T-11-10-02 | Tampering | Swift continuation | mitigate | Exact pending request ID/cursor/mode, one-time consumption, stale/duplicate vectors. |
| T-11-10-03 | Tampering | Swift parity proof | mitigate | Throwing item lookup, exhaustive projection switch, full Equatable comparisons, byte drift, fixed count. |
| T-11-10-04 | Repudiation | test target substitution | mitigate | Automated `xcodebuild` simulator target required; host SwiftPM result is explicitly non-authoritative. |
| T-11-10-SC | Tampering | package supply chain | mitigate | No SwiftPM/pnpm dependency or lockfile modification. |

## OWASP ASVS v5.0.0 Level 1 Mapping

Authoritative baseline: OWASP Application Security Verification Standard v5.0.0, Level 1 (`https://github.com/OWASP/ASVS/tree/v5.0.0`). Every HIGH row is blocking and must reach `RESOLVED-BY-GATE` in the intended simulator command before Plan 11; no HIGH threat is accepted or transferred.

| Threat ID | Severity | ASVS v5.0.0 L1 control and title | Required resolution | Verification evidence |
|---|---|---|---|---|
| T-11-10-01 | HIGH | `v5.0.0-8.2.2` — General Authorization Design: data-specific access is restricted to consumers with explicit permissions | RESOLVED-BY-GATE | Task 1 canonical simulator replay proves mixed/mismatched initial, page, and realtime events preserve complete Swift state. |
| T-11-10-02 | HIGH | `v5.0.0-2.3.1` — Business Logic Security: flows follow the expected sequential step order without skipped steps | RESOLVED-BY-GATE | Task 1 stale A/B, duplicate, wrong-cursor, wrong-mode, identity, and tombstone vectors prove only the active request completes once. |
| T-11-10-03 | MEDIUM | `v5.0.0-2.2.1` — Input Validation: security-decision input enforces defined expectations | RESOLVED-BY-GATE | Task 1 removes compactMap/default hiding, fails unknown discriminators/projections, and compares complete values with byte-identical fixed-count corpus. |
| T-11-10-04 | MEDIUM | `v5.0.0-2.2.1` — Input Validation: security-decision input enforces defined expectations | RESOLVED-BY-GATE | Task 1 automated `pnpm ios:test` must name an iOS Simulator and `FishKit-Package`; host SwiftPM cannot satisfy the gate, and Plan 12 repeats human confirmation. |
| T-11-10-SC | LOW | `v5.0.0-15.2.1` — Security Architecture and Dependencies: components remain within documented update/remediation timeframes | RESOLVED-BY-GATE | Task 1 changes no SwiftPM/pnpm dependency or lockfile and passes drift, simulator, canonical, and build gates. |
</threat_model>

<verification>
- Byte-identical fixture check.
- Intended iOS simulator `pnpm ios:test`.
- Canonical TypeScript replay and workspace build.
</verification>

<success_criteria>
- Swift rejects cross-conversation payloads and stale completions.
- Swift tests fail loudly on missing fixture data and compare complete expected values.
- Intended simulator automation passes with byte-identical corpus.
</success_criteria>

<output>
Create `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-10-SUMMARY.md` when done.
</output>
