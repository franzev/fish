---
phase: 11-shared-content-contract-and-privacy-boundary
plan: 11
type: execute
wave: 6
depends_on:
  - 11-06
  - 11-07
  - 11-08
  - 11-09
  - 11-10
files_modified:
  - scripts/verify-shared-content.ts
  - package.json
autonomous: true
gap_closure: true
skip_ui: true
scope_exclusions:
  - "Phase 12 data/cache/recovery, delivery URLs, and offline behavior"
  - "Phase 13 gallery UI and navigation"
  - "Phase 14 preview/export/source navigation and user-facing deletion"
  - "Phase 15 accessibility, native UI parity, and release surfaces"
requirements:
  - DISC-03
  - PRIV-01
  - PAGE-01
  - PAGE-02
  - PAR-01
user_setup:
  - service: supabase
    why: "Run destructive-but-cleaned isolated adversarial fixtures against the linked project after migration/function deployment"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens"
      - name: SUPABASE_LINKED_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: SUPABASE_LINKED_PUBLISHABLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API keys -> Publishable key"
      - name: SUPABASE_LINKED_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API keys -> Secret/service-role key"
must_haves:
  truths:
    - "Per D-01 through D-03, exact local and linked cases reject unproven, malformed, private, mismatched-host, and DNS-unsafe link identities, preserve the original first-safe canonical pair, and prove optional redirect/enrichment egress is disabled."
    - "Per D-04, canonical TypeScript/Kotlin/Swift cases prove cross-conversation payload rejection and stale/duplicate/out-of-order completion safety without cursor/hasMore regression."
    - "Per D-05 and D-06, exact deleted-neighbor context and legacy tombstone cleanup fixtures pass locally and on the corrected linked schema."
    - "Per D-07, native/portable suites compare complete items, pages, cursors, capabilities, statuses, purge, and deletion projections and fail on missing fixture identities."
    - "Per D-08, the live verifier uses UTF-8/C byte ordering, deep equality for every normalized row field, and parsed JSON EXPLAIN limits rather than IDs/counts/text tokens."
    - "PAGE-01 remains proven by raw 40+1 behavior and PAGE-02 by complete exact multi-page rows plus request-sequenced client state."
  artifacts:
    - path: "scripts/verify-shared-content.ts"
      provides: "Strict local/linked adversarial verifier with complete rows, C ordering, deleted-neighbor/legacy cleanup, and quantitative JSON plan gates"
    - path: "package.json"
      provides: "Separate root commands for local and linked isolated shared-content verification"
      contains: "verify:shared-content:linked"
  key_links:
    - from: "scripts/verify-shared-content.ts --target=linked"
      to: "linked migration/function deployment"
      via: "temporary real Auth identities and isolated conversation fixtures after 0062/function deployment"
      pattern: "target.*linked|SUPABASE_LINKED"
    - from: "reference normalized rows"
      to: "list_conversation_shared_content pages"
      via: "UTF-8 byte comparator and full nullable-field deep equality"
      pattern: "TextEncoder|normalized"
    - from: "EXPLAIN FORMAT JSON"
      to: "bounded query contract"
      via: "parsed relation node types plus 40+1-derived rows-examined/removed and shared-buffer budgets"
      pattern: "format json|Actual Rows|Rows Removed by Filter|Relation Name|Shared Hit Blocks"
---

<objective>
Replace passing-but-insufficient evidence with discriminating local and linked proof of every Phase 11 correction, implementing D-08 and re-proving D-01 through D-07.

Purpose: Phase 11 cannot pass re-verification while locale-sensitive, ID-only, shallow-plan, local-only, or weak native assertions can miss the documented defects.
Output: Strict `verify:shared-content` and `verify:shared-content:linked` gates plus one full cross-platform automated result.
</objective>

<execution_context>
@/Users/franz/.codex/gsd-core/workflows/execute-plan.md
@/Users/franz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-CONTEXT.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-VERIFICATION.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-05-SUMMARY.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-06-SUMMARY.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-07-SUMMARY.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-08-SUMMARY.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-09-SUMMARY.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-10-SUMMARY.md
@scripts/verify-shared-content.ts
@scripts/verify-shared-content-migration.ts
@packages/core/src/shared-content/fixtures/shared-content-vectors.json
@package.json

<interfaces>
- Local target continues using `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, and `SUPABASE_SERVICE_ROLE_KEY` from `apps/web/.env.local`.
- Linked target uses only `SUPABASE_LINKED_URL`, `SUPABASE_LINKED_PUBLISHABLE_KEY`, and `SUPABASE_LINKED_SERVICE_ROLE_KEY`; never fall back across target namespaces.
- `list_conversation_shared_content` returns 28 normalized fields; strict equality includes every nullable payload and capability field, not only identity/category/kind.
- `supabase db query` supports `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)` as one prepared statement with the existing auth CTE.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make adversarial evidence exact, quantitative, and target-aware</name>
  <files>scripts/verify-shared-content.ts, package.json</files>
  <read_first>
    - `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-CONTEXT.md` — D-01..D-08 proof contract
    - `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-VERIFICATION.md` — every blocker/warning and human linked-fixture expectation
    - `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-06-SUMMARY.md` — final 0062 helper/field/trigger and migration-regression behavior
    - `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-07-SUMMARY.md` — final DNS/no-egress symbols plus deployed UUID/version/digest evidence
    - `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-08-SUMMARY.md` — canonical fixture version/count and request-token contract
    - `scripts/verify-shared-content.ts` — locale comparator, partial ExpectedItem, shallow EXPLAIN, local-only guard, and existing fixture cleanup
    - `scripts/verify-shared-content-migration.ts` — pre/post migration regression that final evidence must invoke/reference rather than duplicate
    - `packages/supabase/src/database.generated.ts` — exact 0062 table/RPC fields
    - `packages/core/src/shared-content/fixtures/shared-content-vectors.json` — strict portable/native corpus metadata
    - `package.json` — existing local command and new linked command target
  </read_first>
  <action>
Add CLI parser accepting exactly `--target=local` (default) or `--target=linked`; unknown/duplicate target flags exit nonzero. Resolve credentials from the target-specific variables above and keep the current hard local-host assertion for local mode. Linked mode must require all linked variables, require `SUPABASE_ACCESS_TOKEN`, check migration history contains 0062, and create/delete temporary real Auth identities and every fixture row/object in `finally`. Add root `verify:shared-content:linked` invoking `scripts/verify-shared-content.ts --target=linked`; keep the local command deterministic.

Replace `ExpectedItem` with a complete normalized-row expectation covering all 28 RPC fields with explicit nulls. Implement a UTF-8 byte comparator using `TextEncoder` for the C-collated text tie-breaker; do not call `localeCompare`. Compare each raw page and the concatenated retained sequence against full normalized rows in exact order, including `conversation_id`, sender/source fields, category/kind, every attachment/GIF/sticker/link field, and both capabilities. Seed non-ASCII/codepoint tie values where the contract permits and keep UUID/time comparisons exact. Fail on the first field mismatch with a stable field name only, never values.

Extend adversarial fixtures: unproven public legacy preview is stored but omitted; private/mismatched-host version-2 direct writes are rejected; a canonical version-2 link remains visible; and the linked `send-message` path creates no preview job or fetched metadata while preserving its URL/hostname/proof. Unsafe DNS, redirect, and rebinding cases are covered by Plan 07's injected resolver/no-fetch suite and are reported only after that suite passes; assert global fetch count is zero and legacy jobs end as `preview_fetch_disabled`. Create a tombstoned source then bind an attachment with a null requested marker and assert the 0062 trigger/backfill invariant makes it claimable. Create exact live target/older/newer messages with deleted rows immediately adjacent, assert returned IDs are precisely target plus live neighbors, deleted IDs are absent, and both gap flags reflect the live set. Assert zero residual linked attachments where source is deleted and marker is null.

Change EXPLAIN to `FORMAT JSON`, parse the complete plan tree, and reject malformed/text evidence. Define named budgets from the locked contract rather than a fixture-sized constant: `pageSlots = 40 + 1`; `messageBranchFactor = 8` (the conservative maximum message-relation work factor across the four eligible source branches plus membership/join probes); `pageMessageRowsBudget = pageSlots * messageBranchFactor = 328`; `perMessageNodeBudget = pageSlots * 2 = 82`; and `pageSharedBlocksBudget = pageSlots * messageBranchFactor = 328`. For each unfiltered, filtered-category, and deep-cursor list plan require root `Actual Rows &lt;= 41` and `Actual Loops == 1`; recursively locate every node whose `Relation Name` is `messages`; reject `Seq Scan`; calculate examined work as `(Actual Rows + Rows Removed by Filter + Rows Removed by Index Recheck) * Actual Loops`; require every such node at most 82 and their sum at most 328; fail explicitly if any node or aggregate reaches half of the 2,000-message fixture (near-full) even if its node type is an index scan; and require total shared hit+read blocks across the tree at most 328 as secondary evidence. Require the purpose-built live-message/eligible-attachment indexes from Plan 06 to appear in the relevant scan nodes rather than accepting an arbitrary index name.

For category availability derive separate budgets from the four-category contract: `categorySlots = 4`, `categoryBranchFactor = 8`, `categoryMessageRowsBudget = 32`, `perCategoryMessageNodeBudget = 8`, and `categorySharedBlocksBudget = 32`; require root rows at most 4, one loop, no `messages` sequential scan, each messages node examined work at most 8, aggregate messages work at most 32, the same half-fixture fail-safe, and total shared hit+read blocks at most 32. Treat missing numeric fields as zero only where PostgreSQL omits an inapplicable counter; missing node type/relation/index evidence is a hard failure. Retain complete normalized reference-row equality so plan efficiency cannot mask semantic drift. Keep generated type byte drift local; linked mode instead proves migration 0062, the exact Plan-07 deployed UUID/version/digest tuple, and behavior. Stable output contains case names, formulas, derived ceilings, node types/index names, and safe counts only—never user IDs, messages, URLs/hostnames, DNS answers, paths, tokens, plan SQL, or row values.
  </action>
  <verify>
    <automated>pnpm verify:shared-content &amp;&amp; node --experimental-strip-types --test supabase/functions/_shared/link-preview.test.ts &amp;&amp; node --experimental-strip-types --test packages/core/src/shared-content/shared-content.test.ts &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - No `localeCompare` remains; C ordering is bytewise and covered by a discriminating tie fixture.
    - Every list/page/reference assertion compares complete normalized rows with explicit nullable fields.
    - Unsafe/unproven/mismatched proof, canonical no-egress identity, disabled legacy preview jobs, legacy cleanup trigger, deleted-neighbor context, and residual cleanup invariants have named failing cases.
    - EXPLAIN is parsed JSON; no `messages` sequential scan is accepted; rows examined/removed are bounded at 82 per node and 328 per list plan (8/32 for category availability); shared buffers use the derived 328/32 ceilings; and the half-fixture near-full fail-safe rejects disguised full scans. Token/length and legacy oversized-buffer checks are gone.
    - Local and linked targets use disjoint variables and isolated cleanup; any fixture/setup/assertion failure exits nonzero.
    - Root linked command exists and no secret/private value is printed or persisted.
    - Local verifier, link suite, canonical core suite, and build exit 0.
  </acceptance_criteria>
  <done>The verifier can detect the exact defects that previously passed and can exercise the corrected linked deployment safely.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Pass linked adversarial and cross-platform gates</name>
  <files>None — this task executes the committed verification/deployment contract and records safe results only.</files>
  <read_first>
    - `scripts/verify-shared-content.ts` — exact local/linked isolation, assertions, and cleanup behavior
    - `scripts/verify-shared-content-migration.ts` — local pre/post 0062 proof
    - `package.json` — local/linked/native gate commands
    - `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-06-SUMMARY.md` — actual linked migration push evidence
    - `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-07-SUMMARY.md` — actual linked function deployment evidence
  </read_first>
  <action>
Fail closed unless all linked credentials and `SUPABASE_ACCESS_TOKEN` are present. Run, in order: confirm `supabase functions list --output json` still reports the exact ACTIVE `send-message` and `link-preview` UUID/version/`ezbr_sha256` tuples captured by Plan 07; the local pre/post migration regression; local strict shared-content verifier; linked strict shared-content verifier; link-preview DNS/no-fetch unit suite; canonical TypeScript suite; focused Android parity; iOS fixture drift; intended iOS simulator `pnpm ios:test`; build; lint; typecheck. Do not skip the linked command, substitute source inspection, downgrade a nonzero result, or expose fixture/secrets. Confirm each verifier's `finally` cleanup succeeded and `supabase db push --linked --dry-run --yes` is empty after the run. Record only command names, exit status, deployment UUID/version/digest, migration 0062 parity, fixture case count, derived plan ceilings, and safe PASS/FAIL totals.
  </action>
  <verify>
    <automated>test -n "$SUPABASE_ACCESS_TOKEN" &amp;&amp; test -n "$SUPABASE_LINKED_URL" &amp;&amp; test -n "$SUPABASE_LINKED_PUBLISHABLE_KEY" &amp;&amp; test -n "$SUPABASE_LINKED_SERVICE_ROLE_KEY" &amp;&amp; node --experimental-strip-types --env-file=apps/web/.env.local scripts/verify-shared-content-migration.ts &amp;&amp; pnpm verify:shared-content &amp;&amp; pnpm verify:shared-content:linked &amp;&amp; node --experimental-strip-types --test supabase/functions/_shared/link-preview.test.ts &amp;&amp; node --experimental-strip-types --test packages/core/src/shared-content/shared-content.test.ts &amp;&amp; scripts/android-gradle.sh :feature:chat:testDebugUnitTest --tests '*SharedContentParityTest' &amp;&amp; pnpm ios:chat-vectors:check &amp;&amp; pnpm ios:test &amp;&amp; pnpm build &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Local migration regression proves a real 0061 fixture is corrected by 0062.
    - Local and linked adversarial commands both exit 0 with exact rows, safe links, context, cleanup, authorization, and query-plan cases passing.
    - Linked migration dry run is empty and history includes 0062; deployed Edge UUID/version/digest tuples exactly match Plan 07's captured no-egress revisions.
    - TypeScript, Android, and intended iOS simulator replay the same strict fixed-count corpus successfully.
    - Build, lint, and typecheck exit 0.
    - Temporary linked Auth users, rows, Storage objects, and sessions are removed; logs/summaries contain no sensitive values.
  </acceptance_criteria>
  <done>All automated Phase 11 database, Edge, portable, Android, and iOS evidence is green against both local and linked boundaries.</done>
</task>

</tasks>

## Artifacts this phase produces

- New CLI flag: `scripts/verify-shared-content.ts --target=local|linked` (`local` default).
- New root command: `verify:shared-content:linked` in `package.json`.
- New environment contract: `SUPABASE_LINKED_URL`, `SUPABASE_LINKED_PUBLISHABLE_KEY`, `SUPABASE_LINKED_SERVICE_ROLE_KEY` for linked-only isolated verification.
- Modified functions in `scripts/verify-shared-content.ts`: target/credential selection, UTF-8 C comparator, complete normalized-row comparator, JSON EXPLAIN plan-tree/node-work limits, and fixture cases; exact private helper names may remain file-private.
- Modified files: `scripts/verify-shared-content.ts`, `package.json`.
- No new production API, schema field, mobile surface, or UI artifact.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local/linked CLI target -> credentials | Target confusion could run destructive fixtures against the wrong project or leak service credentials. |
| service-role setup -> member assertions | Administrative access must not authorize the code path under test. |
| database rows -> expected comparator | ID-only equality can hide unsafe fields, kinds, capabilities, or cross-conversation payloads. |
| EXPLAIN output -> performance claim | Root counts or loose buffers can claim bounded work while a messages node still examines the full transcript. |
| verifier output -> logs/summary | Hosted private data and secrets must never become evidence text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-11-01 | Spoofing / information disclosure | target credential selection | mitigate | Exact target enum, disjoint env names, local-host assertion, linked 0062 check, fail on missing/mixed setup. |
| T-11-11-02 | Elevation of privilege | adversarial identities | mitigate | Temporary real Auth users via publishable sessions; service role only for setup/inspection/teardown. |
| T-11-11-03 | Information disclosure / SSRF | unsafe/legacy link proof | mitigate | Direct unproven/private/mismatched cases plus mandatory Plan-07 DNS/no-fetch suite, zero outbound requests, and canonical identity assertions. |
| T-11-11-04 | Tampering | row/order/page evidence | mitigate | UTF-8 C comparator, full 28-field deep equality, sentinel/retained cursor, no duplicates/gaps. |
| T-11-11-05 | Tampering / denial of service | EXPLAIN evidence | mitigate | Parse every node; reject messages Seq Scan/full-or-near-full work; enforce 40+1-derived examined/removed and buffer budgets while retaining exact reference rows. |
| T-11-11-06 | Information disclosure | linked fixture/log cleanup | mitigate | Isolated prefix, `finally` teardown, stable labels only, no values/SQL/credentials in logs. |
| T-11-11-SC | Tampering | package supply chain | mitigate | No installs or lock changes; existing runtimes only. |

## OWASP ASVS v5.0.0 Level 1 Mapping

Authoritative baseline: OWASP Application Security Verification Standard v5.0.0, Level 1 (`https://github.com/OWASP/ASVS/tree/v5.0.0`). Every HIGH row is blocking and must reach `RESOLVED-BY-GATE` in both local and linked commands before human verification; no HIGH threat is accepted or transferred.

| Threat ID | Severity | ASVS v5.0.0 L1 control and title | Required resolution | Verification evidence |
|---|---|---|---|---|
| T-11-11-01 | HIGH | `v5.0.0-2.2.1` — Input Validation: security-decision input enforces defined expectations; `v5.0.0-14.2.1` — General Data Protection: sensitive data is not placed in URLs/query strings | RESOLVED-BY-GATE | Task 1 exact target parser rejects unknown/duplicate/mixed setup, local host guard stays mandatory, and stable output omits credentials/URLs; Task 2 requires every linked variable. |
| T-11-11-02 | HIGH | `v5.0.0-8.3.1` — Operation Level Authorization: authorization is enforced at a trusted service layer; `v5.0.0-8.2.2` — General Authorization Design: data-specific access is explicitly permissioned | RESOLVED-BY-GATE | Task 1 real publishable-key member/outsider/other/former sessions perform reads while service role is confined to fixture setup/inspection/teardown; both targets must pass. |
| T-11-11-03 | HIGH | `v5.0.0-1.2.2` — Injection Prevention: dynamically built URLs allow only safe protocols; `v5.0.0-2.2.1` — Input Validation: security-decision input enforces defined expectations | RESOLVED-BY-GATE | Task 1 direct proof cases plus Plan 07 DNS/no-fetch suite assert unproven/private/mismatched absence, zero outbound fetch, disabled jobs, and immutable canonical identity locally and linked. |
| T-11-11-04 | HIGH | `v5.0.0-2.2.1` — Input Validation: security-decision input enforces defined expectations | RESOLVED-BY-GATE | Task 1 UTF-8 C comparator and complete 28-field reference/page equality cover first, category, deep cursor, sentinel, and concatenated sequences. |
| T-11-11-05 | HIGH | `v5.0.0-2.2.1` — Input Validation: business/security limits are positive, predefined, and enforced | RESOLVED-BY-GATE | Task 1 parsed JSON node walk rejects messages Seq Scan/full-or-near-full work and enforces formula-derived 82/328 and 8/32 examined/removed plus buffer budgets while exact rows remain equal. |
| T-11-11-06 | HIGH | `v5.0.0-15.3.1` — Defensive Coding: return only the required subset of fields | RESOLVED-BY-GATE | Task 1 stable field-name-only failures and `finally` teardown; Task 2 checks cleanup and records only deployment/migration IDs, counts, formulas, and PASS/FAIL totals. |
| T-11-11-SC | LOW | `v5.0.0-15.2.1` — Security Architecture and Dependencies: components remain within documented update/remediation timeframes | RESOLVED-BY-GATE | Tasks use existing runtimes only, reject install/lock drift, and finish with build/lint/typecheck. |
</threat_model>

<verification>
- Local pre/post migration regression.
- Strict local and linked adversarial commands.
- DNS/no-fetch, canonical TypeScript, Android, byte drift, and iOS simulator suites.
- Build, lint, typecheck, and empty linked migration dry run.
</verification>

<success_criteria>
- Every verification blocker/warning has a discriminating passing assertion.
- Corrected schema and Edge behavior are proven against the linked project after push/deploy.
- Complete row/order/plan evidence replaces IDs/counts/text heuristics.
- All three language implementations pass the same strict corpus.
</success_criteria>

<output>
Create `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-11-SUMMARY.md` when done.
</output>
