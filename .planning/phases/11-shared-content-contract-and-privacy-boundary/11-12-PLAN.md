---
phase: 11-shared-content-contract-and-privacy-boundary
plan: 12
type: execute
wave: 7
depends_on:
  - 11-11
files_modified: []
autonomous: false
gap_closure: true
skip_ui: true
scope_exclusions:
  - "Phase 12 data/cache/recovery, delivery URLs, and offline behavior"
  - "Phase 13 gallery UI and navigation"
  - "Phase 14 preview/export/source navigation and user-facing deletion"
  - "Phase 15 accessibility, native UI parity, and release surfaces"
requirements:
  - PAR-01
must_haves:
  truths:
    - "Per D-07, a human confirms the strict SharedContentContractTests run in the intended Xcode iOS simulator target with the canonical fixed case count and no skipped/unknown fixture data."
    - "The human gate verifies the same already-automated simulator result; it does not accept unavailable host SwiftPM as substitute evidence."
  artifacts:
    - path: ".planning/phases/11-shared-content-contract-and-privacy-boundary/11-12-SUMMARY.md"
      provides: "Recorded human acceptance of intended iOS simulator parity evidence"
  key_links:
    - from: "pnpm ios:test"
      to: "FishKit-Package SharedContentContractTests"
      via: "Xcode iOS Simulator destination selected by FISH_IOS_SIM or iPhone 17 Pro default"
      pattern: "SharedContentContractTests"
---

<objective>
Close the explicit human iOS-target verification gate after all automated local, linked, and cross-platform proof is green.

Purpose: The original verifier required confirmation in the intended UIKit-capable simulator target because host SwiftPM cannot compile the package graph.
Output: Human-approved simulator parity evidence; no production source or behavior change.
</objective>

<execution_context>
@/Users/franz/.codex/gsd-core/workflows/execute-plan.md
@/Users/franz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-CONTEXT.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-VERIFICATION.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-10-SUMMARY.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-11-SUMMARY.md
@apps/ios/FishKit/Tests/ChatCoreTests/SharedContentContractTests.swift
@package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm strict iOS parity in the intended simulator target</name>
  <files>None — verification-only checkpoint; do not edit source, fixtures, project files, or test expectations.</files>
  <read_first>
    - `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-VERIFICATION.md` — original iOS human verification requirement and expected result
    - `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-10-SUMMARY.md` — implemented strict Swift assertions and automated simulator result
    - `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-11-SUMMARY.md` — final automated cross-platform and linked gate result
    - `apps/ios/FishKit/Tests/ChatCoreTests/SharedContentContractTests.swift` — exact test suite being confirmed
    - `package.json` — `pnpm ios:test` Xcode simulator command/destination contract
  </read_first>
  <action>
Rerun `pnpm ios:chat-vectors:check` and `pnpm ios:test` without modifying files. Present the human with the Xcode test result for `SharedContentContractTests`, including simulator destination, canonical fixture version/fixed case count, tests executed, failures, and skips. Do not present host `(cd apps/ios/FishKit &amp;&amp; swift test ...)` as evidence. Do not reveal fixture content or private linked-verifier data.
  </action>
  <verify>
    <automated>pnpm ios:chat-vectors:check &amp;&amp; pnpm ios:test</automated>
    <human-check>
      1. Confirm the destination is an iOS Simulator (default `iPhone 17 Pro` unless `FISH_IOS_SIM` deliberately selects another available simulator).
      2. In the Xcode result, locate `SharedContentContractTests` and confirm every strict canonical test passed with zero failures and zero skipped tests.
      3. Confirm output reports the canonical fixture version/fixed case count from Plan 08 and no unknown/missing fixture-item issue.
      4. Confirm this evidence came from `FishKit-Package` simulator `xcodebuild test`, not host SwiftPM.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Fixture byte drift check exits 0 immediately before the simulator run.
    - `pnpm ios:test` exits 0 on an iOS Simulator destination.
    - `SharedContentContractTests` has zero failures and zero skips and reports the canonical fixed count.
    - Human explicitly types `approved`; silence, host SwiftPM failure/skip, or partial suite output is not approval.
    - No source/test/fixture file changes during this checkpoint.
  </acceptance_criteria>
  <what-built>Strict Swift full-value fixture replay, request sequencing, conversation ownership, and cross-platform parity already implemented and automated in Plans 10-11.</what-built>
  <how-to-verify>Review the four points in `human-check` against the fresh `pnpm ios:test` result.</how-to-verify>
  <resume-signal>Type "approved" if all four checks pass, or describe the exact failing test/target/count.</resume-signal>
  <done>The intended iOS simulator parity target is human-approved with strict canonical evidence and no skipped fixture cases.</done>
</task>

</tasks>

## Artifacts this phase produces

- No new production symbol, field, function, CLI flag, or source file.
- Verification artifact only: `11-12-SUMMARY.md` records the simulator destination, strict suite result, fixed fixture count, zero skips/failures, and the human `approved` signal without private fixture data.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| automated output -> human acceptance | Ambiguous, skipped, or wrong-target output could be mistaken for parity proof. |
| host SwiftPM -> iOS simulator | The unavailable host target does not exercise the intended UIKit-capable package graph. |
| verification summary -> private data | Evidence must not copy linked fixtures, URLs, IDs, credentials, or message content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-12-01 | Spoofing / repudiation | target identity | mitigate | Require Xcode iOS Simulator destination and `FishKit-Package` suite name in fresh output. |
| T-11-12-02 | Tampering | skipped/partial parity suite | mitigate | Require fixed canonical count, zero failures, zero skips, and explicit human approval. |
| T-11-12-03 | Information disclosure | evidence summary | mitigate | Record only target, counts, status, and approval; no fixture values or linked data. |
| T-11-12-SC | Tampering | package supply chain | mitigate | Verification-only; no install or lockfile change. |

## OWASP ASVS v5.0.0 Level 1 Mapping

Authoritative baseline: OWASP Application Security Verification Standard v5.0.0, Level 1 (`https://github.com/OWASP/ASVS/tree/v5.0.0`). This verification-only plan has no HIGH threat; every MEDIUM row is nevertheless blocking and must reach `RESOLVED-BY-GATE` before approval. No threat is left unresolved.

| Threat ID | Severity | ASVS v5.0.0 L1 control and title | Required resolution | Verification evidence |
|---|---|---|---|---|
| T-11-12-01 | MEDIUM | `v5.0.0-2.2.1` — Input Validation: security-decision input enforces defined expectations | RESOLVED-BY-GATE | Task 1 fresh command plus human check requires iOS Simulator destination, `FishKit-Package`, and rejects host SwiftPM substitution. |
| T-11-12-02 | MEDIUM | `v5.0.0-2.2.1` — Input Validation: security-decision input enforces defined expectations | RESOLVED-BY-GATE | Task 1 requires canonical version/fixed count, zero skips, zero failures, no missing/unknown fixture issue, and explicit `approved`. |
| T-11-12-03 | MEDIUM | `v5.0.0-15.3.1` — Defensive Coding: return only the required subset of fields | RESOLVED-BY-GATE | Task 1 summary records only target, fixed counts, status, and approval; fixture/linked private values are forbidden. |
| T-11-12-SC | LOW | `v5.0.0-15.2.1` — Security Architecture and Dependencies: components remain within documented update/remediation timeframes | RESOLVED-BY-GATE | Task 1 is verification-only and accepts no install or lockfile change. |
</threat_model>

<verification>
- Fresh byte drift and intended iOS simulator suite are green.
- Human confirms target, strict suite, fixed count, zero skips/failures, and approves.
</verification>

<success_criteria>
- The authoritative iOS target has both automated and human-confirmed strict parity evidence.
- No host-target substitution or skipped fixture case is accepted.
- No source change occurs in this verification-only plan.
</success_criteria>

<output>
Create `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-12-SUMMARY.md` when approved.
</output>
