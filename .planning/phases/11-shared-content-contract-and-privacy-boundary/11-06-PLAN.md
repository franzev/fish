---
phase: 11-shared-content-contract-and-privacy-boundary
plan: 06
type: execute
wave: 4
depends_on:
  - 11-01
  - 11-05
files_modified:
  - supabase/migrations/0062_shared_content_privacy_hardening.sql
  - scripts/verify-shared-content-migration.ts
  - packages/supabase/src/database.generated.ts
autonomous: true
gap_closure: true
skip_ui: true
scope_exclusions:
  - "Phase 12 data/cache/recovery, delivery URLs, and offline behavior"
  - "Phase 13 gallery UI and navigation"
  - "Phase 14 preview/export/source navigation and user-facing deletion"
  - "Phase 15 accessibility, native UI parity, and release surfaces"
requirements:
  - DISC-03
  - PRIV-01
  - PAGE-01
  - PAGE-02
user_setup:
  - service: supabase
    why: "Apply corrective migration 0062 to the already-linked project after local migration regression passes"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens"
must_haves:
  truths:
    - "Per D-01 and D-03, legacy, direct service-role, or newly written link rows remain absent from member reads until they carry the current safe-link proof and a canonical URL/hostname pair."
    - "Per D-05, bounded source context returns only nondeleted target/neighbor messages and computes both gap flags over that same nondeleted set."
    - "Per D-06, attachments already bound to tombstoned messages receive a cleanup marker during migration and enter the same retry-safe claim path as newly deleted attachments."
    - "Per D-08 and PAGE-01, 0062 preserves exact 40+1 semantics while making each eligible-source branch and category probe stop through live-message/index predicates instead of scanning the ordinary transcript."
    - "The already-applied 0061 history is preserved; corrective schema is introduced only by 0062 and accepted by the linked database before later linked verification."
  artifacts:
    - path: "supabase/migrations/0062_shared_content_privacy_hardening.sql"
      provides: "Safe-link proof boundary, bounded indexed listing/category branches, legacy cleanup backfill, and nondeleted message-context correction"
      contains: "safe_link_validation_version"
    - path: "scripts/verify-shared-content-migration.ts"
      provides: "A before-0062 to after-0062 local migration regression with unsafe legacy links, tombstoned attachments, and deleted context neighbors"
    - path: "packages/supabase/src/database.generated.ts"
      provides: "Generated contract containing the linked/local 0062 fields"
      contains: "safe_link_validated_at"
  key_links:
    - from: "public.message_link_previews"
      to: "private.is_canonical_safe_link_identity"
      via: "RLS plus both shared-content projections require current proof and canonical URL/hostname equality"
      pattern: "is_canonical_safe_link_identity"
    - from: "public.messages.deleted_at"
      to: "public.message_attachments.delete_requested_at"
      via: "0062 idempotent legacy backfill plus a bound-to-tombstone write trigger before cleanup claims are evaluated"
      pattern: "delete_requested_at.*deleted_at|deleted_at.*delete_requested_at"
    - from: "public.list_conversation_message_context"
      to: "public.messages.deleted_at"
      via: "target, older, newer, older-gap, and newer-gap predicates all use deleted_at is null"
      pattern: "deleted_at is null"
---

<objective>
Correct the deployed database contract without altering applied migration history, implementing D-01, D-03, D-05, and D-06.

Purpose: Unsafe or unproven link identities, legacy tombstoned attachments, and deleted context neighbors currently violate DISC-03, PRIV-01, and PAGE-02 at the authoritative database boundary.
Output: Corrective migration 0062, an executable migration-state regression, a successful linked schema push, and freshly generated provider types.
</objective>

<execution_context>
@/Users/franz/.codex/gsd-core/workflows/execute-plan.md
@/Users/franz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-CONTEXT.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-VERIFICATION.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-01-SUMMARY.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-05-SUMMARY.md
@supabase/migrations/0058_chat_link_previews.sql
@supabase/migrations/0061_shared_content_contract.sql
@packages/supabase/src/database.generated.ts

<interfaces>
- Applied history ends at `0061_shared_content_contract.sql`; never edit or replay that deployed migration to close these gaps.
- Current link identity is `message_link_previews(message_id, url, hostname, nullable metadata...)`; 0062 adds proof without making unproven legacy rows visible.
- Existing cleanup selection requires `message_attachments.delete_requested_at is not null`.
- Existing context signature and return shape remain `list_conversation_message_context(uuid, uuid, integer, integer)`; only eligibility/gap semantics change.
- Existing list/category signatures and exact four-field ordering remain unchanged; 0062 may add purpose-built partial indexes and branch-local 40+1 limits because D-08 requires bounded indexed work, not a passing full-transcript scan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write corrective migration and a real pre/post migration regression</name>
  <files>supabase/migrations/0062_shared_content_privacy_hardening.sql, scripts/verify-shared-content-migration.ts</files>
  <read_first>
    - `AGENTS.md` — Supabase-only backend boundary and pnpm/build requirements
    - `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-CONTEXT.md` — locked D-01, D-03, D-05, and D-06 behavior
    - `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-VERIFICATION.md` — exact unsafe-link, legacy-cleanup, and deleted-neighbor failures
    - `supabase/migrations/0058_chat_link_previews.sql` — current link tables, grants, and direct-read policy
    - `supabase/migrations/0061_shared_content_contract.sql` — applied function signatures and predicates to replace safely
    - `scripts/verify-shared-content.ts` — isolated identity/fixture and safe-report conventions
    - `packages/supabase/src/database.generated.ts` — current provider contract before 0062
  </read_first>
  <action>
Create `0062_shared_content_privacy_hardening.sql`; do not modify 0061. Add nullable `message_link_previews.safe_link_validation_version smallint` and `safe_link_validated_at timestamptz`. Add immutable `private.is_canonical_safe_link_identity(p_url text, p_hostname text, p_validation_version smallint, p_validated_at timestamptz) returns boolean` that accepts only the current version `2`, a nonnull validation timestamp, HTTP(S), no credentials/fragment/whitespace/custom port, a lowercase canonical hostname exactly derived from the URL, and no localhost, `.local`, `.internal`, literal loopback/link-local/private/CGNAT/multicast address. Add a check constraint that permits an entirely unproven legacy pair to remain stored but rejects partial/malformed proof; do not mark any legacy row as version 2 automatically.

Replace the member link-preview SELECT policy and the link branches in both `list_conversation_shared_content` and `list_conversation_shared_content_categories` so they call the helper and omit every unproven or malformed row. Preserve the existing signatures, SECURITY INVOKER mode, membership checks, static union, four-field C-collated order, `p_limit + 1` sentinel, and grants. Do not parse message bodies or perform DNS inside PostgreSQL; Plan 07 supplies the version-2 proof only after address validation.

In the same function replacements, satisfy D-08/PAGE-01 with branch-bounded indexed retrieval rather than relying on a top-level limit after full union materialization. Push the four-field cursor predicate and descending source order into each eligible attachment/GIF/sticker/link branch, cap each branch at `p_limit + 1`, then retain the final union order/limit as the semantic authority. Drive source lookup from live messages in conversation order and add only the partial indexes required by parsed EXPLAIN evidence, including a live-message conversation/order index and a ready/bound/unmarked attachment lookup index; GIF/link primary-key joins remain indexed. Implement category availability as bounded `EXISTS`/one-row probes per fixed category rather than materializing every eligible item. Preserve complete reference-row equality, all seven supported kinds, link proof predicates, membership checks, and the row-40 plus sentinel contract; do not add a cached gallery table or transcript hydration.

Before recreating cleanup functions, idempotently update every bound attachment whose source `messages.deleted_at is not null` and whose marker is null, setting `delete_requested_at` from the tombstone timestamp (falling back to transaction time only if required by legacy data) and updating `updated_at`. Add private trigger function `private.enforce_deleted_source_attachment_cleanup()` and trigger `message_attachments_deleted_source_cleanup` so a later service/import write that binds an attachment to an already tombstoned source receives the same marker rather than recreating an unclaimable legacy state. The trigger must not alter live-source or unbound attachment semantics. Replace `list_conversation_message_context` so `message.deleted_at is null` appears in the target, both neighbor CTEs, and both gap probes. Preserve generic unauthorized/missing/deleted behavior, bounds, result fields, and ascending display order.

Create `scripts/verify-shared-content-migration.ts` that runs only against local Supabase and orchestrates a real transition: reset through version 0061, create isolated service-role fixtures for an unproven private-host preview, an attachment bound to an already tombstoned message with a null marker, and live target messages with immediately adjacent deleted rows; run `supabase migration up --local`; assert the unsafe/unproven link is absent from direct member/RPC projections, the legacy attachment is claimable, and context returns the exact live-neighbor IDs with gap flags calculated over live rows. Always clean temporary Auth/data fixtures, emit stable labels/codes only, and leave the local database migrated through 0062. Do not duplicate 0062 SQL inside the script or weaken an assertion when the migration fails.
  </action>
  <verify>
    <automated>node --experimental-strip-types --env-file=apps/web/.env.local scripts/verify-shared-content-migration.ts &amp;&amp; supabase db lint --local --level error &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `0061_shared_content_contract.sql` is byte-unchanged and 0062 is the sole new migration.
    - Version-2 proof requires a canonical URL/hostname pair and timestamp; null/legacy, partial, private literal, credentialed, fragmented, and custom-port identities cannot pass the helper.
    - Both normalized list/category projections and direct member policy use the helper, so an arbitrary service-role insert without proof remains invisible.
    - Every list branch applies the full cursor and a branch-local `p_limit + 1` before final union ordering, category probes short-circuit, and purpose-built partial indexes support live-message/eligible-attachment access without changing exact normalized rows.
    - The migration contains an idempotent tombstoned-source attachment backfill and a trigger that marks later attachments bound to an already tombstoned source before the existing claim path is used.
    - Target, both neighbor queries, and both gap probes explicitly exclude deleted messages.
    - The regression actually starts at database version 0061, applies 0062, and asserts exact unsafe-link absence, claimable legacy attachment identity, live-neighbor IDs, and gap booleans.
    - Database lint and `pnpm build` exit 0 with no package or UI change.
  </acceptance_criteria>
  <done>The corrective migration is source-complete and proven against data that existed before it was applied.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Push corrective schema to the linked database</name>
  <files>None — deployment-only task; the migration source is read and pushed without further edits.</files>
  <read_first>
    - `supabase/migrations/0062_shared_content_privacy_hardening.sql` — the only schema change permitted in this closure
    - `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-01-SUMMARY.md` — linked project identity and history through 0061
    - `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-VERIFICATION.md` — requirement for linked acceptance before behavior proof
    - `supabase/config.toml` — linked project configuration
  </read_first>
  <action>
Require `SUPABASE_ACCESS_TOKEN` in the execution environment and verify `supabase migration list` reports matching local/remote history through 0061 with 0062 pending locally. Run the actual non-interactive `supabase db push --linked --yes`; stop immediately on a prompt, partial application, unknown remote-only migration, or nonzero exit. Then rerun `supabase migration list` and require local/remote 0062 parity. Record only migration identifiers and safe exit status in the summary; never print the access token, database password, connection string, URLs, message IDs, or fixture content. This blocking task must finish before Plan 11 runs any linked-schema verifier.
  </action>
  <verify>
    <automated>test -n "$SUPABASE_ACCESS_TOKEN" &amp;&amp; supabase db push --linked --dry-run --yes &amp;&amp; supabase migration list</automated>
  </verify>
  <acceptance_criteria>
    - The actual `supabase db push --linked --yes` exits 0 after every schema file modification is complete.
    - The linked migration list shows 0062 on both local and remote sides and the dry run reports no pending migration.
    - No behavior/type-only check is accepted in place of linked database acceptance.
    - No secret or private fixture value appears in output or the plan summary.
  </acceptance_criteria>
  <done>The linked database has accepted 0062, unblocking generated-contract and adversarial verification.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate the accepted provider contract</name>
  <files>packages/supabase/src/database.generated.ts</files>
  <read_first>
    - `supabase/migrations/0062_shared_content_privacy_hardening.sql` — accepted field/function contract
    - `packages/supabase/src/database.generated.ts` — generated target; never hand-edit
    - `packages/supabase/src/database.types.ts` — intentional aliases that must continue indexing generated rows/functions
    - `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-05-SUMMARY.md` — byte-exact generator newline precedent
  </read_first>
  <action>
After the linked push, run a fresh `supabase db reset --local --yes` and `supabase db lint --local --level error`. Replace `packages/supabase/src/database.generated.ts` mechanically with the exact bytes from `supabase gen types typescript --local`, including its final newline. Confirm the generated `message_link_previews.Row/Insert/Update` shapes contain `safe_link_validation_version` and `safe_link_validated_at`, and existing shared-content RPC signatures/aliases still typecheck without handwritten duplication. Do not change `database.types.ts` unless generation exposes a real compile break; if it changes, preserve intentional indexed aliases and full public surfaces.
  </action>
  <verify>
    <automated>shared_content_types_tmp=$(mktemp) &amp;&amp; supabase gen types typescript --local &gt; "$shared_content_types_tmp" &amp;&amp; cmp -s "$shared_content_types_tmp" packages/supabase/src/database.generated.ts &amp;&amp; rm "$shared_content_types_tmp" &amp;&amp; supabase db lint --local --level error &amp;&amp; rg -q 'safe_link_validation_version' packages/supabase/src/database.generated.ts &amp;&amp; rg -q 'safe_link_validated_at' packages/supabase/src/database.generated.ts &amp;&amp; pnpm --filter @fish/supabase typecheck &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - A full local reset applies 0062 and database lint reports zero errors.
    - Generated types contain both new proof fields and every existing shared-content RPC.
    - Generated bytes match `supabase gen types typescript --local`; no manual row shape is introduced.
    - Existing `MessageLinkPreviewRow` and shared-content aliases continue indexing `Database` and the Supabase package typecheck passes.
    - `pnpm build` exits 0 before commit.
  </acceptance_criteria>
  <done>Local, linked, generated, and intentional provider contracts agree through migration 0062.</done>
</task>

</tasks>

## Artifacts this phase produces

- New file: `supabase/migrations/0062_shared_content_privacy_hardening.sql`.
- New file: `scripts/verify-shared-content-migration.ts`.
- New database fields: `public.message_link_previews.safe_link_validation_version`, `public.message_link_previews.safe_link_validated_at`.
- New database function: `private.is_canonical_safe_link_identity(text, text, smallint, timestamptz)`.
- New database function/trigger: `private.enforce_deleted_source_attachment_cleanup()` and `message_attachments_deleted_source_cleanup`.
- Modified database symbols: `public.list_conversation_shared_content`, `public.list_conversation_shared_content_categories`, `public.list_conversation_message_context`, and the `members read message link previews` policy.
- Modified generated file: `packages/supabase/src/database.generated.ts` with the two proof fields; no handwritten provider alias is added.
- CLI flags: none added by this plan; existing Supabase `--linked`, `--yes`, `--dry-run`, `--local`, and `--version` flags are used.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| service/legacy row -> member projection | Stored link metadata is untrusted unless current proof and canonical identity checks pass. |
| migration 0061 -> 0062 | Applied history cannot be rewritten; correction must be forward-only and accepted by the linked database. |
| deleted source -> cleanup claim | Legacy rows must receive durable work without exposing paths or losing retry state. |
| context UUIDs -> private messages | Unauthorized, deleted, or wrong-conversation targets/neighbors must not leak through rows or gap flags. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-11-06-01 | Information disclosure | link-preview RLS/list/category | mitigate | Current version/timestamp proof plus immutable canonical URL/hostname helper is required in all three read paths; unproven rows stay stored but invisible. |
| T-11-06-02 | Tampering | migration history | mitigate | Preserve 0061, create 0062, run pre/post regression, linked push, migration-list parity, fresh reset, and generated byte drift. |
| T-11-06-03 | Information disclosure | message context | mitigate | Nondeleted predicates on target, both neighbors, and both gap probes with exact deleted-neighbor regression. |
| T-11-06-04 | Tampering / denial of service | legacy deletion cleanup | mitigate | Idempotent backfill uses source tombstone time; existing service-only SKIP LOCKED claim/finish path remains authoritative and retryable. |
| T-11-06-05 | Information disclosure | migration verifier output | mitigate | Isolated fixtures, stable labels/codes, final cleanup, and no URLs/paths/tokens/bodies/real user IDs in output. |
| T-11-06-SC | Tampering | package-manager supply chain | mitigate | No package install or lockfile change; unexpected dependencies block for legitimacy review. |

## OWASP ASVS v5.0.0 Level 1 Mapping

Authoritative baseline: OWASP Application Security Verification Standard v5.0.0, Level 1 (`https://github.com/OWASP/ASVS/tree/v5.0.0`). Every HIGH row is blocking: it must reach `RESOLVED-BY-GATE` through the named task evidence before this plan completes or a dependent plan executes; no HIGH threat is accepted or transferred.

| Threat ID | Severity | ASVS v5.0.0 L1 control and title | Required resolution | Verification evidence |
|---|---|---|---|---|
| T-11-06-01 | HIGH | `v5.0.0-8.2.2` — General Authorization Design: data-specific access is restricted to consumers with explicit permissions; `v5.0.0-2.2.1` — Input Validation: security-decision input enforces defined expectations | RESOLVED-BY-GATE | Task 1 migration regression proves unproven/malformed rows are absent from direct member and both RPC projections; Task 2 linked push and Plan 11 linked cases repeat the boundary. |
| T-11-06-02 | HIGH | `v5.0.0-2.3.1` — Business Logic Security: flows follow the expected sequential step order without skipped steps | RESOLVED-BY-GATE | Task 1 starts at 0061 then applies 0062; Task 2 requires actual linked push and migration parity; Task 3 requires reset plus byte-exact generated types. |
| T-11-06-03 | HIGH | `v5.0.0-8.2.2` — General Authorization Design: data-specific access is restricted to consumers with explicit permissions | RESOLVED-BY-GATE | Task 1 exact deleted-neighbor regression checks target, both neighbor sets, both gap probes, and returned IDs. |
| T-11-06-04 | HIGH | `v5.0.0-2.3.1` — Business Logic Security: flows follow the expected sequential step order without skipped steps; `v5.0.0-8.2.2` — General Authorization Design: data-specific access is restricted to consumers with explicit permissions | RESOLVED-BY-GATE | Task 1 pre-0062 tombstone fixture proves backfill/trigger to the existing service-only retry-safe claim path; Plan 11 asserts zero residual unmarked deleted-source attachments. |
| T-11-06-05 | MEDIUM | `v5.0.0-15.3.1` — Defensive Coding: return only the required subset of fields | RESOLVED-BY-GATE | Task 1 verifier output and Tasks 2-3 summaries are restricted to stable case labels, migration IDs, safe counts, and exit status. |
| T-11-06-SC | LOW | `v5.0.0-15.2.1` — Security Architecture and Dependencies: components remain within documented update/remediation timeframes | RESOLVED-BY-GATE | Task 1-3 gates include build/type checks and require no package or lockfile change; any unexpected dependency blocks execution for legitimacy review. |
</threat_model>

<verification>
- `node --experimental-strip-types --env-file=apps/web/.env.local scripts/verify-shared-content-migration.ts`
- Actual `supabase db push --linked --yes`, followed by an empty dry run and matching migration list through 0062.
- Fresh local reset, database lint, byte-exact generated types, Supabase typecheck, and `pnpm build`.
</verification>

<success_criteria>
- Unsafe/unproven legacy or service rows are omitted at every member-visible database path.
- Pre-0062 tombstoned attachments become claimable after 0062.
- Deleted context neighbors cannot appear or influence continuity.
- Linked/local migration history and generated types agree through 0062.
</success_criteria>

<output>
Create `.planning/phases/11-shared-content-contract-and-privacy-boundary/11-06-SUMMARY.md` when done.
</output>
