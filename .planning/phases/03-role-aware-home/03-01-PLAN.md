---
phase: 03-role-aware-home
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0006_client_reads_coach_name.sql
  - supabase/migrations/0002_handle_new_user.sql
  - packages/supabase/src/database.generated.ts
  - packages/supabase/src/auth.ts
  - apps/web/app/page.tsx
  - apps/web/app/login/page.tsx
  - apps/web/app/(authenticated)/layout.tsx
  - apps/web/app/(authenticated)/layout.test.tsx
  - apps/web/components/shell/app-shell.tsx
  - apps/web/components/shell/app-shell.test.tsx
  - apps/web/components/home/empty-state.tsx
  - apps/web/components/home/empty-state.test.tsx
autonomous: false
requirements: [ROUT-01, ROUT-02, ROUT-03, SHEL-01, SHEL-02]
user_setup: []

must_haves:
  truths:
    - "A signed-out visitor to any protected route is redirected to /login"
    - "A signed-in person is landed inside the app shell (top bar + centered content column)"
    - "A signed-in client visiting / is redirected to /home; a coach to /coach; a signed-out visitor to /login"
    - "The public profiles table exposes an email column populated for every account including the three seeded ones"
    - "A client can read their own assigned coach's profile row via RLS (is_client_of)"
    - "The shell shows at most one primary action (the ghost/primary logout button) and never a page title"
  artifacts:
    - path: "supabase/migrations/0006_client_reads_coach_name.sql"
      provides: "private.is_client_of() SECURITY DEFINER helper + 'client reads own coach' SELECT policy on profiles"
      contains: "create or replace function private.is_client_of"
    - path: "apps/web/app/(authenticated)/layout.tsx"
      provides: "Shared auth+role guard: getUser() → redirect /login if absent, resolve profiles.role, wrap children in AppShell"
      min_lines: 25
    - path: "apps/web/components/shell/app-shell.tsx"
      provides: "Slim top bar (logo + muted display name + logout) over a max-w-[640px] content column"
      exports: ["AppShell"]
    - path: "apps/web/components/home/empty-state.tsx"
      provides: "Shared calm empty-state shape (one Tabler icon + calm copy, zero actions) inside a Card"
      exports: ["EmptyState"]
    - path: "apps/web/app/page.tsx"
      provides: "Pure redirect root: signed-out → /login, client → /home, coach → /coach"
      contains: "redirect"
    - path: "packages/supabase/src/auth.ts"
      provides: "authRedirects with signedOut=/login, clientHome=/home, coachHome=/coach"
      contains: "clientHome: \"/home\""
  key_links:
    - from: "apps/web/app/(authenticated)/layout.tsx"
      to: "profiles table"
      via: "getUser() then .from('profiles').select('role, display_name').eq('id', user.id).single()"
      pattern: "from\\(\"profiles\"\\)"
    - from: "apps/web/app/(authenticated)/layout.tsx"
      to: "components/shell/app-shell.tsx"
      via: "renders <AppShell displayName={...}>{children}</AppShell>"
      pattern: "AppShell"
    - from: "apps/web/app/login/page.tsx"
      to: "packages/supabase/src/auth.ts"
      via: "router.push(authRedirects.clientHome) instead of literal '/home'"
      pattern: "authRedirects"
    - from: "supabase/migrations/0006_client_reads_coach_name.sql"
      to: "coach_clients table"
      via: "is_client_of() checks a coach_clients row for auth.uid()"
      pattern: "from public.coach_clients"
---

<objective>
Build the routing + shell spine for the role-aware home, and land the schema this phase needs. After this plan, a signed-out visitor to any protected URL is bounced to /login, a signed-in person is landed inside the calm app shell, the root `/` becomes a role-aware pure redirect, and the database exposes both the client→coach-name read (D-16) and a client email on `profiles` (D-13) that the coach home will consume next.

Purpose: This is the vertical spine every later slice stands on — routing, the shell chrome, the shared empty-state primitive, and the two schema additions. Both the client-home slice (Plan 02) and the coach-home slice (Plan 03) depend on it and run in parallel after it.
Output: New `0006` migration (is_client_of helper + policy, email column, handle_new_user extension, backfill), regenerated DB types, rewired authRedirects, root pure-redirect page, the `(authenticated)` route group with its guard layout, the AppShell component, and the shared EmptyState component — all with tests.
</objective>

<execution_context>
@/Users/franz/.claude/gsd-core/workflows/execute-plan.md
@/Users/franz/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-role-aware-home/03-CONTEXT.md
@.planning/phases/03-role-aware-home/03-RESEARCH.md
@.planning/phases/03-role-aware-home/03-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. Use directly — no exploration. -->

Existing server auth read (apps/web/lib/supabase/server.ts) — never getSession():
```
export async function createClient(): Promise<SupabaseClient>  // reads NEXT_PUBLIC_SUPABASE_URL + NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
```

Existing role vocabulary (packages/core/src/roles.ts):
```
export const userRoles = ["client", "coach"] as const;
export type UserRole = (typeof userRoles)[number];
export function isUserRole(value: string): value is UserRole;
```

Existing authRedirects BEFORE this plan (packages/supabase/src/auth.ts) — edit in place:
```
export const authRedirects = {
  signedOut: "/",          // → change to "/login"
  home: "/home",           // → drop this stale interim key
  clientHome: "/chat",     // → change to "/home"
  coachHome: "/coach",     // → unchanged
} as const;
```

Existing LogoutButton (apps/web/components/auth/logout-button.tsx) — unchanged, relocates into AppShell:
```
export function LogoutButton(): JSX.Element   // "use client"; renders <Button variant="primary">Log out</Button>; the shell's ONE primary action
```

Existing Card (apps/web/components/ui/card.tsx): `export function Card` — accepts className, wraps children in the surface/border/rounded-card container.

Existing profiles handle_new_user trigger (supabase/migrations/0002_handle_new_user.sql) — insert currently writes (id, role, display_name); this plan adds email.

Existing is_coach_of shape (supabase/migrations/0004_rls_helpers.sql) — the exact analog to mirror for is_client_of:
```
create or replace function private.is_coach_of(client_uuid uuid)
returns boolean language sql security definer stable set search_path = '' as $$ ... $$;
```
</interfaces>
</context>

## Phase Goal

**As a** logged-in person, **I want to** be landed inside a calm app shell on the home that matches my role — with every wrong door silently opening my own right room — **so that** I never see another person's data, an error screen, or anything competing for my attention.

## Artifacts this phase produces

New symbols/paths created across the whole phase (planner note for source-grounding — exclude these from drift verification):

- SQL: `private.is_client_of(coach_uuid uuid)` function; `"client reads own coach"` RLS policy on `public.profiles`; new `email` column on `public.profiles`.
- Component exports: `AppShell` (apps/web/components/shell/app-shell.tsx), `EmptyState` (apps/web/components/home/empty-state.tsx), `ClientList` (apps/web/components/coach/client-list.tsx — Plan 03).
- New route files: `apps/web/app/(authenticated)/layout.tsx`, `apps/web/app/(authenticated)/home/page.tsx` (Plan 02), `apps/web/app/(authenticated)/coach/page.tsx` (Plan 03).
- Rewritten `apps/web/app/page.tsx` (pure redirect).
- `authRedirects` key value changes (no new keys; `home` key removed).
- New TS symbol on generated types: `profiles.Row.email: string`.
- Test files: `(authenticated)/layout.test.tsx`, `components/shell/app-shell.test.tsx`, `components/home/empty-state.test.tsx`, `components/coach/client-list.test.tsx` (Plan 03).
- `scripts/verify-rls.ts` gains `checkClientReadsCoachName()` (Plan 03).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0006 — is_client_of helper + policy, email column + trigger extension + backfill; regenerate types</name>
  <files>supabase/migrations/0006_client_reads_coach_name.sql, supabase/migrations/0002_handle_new_user.sql, packages/supabase/src/database.generated.ts</files>
  <read_first>
    - supabase/migrations/0004_rls_helpers.sql (the is_coach_of() shape to mirror — SECURITY DEFINER, stable, set search_path='', never bare-SELECT the protected table)
    - supabase/migrations/0002_handle_new_user.sql (the trigger being extended to copy new.email)
    - supabase/migrations/0001_profiles.sql (current profiles columns: id, role, display_name, created_at, updated_at — no email; the grant on line 19 already covers authenticated SELECT)
    - supabase/migrations/0003_coach_clients.sql (the enforce_coach_client_roles trigger that guarantees client_id references a client-role profile — the reason is_client_of omits the caller-role re-check)
    - packages/supabase/src/database.generated.ts (the profiles Row/Insert/Update blocks that must gain an `email` field after regeneration)
  </read_first>
  <behavior>
    - Test (SQL applies clean): after schema apply, `private.is_client_of(<seeded coach id>)` returns true when called as a seeded client assigned to that coach, false for a non-coach uuid.
    - Test (RLS reverse read): signed in as client1@fish.dev, `select display_name from profiles where id = <coach id>` returns exactly one row (the coach's) with no 42P17 recursion and no permission-denied.
    - Test (email present + backfilled): after apply + reseed, `select email from profiles where id = <client1 id>` returns 'client1@fish.dev' (not null).
    - Test (types): packages/supabase/src/database.generated.ts profiles Row contains `email: string`.
  </behavior>
  <action>
    Create `supabase/migrations/0006_client_reads_coach_name.sql` doing THREE things in one migration:
    (1) Add the `email` column to `public.profiles`: `alter table public.profiles add column email text not null default ''`. The existing `grant select, update on public.profiles to authenticated` (0001 line 19) already covers it — add no new grant.
    (2) Extend the profile-creation path so email is populated going forward AND backfilled for existing rows. In `supabase/migrations/0002_handle_new_user.sql`, change the INSERT column list to `(id, role, display_name, email)` and add the value `new.email` (auth.users.email is available on the trigger's NEW row). Keep all four hardening elements intact (security definer, set search_path='', coalesce for display_name, on conflict do nothing). Because 0002 already ran in the live DB, ALSO put a `create or replace function public.handle_new_user()` block in 0006 with the same extended body so the live function is updated by applying 0006 (do not rely only on editing 0002, which only takes effect on a fresh `db reset`). Then backfill existing rows in 0006: `update public.profiles p set email = u.email from auth.users u where u.id = p.id and p.email = ''`.
    (3) Create the reverse-read helper + policy, mirroring `private.is_coach_of()` EXACTLY but in reverse and WITHOUT the extra caller-role check: `create or replace function private.is_client_of(coach_uuid uuid) returns boolean language sql security definer stable set search_path = ''` whose body is `select exists (select 1 from public.coach_clients cc where cc.coach_id = coach_uuid and cc.client_id = (select auth.uid()))`. Then `create policy "client reads own coach" on public.profiles for select to authenticated using (private.is_client_of(id))`. Omit the `and (select role ...) = 'client'` clause that is_coach_of carries — the enforce_coach_client_roles trigger (0003) already guarantees coach_clients.client_id references a client-role profile, so the caller being the referenced client_id is sufficient (RESEARCH.md Assumption A3 flags this as a review checkpoint, not a bug — do not add the redundant clause).
    Do NOT regenerate types yet in this task's edit — regeneration runs against the live DB in Task 3 after the schema is applied, and lands the `email: string` field into database.generated.ts's profiles Row/Insert/Update blocks. If you must scaffold the type change before apply to keep the build green, hand-add `email: string` (Row) / `email?: string` (Insert/Update) to the profiles blocks, and Task 3 confirms `supabase gen types` produces the identical shape.
  </action>
  <verify>
    <automated>grep -q "create or replace function private.is_client_of" supabase/migrations/0006_client_reads_coach_name.sql &amp;&amp; grep -q "add column email" supabase/migrations/0006_client_reads_coach_name.sql &amp;&amp; grep -q "new.email" supabase/migrations/0002_handle_new_user.sql</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/0006_client_reads_coach_name.sql contains `create or replace function private.is_client_of(coach_uuid uuid)` with `security definer`, `stable`, and `set search_path = ''` all present
    - 0006 contains `create policy "client reads own coach"` on `public.profiles for select to authenticated using (private.is_client_of(id))`
    - 0006 contains `alter table public.profiles add column email text` and an `update public.profiles ... from auth.users` backfill
    - 0006 contains a `create or replace function public.handle_new_user()` block whose INSERT column list includes `email` and whose values include `new.email`
    - supabase/migrations/0002_handle_new_user.sql INSERT column list includes `email` and values include `new.email`
    - is_client_of body queries `public.coach_clients` only and never bare-selects `public.profiles` (no `from public.profiles` inside the is_client_of function body)
    - packages/supabase/src/database.generated.ts profiles Row contains `email: string`
  </acceptance_criteria>
  <done>The migration adds the reverse-read RLS helper+policy, adds and backfills the email column, updates the live handle_new_user function, and the generated profiles type carries an email field.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 [BLOCKING]: Apply schema, reseed, and prove RLS live</name>
  <what-built>
    Migration 0006 is written. It must now be applied to the live local Supabase DB, and the seed re-run so the three seeded accounts get their email backfilled. Phase 2's proven proof gate for migrations is `supabase db reset` (reapplies 0001→0006 from scratch); because `db reset` wipes seeded auth users, the seed must be re-run immediately after. Build/typecheck alone give a FALSE PASS here — types come from the generated file, not the live DB, so the schema MUST be pushed and reseeded before any verification is trusted.
  </what-built>
  <how-to-verify>
    Run these in order from the repo root and confirm each succeeds:
    1. `supabase status` — confirm the local stack is running (API_URL responds). If stopped, run `supabase start` first.
    2. `supabase db reset` — reapplies migrations 0001→0006 in order with zero errors (no 42P01 forward-reference, no 42P17 recursion). This is Phase 2's documented migration proof gate.
    3. `pnpm seed` — recreates coach@fish.dev + the three clients; the extended handle_new_user now writes email on creation, and the 0006 backfill covers any pre-existing rows.
    4. `supabase gen types typescript --local` piped to `packages/supabase/src/database.generated.ts` (the exact command Phase 2 used — overwrites ONLY database.generated.ts; never touch database.types.ts). Confirm the regenerated profiles Row now contains `email: string`.
    5. `pnpm verify:rls` — the existing three assertions still pass (client boundary, coach boundary sees 4 rows, escalation rejected) with the new email column present.
    Report: paste the tail of `supabase db reset`, the seed output, and the `pnpm verify:rls` "All assertions passed." line.
  </how-to-verify>
  <resume-signal>Type "approved" once db reset + seed + gen types + verify:rls all succeed, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: authRedirects rewire + root pure-redirect page + wire login to the constant</name>
  <files>packages/supabase/src/auth.ts, apps/web/app/page.tsx, apps/web/app/login/page.tsx</files>
  <read_first>
    - packages/supabase/src/auth.ts (the 14-line file being edited in place — current keys: signedOut:"/", home:"/home", clientHome:"/chat", coachHome:"/coach")
    - apps/web/app/page.tsx (the stale monochrome showcase being deleted wholesale — swatches/Buttons/Inputs gallery; note it references removed accent-pink/accent-yellow utilities, which this deletion clears per D-02)
    - apps/web/app/login/page.tsx (line 50 `router.push("/home")` — the literal to replace with authRedirects.clientHome; note it is a "use client" component so it imports the constant, not a server redirect)
    - apps/web/app/home/page.tsx (the getUser()-then-redirect server-component shape to copy for the root page)
    - .planning/phases/03-role-aware-home/03-RESEARCH.md (Code Examples → "The `/` pure-redirect page (D-02)" — the ready-to-copy shape)
  </read_first>
  <action>
    Edit `packages/supabase/src/auth.ts` per D-01/D-07: set `signedOut: "/login"`, set `clientHome: "/home"`, keep `coachHome: "/coach"`, and remove the stale `home: "/home"` key and its "interim landing" comment (grep confirmed `authRedirects` has no other importers, so removing the key breaks nothing — but Task-3 also ADDS login as an importer, so verify the build after). Keep the `as const` and the `FishAuthClaims` interface untouched.
    Rewrite `apps/web/app/page.tsx` as a pure-redirect Server Component (D-02): delete the entire showcase body. New body: `createClient()` → `getUser()`; if no user, `redirect(authRedirects.signedOut)`; else read `profiles.role` via `.from("profiles").select("role").eq("id", user.id).single()` and `redirect(profile?.role === "coach" ? authRedirects.coachHome : authRedirects.clientHome)`. Import `authRedirects` from `@fish/supabase/auth`, `createClient` from `@/lib/supabase/server`, `redirect` from `next/navigation`. This file renders NOTHING — every branch redirects.
    In `apps/web/app/login/page.tsx`, replace the literal `router.push("/home")` on line 50 with `router.push(authRedirects.clientHome)` and add the import `import { authRedirects } from "@fish/supabase/auth"`. Leave the check-inbox push and all other logic unchanged. (After login the destination is always the client home — D-04, no ?next; a coach who logs in via this form lands on /home and the layout guard silently forwards them to /coach.)
  </action>
  <verify>
    <automated>grep -q 'clientHome: "/home"' packages/supabase/src/auth.ts &amp;&amp; grep -q 'signedOut: "/login"' packages/supabase/src/auth.ts &amp;&amp; grep -q "authRedirects.clientHome" apps/web/app/login/page.tsx &amp;&amp; ! grep -q "accent-pink\|accent-yellow\|swatches" apps/web/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - packages/supabase/src/auth.ts contains `signedOut: "/login"` and `clientHome: "/home"` and `coachHome: "/coach"`, and no longer contains the `home:` key
    - apps/web/app/page.tsx contains `redirect(` in every branch, imports `authRedirects`, and contains NO JSX/`return (` rendering markup (pure redirect)
    - apps/web/app/page.tsx no longer contains `accent-pink`, `accent-yellow`, or `swatches`
    - apps/web/app/login/page.tsx contains `router.push(authRedirects.clientHome)` and `import { authRedirects }` and no longer contains the literal `router.push("/home")`
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>authRedirects is the single source of truth for redirect destinations, the root path is a role-aware pure redirect with the stale showcase gone, and login routes through the constant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: AppShell + EmptyState components, and the (authenticated) guard layout (ROUT-01/02/03, SHEL-01/02)</name>
  <files>apps/web/components/shell/app-shell.tsx, apps/web/components/shell/app-shell.test.tsx, apps/web/components/home/empty-state.tsx, apps/web/components/home/empty-state.test.tsx, apps/web/app/(authenticated)/layout.tsx, apps/web/app/(authenticated)/layout.test.tsx</files>
  <read_first>
    - apps/web/app/home/page.tsx (the getUser()→redirect('/login') pattern the layout copies verbatim; the Card+LogoutButton composition the shell generalizes)
    - apps/web/app/home/page.test.tsx (the mock scaffold to copy: redirect mock re-throwing NEXT_REDIRECT, getUser mock, the variant="primary" grep-gate at lines 60-71)
    - apps/web/components/auth/logout-button.tsx (variant="primary"; the shell's single primary action — SHEL-01 grep gate must count it)
    - apps/web/components/ui/card.tsx (Card container for EmptyState)
    - apps/web/components/ui/alert.tsx (icon-rendering convention: size, stroke={1.5}, aria-hidden="true")
    - .planning/phases/03-role-aware-home/03-PATTERNS.md (AppShell, EmptyState, and layout pattern blocks — copy the exact composition shown)
    - apps/web/tests/icon-source.test.ts (the hard gate: only @tabler/icons-react may be imported — EmptyState's icon prop type comes from there)
  </read_first>
  <behavior>
    - Test (layout redirects signed-out — ROUT-01): getUser() returns no user → `redirect("/login")` is called (mock re-throws NEXT_REDIRECT).
    - Test (layout resolves role + renders shell — ROUT-02/03): getUser() returns a user and the mocked profiles query returns `{role:'client', display_name:'Alex Rivera'}` → the layout renders AppShell with the display name and does not redirect.
    - Test (layout defensive redirect): getUser() returns a user but the profiles query returns null → `redirect("/login")`.
    - Test (SHEL-01 one primary): a source grep across app-shell.tsx + logout-button.tsx finds exactly one `variant="primary"` occurrence.
    - Test (EmptyState — SHEL-02): renders the passed icon (aria-hidden) and the calm copy children; contains zero `<button>`/primary action.
  </behavior>
  <action>
    Create `apps/web/components/home/empty-state.tsx` exporting `EmptyState({ Icon, children }: { Icon: Icon; children: React.ReactNode })` (import `type { Icon } from "@tabler/icons-react"`, `Card` from `@/components/ui/card`). Render a `Card` (className `flex flex-col items-center gap-3 py-10 text-center`) containing `<Icon size={32} stroke={1.5} aria-hidden="true" className="text-muted" />` then `<div className="text-body">{children}</div>`. Zero actions (D-18). Named export, no forwardRef (not focusable).
    Create `apps/web/components/shell/app-shell.tsx` exporting `AppShell({ displayName, children }: { displayName: string; children: React.ReactNode })`. Structure (D-08..D-11): an outer `flex min-h-dvh flex-col`; a `<header>` (`flex items-center justify-between border-b border-border px-5 py-4`) holding the FISH logo on the left (reuse the existing `<Image src="/logo.svg" alt="FISH" width={.. } height={..} />` pattern from the deleted page.tsx / kit page — small, e.g. 32-40px), a muted display name span in the middle-or-right (`text-[14px] text-muted`), and the existing `<LogoutButton />` on the right; then `<main className="mx-auto w-full max-w-[640px] flex-1 px-5 py-12">{children}</main>`. The bar carries NO page title (D-11) — each page owns its Fraunces heading in the content column. `LogoutButton` (variant="primary") is the shell's ONLY primary action (SHEL-01).
    Create `apps/web/app/(authenticated)/layout.tsx` as an async Server Component (RESEARCH.md Pattern 1 / PATTERNS.md layout block): `createClient()` → `getUser()`; if `!user` → `redirect("/login")` (ROUT-01, never getSession()). Then `.from("profiles").select("role, display_name").eq("id", user.id).single()`; if `!profile` → `redirect("/login")` (defensive). Return `<AppShell displayName={profile.display_name}>{children}</AppShell>`. Do NOT try/catch getUser or the query — redirect() throws by design; let genuine network failures surface to the Next.js error boundary. The per-page "wrong role for THIS page" guard (D-03) is NOT in the layout — it lives in each leaf page (Plans 02/03), because a layout does not cleanly know which leaf route it wraps.
    Write the three test files. `app-shell.test.tsx`: render AppShell with a displayName, assert the name and logout button render; plus the SHEL-01 grep gate reading app-shell.tsx + ../auth/logout-button.tsx sources and asserting exactly one `variant="primary"`. `empty-state.test.tsx`: render with a Tabler icon + copy, assert the copy text is present and no button role exists. `layout.test.tsx`: copy the mock scaffold from home/page.test.tsx (redirect mock re-throwing NEXT_REDIRECT + getUser mock), extend the createClient mock so `.from().select().eq().single()` returns a configurable profile; assert the three behaviors (no-user→redirect /login; null-profile→redirect /login; valid client profile→renders without redirect).
  </action>
  <verify>
    <automated>pnpm --filter @fish/web test -- --run app/\\(authenticated\\)/layout.test.tsx components/shell/app-shell.test.tsx components/home/empty-state.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components/home/empty-state.tsx exports `EmptyState`, imports `type { Icon } from "@tabler/icons-react"`, wraps children in `Card`, and contains no `<button` or `variant="primary"`
    - apps/web/components/shell/app-shell.tsx exports `AppShell`, imports `LogoutButton`, contains `max-w-[640px]`, and contains no page-title/heading text prop
    - apps/web/app/(authenticated)/layout.tsx is `async function`, calls `getUser()` (not `getSession()`), calls `redirect("/login")` on absent user AND absent profile, queries `.from("profiles").select("role, display_name")`, and returns `<AppShell`
    - `app-shell.test.tsx` grep gate asserts exactly one `variant="primary"` across app-shell.tsx + logout-button.tsx
    - `layout.test.tsx` asserts redirect('/login') for both no-user and null-profile cases and no redirect for a valid client profile
    - `pnpm --filter @fish/web test -- --run app/\\(authenticated\\)/layout.test.tsx components/shell/app-shell.test.tsx components/home/empty-state.test.tsx` exits 0
    - `pnpm --filter @fish/web test` full suite stays green (152+ passing) and `pnpm --filter @fish/web test -- --run tests/icon-source.test.ts` still passes (no non-Tabler icon import introduced)
  </acceptance_criteria>
  <done>The guard layout redirects signed-out visitors, resolves role, and wraps children in the shell; the shell shows one primary action and no page title; EmptyState is a reusable calm no-action primitive — all tested and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Component (layout/root page) | An untrusted, possibly forged, session cookie crosses here; role must be resolved server-side, never from a client-supplied value |
| authenticated role → public.profiles / coach_clients (SQL) | RLS is the actual authorization boundary; the new is_client_of policy widens a client's read surface to one specific coach row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Spoofing | (authenticated)/layout.tsx role read | mitigate | Resolve role via `getUser()` (network-verified) + `profiles.role` DB read; never `getSession()`, never a client-supplied role. Enforced by the layout test and the getSession grep discipline carried from Phase 2. |
| T-3-02 | Elevation of Privilege | is_client_of() RLS policy | mitigate | Helper is SECURITY DEFINER, queries only coach_clients (never bare-selects profiles from inside a profiles policy → no 42P17), and scopes to `(select auth.uid())`. The enforce_coach_client_roles trigger already guarantees row-shape integrity, so no redundant caller-role clause is needed. Proven live by pnpm verify:rls (Plan 03 adds the assertion). |
| T-3-03 | Information Disclosure | email column on profiles | accept | Email is added to `public.profiles` under the SAME existing RLS policies (self-read + is_coach_of + is_client_of). No new grant widens exposure beyond a coach reading assigned clients and a client reading their own coach — both intended (D-13/D-16). |
| T-3-SC | Tampering | npm/pnpm installs | mitigate | No new packages this phase (RESEARCH.md: zero new dependencies). No install task exists; nothing to gate. |
</threat_model>

<verification>
- `supabase db reset` applies 0001→0006 with no 42P01/42P17; `pnpm seed` succeeds; `pnpm verify:rls` exits 0 (Task 2 gate).
- `supabase gen types typescript --local` produces a profiles Row containing `email: string`.
- `pnpm build` exits 0 (route group compiles; root page is a valid Server Component).
- `pnpm --filter @fish/web test` full suite green; icon-source guard still passes.
</verification>

<success_criteria>
- A signed-out visitor to a protected route is redirected to /login (ROUT-01, layout test).
- A signed-in person is landed inside the AppShell with their display name and one primary action (SHEL-01).
- `/` redirects by role/session with no rendered content (D-02).
- `profiles.email` is populated for all accounts; a client can read their own coach's row via is_client_of (D-13/D-16 foundation).
- authRedirects is the single source of truth (clientHome=/home, signedOut=/login), consumed by login.
</success_criteria>

<output>
Create `.planning/phases/03-role-aware-home/03-01-SUMMARY.md` when done.
</output>
