---
phase: "09-cross-platform-chat-state"
plan: "11"
type: execute
wave: 7
depends_on:
  - "09-10"
files_modified:
  - "apps/web/app/(authenticated)/chat/message-grouping.ts"
  - "apps/web/app/(authenticated)/chat/message-grouping.test.ts"
  - "apps/web/app/(authenticated)/chat/chat-client.tsx"
  - "apps/web/app/(authenticated)/chat/chat-client.test.tsx"
  - ".claude/skills/sketch-findings-fish/references/states.md"
autonomous: true
gap_closure: true
requirements:
  - CSTATE-06
must_haves:
  truths:
    - "In the community room a same-sender run shows the author avatar and MessageMeta (name + time) again after a documented short time gap and after a day divider — identity and time are no longer suppressed indefinitely (closes WR-02, matches the user's missing avatar/time UAT report)."
    - "The offline banner uses calm, truthful copy that does not promise automatic or background send (no offline queue exists); a failed send remains a visible manual Retry."
    - "Message action controls (reply, react, edit, delete) meet the non-negotiable 56px interaction floor and are reachable on touch/coarse pointers without depending on hover."
  artifacts:
    - path: "apps/web/app/(authenticated)/chat/message-grouping.ts"
      provides: "Pure same-group predicate (same sender + same calendar day + short time gap)"
      exports: ["belongsToSameMessageGroup", "MESSAGE_GROUP_GAP_MS"]
    - path: "apps/web/app/(authenticated)/chat/message-grouping.test.ts"
      provides: "Unit tests for within-cutoff, outside-cutoff, across-day, different-sender"
    - path: "apps/web/app/(authenticated)/chat/chat-client.tsx"
      provides: "Grouping predicate wiring, truthful offline copy, 56px touch-safe actions"
    - path: ".claude/skills/sketch-findings-fish/references/states.md"
      provides: "Corrected offline copy that no longer promises automatic queued send"
  key_links:
    - from: "apps/web/app/(authenticated)/chat/chat-client.tsx"
      to: "apps/web/app/(authenticated)/chat/message-grouping.ts"
      via: "grouped-with-previous/next uses the shared predicate"
      pattern: "belongsToSameMessageGroup"
    - from: "apps/web/app/(authenticated)/chat/chat-client.tsx message actions"
      to: "design token size floor"
      via: "min-h-control / min-w-control on each action control"
      pattern: "min-h-control min-w-control"
---

<objective>
Close the three community-presentation warnings. WR-02: grouping compares only `senderId`, so a same-sender run suppresses avatar and `MessageMeta` indefinitely (this is the user's missing avatar/time report). WR-03: the offline banner promises "Messages will send when you're back" but no queue exists. WR-04: message actions are 40px (`size-10`), hover-only — below the non-negotiable 56px floor and poor for touch.

Purpose: CSTATE-06 (preserve calm, correct chat presentation) and the project's non-negotiable design floor (56px targets, calm copy that never overpromises).
Output: a pure grouping predicate + tests, chat-client wiring for grouping/copy/actions, and a corrected design-skill offline line.
</objective>

<execution_context>
@/Users/franz/.codex/gsd-core/workflows/execute-plan.md
@/Users/franz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/ui-ux-agent-guidelines.md
@.claude/skills/sketch-findings-fish/SKILL.md
@.claude/skills/sketch-findings-fish/references/chat.md
@.claude/skills/sketch-findings-fish/references/states.md
@.planning/phases/09-cross-platform-chat-state/09-CONTEXT.md
@.planning/phases/09-cross-platform-chat-state/09-REVIEW.md
@.planning/phases/09-cross-platform-chat-state/09-VERIFICATION.md
@apps/web/app/(authenticated)/chat/chat-client.tsx
@apps/web/app/(authenticated)/chat/chat-client.test.tsx
@apps/web/app/globals.css

<interfaces>
<!-- Extracted from the codebase. Use directly. -->

chat-client.tsx grouping (lines ~328-331): `groupedWithPrevious = Boolean(previous && previous.senderId === message.senderId)`; `groupedWithNext = Boolean(next && next.senderId === message.senderId)`. Used by `startsCommunityGroup = isCommunity && (!groupedWithPrevious || Boolean(replyMessage))`, which gates `showParticipantAvatar` and `showAuthorMeta` (MessageMeta) at line ~357-362. `previous`/`next` are `filteredMessages[index-1/+1]` (may be undefined). Each message has `senderId` and `createdAt` (ISO string). A `dayDividerLabel` is separately computed at ~363-375.

chat-client.tsx offline banner (line ~606-610): `{isOffline && (<Alert tone="notice" className="mx-md mb-xs">You're offline. Messages will send when you're back.</Alert>)}`. `isOffline = realtimeStatus === "disconnected"`. Composer is de-emphasized with `opacity-60` when offline; a failed send already shows a "Not sent yet" bubble with a real Retry button (min-h-control).

chat-client.tsx message actions (lines ~496-534): a `pointer-events-none absolute ... opacity-0 transition-opacity focus-within:pointer-events-auto focus-within:opacity-100 group-hover:pointer-events-auto group-hover:opacity-100` bar containing four controls (Reply button, EmojiPickerButton, Edit button, Delete button) each styled `inline-flex size-10 items-center justify-center rounded-control ...`. Cancel-reply/cancel-edit buttons elsewhere already use `min-h-control min-w-control`, and Retry uses `min-h-control` — reuse that token pattern.

Design token: `--size-control: 56px` (globals.css line ~101); Tailwind utilities `min-h-control`/`min-w-control` map to it. Tailwind v4 ships the `pointer-coarse:` variant for touch presentation.

states.md (line ~27): "Offline — a calm --notice-tone banner: 'You're offline. Messages will send when you're back.'" — this validated copy assumed an offline queue that was never built.
</interfaces>
</context>

## Artifacts This Phase Produces

New symbols/files created by this plan (exclude from source-drift verification):
- `apps/web/app/(authenticated)/chat/message-grouping.ts` exporting `belongsToSameMessageGroup(previous, current)` and `MESSAGE_GROUP_GAP_MS`.
- `apps/web/app/(authenticated)/chat/message-grouping.test.ts` (new unit test).
- New component test cases in `chat-client.test.tsx` (grouping reappears, offline copy, 56px actions).
- Corrected offline copy line in `.claude/skills/sketch-findings-fish/references/states.md`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the pure message-grouping predicate and unit tests</name>
  <files>apps/web/app/(authenticated)/chat/message-grouping.ts, apps/web/app/(authenticated)/chat/message-grouping.test.ts</files>
  <read_first>
    @apps/web/app/(authenticated)/chat/chat-client.tsx
    @.planning/phases/09-cross-platform-chat-state/09-REVIEW.md
  </read_first>
  <behavior>
    - Two same-sender messages 2 minutes apart on the same calendar day belong to the same group (true).
    - Two same-sender messages 10 minutes apart (outside the gap) on the same day do NOT group (false).
    - Two same-sender messages a few minutes apart but across a calendar-day boundary (e.g., 23:58 -> 00:01 next day) do NOT group (false).
    - Two different-sender adjacent messages do NOT group (false).
    - An undefined `previous` (first row) returns false.
  </behavior>
  <action>Create `message-grouping.ts` exporting `MESSAGE_GROUP_GAP_MS` set to `5 * 60 * 1000` (a documented short 5-minute gap) and a pure function `belongsToSameMessageGroup(previous, current)` that accepts the minimal message shape `{ senderId: string; createdAt: string }` (previous may be `undefined`/`null`). Return true only when: previous exists AND `previous.senderId === current.senderId` AND both timestamps fall on the same calendar day (compare `new Date(createdAt).toDateString()` for each) AND the elapsed time `Date.parse(current.createdAt) - Date.parse(previous.createdAt)` is `>= 0` and `<= MESSAGE_GROUP_GAP_MS`. Add a comment explaining WHY (same sender + same day + short gap keeps identity/time visible on long runs; matches WR-02). Create `message-grouping.test.ts` covering the five behavior cases above.</action>
  <acceptance_criteria>
    - Source assertion: `message-grouping.ts` exports `belongsToSameMessageGroup` and `MESSAGE_GROUP_GAP_MS === 300000`.
    - Behavior assertion: the five test cases (within cutoff true; outside cutoff false; across day false; different sender false; undefined previous false) all pass.
    - Test command: `pnpm --filter @fish/web test "app/(authenticated)/chat/message-grouping.test.ts"` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @fish/web test "app/(authenticated)/chat/message-grouping.test.ts"</automated>
  </verify>
  <done>A pure, unit-tested grouping predicate exists that requires same sender, same day, and a short time gap.</done>
</task>

<task type="auto">
  <name>Task 2: Wire grouping, fix offline copy, and restore 56px touch-safe actions</name>
  <files>apps/web/app/(authenticated)/chat/chat-client.tsx, .claude/skills/sketch-findings-fish/references/states.md</files>
  <read_first>
    @apps/web/app/(authenticated)/chat/chat-client.tsx
    @apps/web/app/(authenticated)/chat/message-grouping.ts
    @.claude/skills/sketch-findings-fish/references/states.md
    @.claude/skills/sketch-findings-fish/references/chat.md
    @docs/ui-ux-agent-guidelines.md
    @apps/web/app/globals.css
  </read_first>
  <action>Load the sketch-findings-fish design direction before editing. Apply three fixes to `chat-client.tsx`:

  1. WR-02 grouping: import `belongsToSameMessageGroup` from `./message-grouping` and replace the senderId-only `groupedWithPrevious`/`groupedWithNext` with `belongsToSameMessageGroup(previous, message)` and `belongsToSameMessageGroup(message, next)`. This restores `showParticipantAvatar` and `showAuthorMeta` (MessageMeta) at the start of a new group after a >5-minute gap or a day boundary. Do not change the reply-preview or day-divider logic.

  2. WR-03 offline copy: replace the offline `Alert` body "You're offline. Messages will send when you're back." with calm, truthful copy that does NOT promise automatic or background send — use "You're offline. Reconnect, then try again." Keep `tone="notice"`, the composer `opacity-60` de-emphasis, and the existing failed-send Retry path (which is the real manual retry). Then update `.claude/skills/sketch-findings-fish/references/states.md` line ~27 offline copy to the same truthful wording and add a short note that no offline queue exists, so the design skill stops re-seeding the overpromise.

  3. WR-04 56px touch-safe actions: change each of the four message-action controls (Reply, EmojiPickerButton, Edit, Delete) from `size-10` to `min-h-control min-w-control` (keep `inline-flex items-center justify-center rounded-control` and the existing color/hover classes, aria-labels, icons, and focus behavior). On the action-bar container, add `pointer-coarse:pointer-events-auto pointer-coarse:opacity-100` alongside the existing hover/focus-within reveal so touch/coarse-pointer users get the actions without hover. Design-tension note: the sketch reference favored a compact hover-only bar, but AGENTS.md's 56px floor is non-negotiable and WR-04 flagged the 40px hover-only as a violation — 56px + touch-discoverable wins; keep the hover reveal as a fine-pointer enhancement.

  Use only design tokens and token-backed spacing utilities (no raw px, no one-off numeric spacing, no new hex).</action>
  <acceptance_criteria>
    - Source assertion: `chat-client.tsx` computes `groupedWithPrevious`/`groupedWithNext` via `belongsToSameMessageGroup` and imports it from `./message-grouping`.
    - Source assertion: the offline `Alert` no longer contains "will send when you're back" and contains "Reconnect"; `states.md` offline line is updated to match and no longer promises automatic send.
    - Source assertion: the four message-action controls use `min-h-control min-w-control` and no longer use `size-10`; the action bar includes a `pointer-coarse:` reveal.
    - Source assertion: no raw hex or one-off numeric spacing utilities were introduced.
    - Test command: `pnpm --filter @fish/web typecheck` and `pnpm --filter @fish/web lint` pass.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @fish/web typecheck && pnpm --filter @fish/web lint</automated>
  </verify>
  <done>Grouping uses the shared predicate, the offline copy is truthful, and message actions meet the 56px touch-safe floor.</done>
</task>

<task type="auto">
  <name>Task 3: Component tests for grouping, offline copy, and action sizing</name>
  <files>apps/web/app/(authenticated)/chat/chat-client.test.tsx</files>
  <read_first>
    @apps/web/app/(authenticated)/chat/chat-client.test.tsx
    @apps/web/app/(authenticated)/chat/chat-client.tsx
    @apps/web/app/(authenticated)/chat/store/chat-store.ts
    @apps/web/app/(authenticated)/chat/store/chat-selectors.ts
  </read_first>
  <action>Add focused tests to `chat-client.test.tsx`, building on the current working-tree state and scoping the commit to these additions only (do not touch the excluded pagination test near line 1290). Construct a community chat prop (`kind: "community"`, with `channelName`) from the existing base fixture.

  1. WR-02 grouping reappears: render `ChatClient` with a community chat whose messages include a run from the same sender where two consecutive messages are more than 5 minutes apart (or straddle a calendar day). Assert that the author name (MessageMeta) and an avatar render again for the message that starts the new group after the gap — i.e., identity/time are not suppressed on the later same-sender message.

  2. WR-03 offline copy: render `ChatClient`, then drive the conversation's realtime status to "disconnected" (e.g., wrap `chatStore.getState().setRealtimeStatus(conversationId, "disconnected")` in `act`). Assert the offline banner is shown, its text contains "Reconnect", and it does NOT contain "will send when you're back".

  3. WR-04 action sizing: render `ChatClient`, reveal the action bar for a sent, non-deleted message (focus-within or hover as the existing tests do), and assert each action control's className includes `min-h-control` and `min-w-control` and does not include `size-10`.

  Use `resetChatStoreForTests()` in setup/teardown as the other tests do.</action>
  <acceptance_criteria>
    - Behavior assertion: a later same-sender community message after the gap shows the author MessageMeta and avatar again.
    - Behavior assertion: the offline banner text contains "Reconnect" and not "will send when you're back".
    - Source/behavior assertion: rendered message-action controls carry `min-h-control min-w-control` and not `size-10`.
    - Test command: `pnpm --filter @fish/web test "app/(authenticated)/chat/chat-client.test.tsx"` passes (excluding the still-excluded pagination test).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @fish/web test "app/(authenticated)/chat/chat-client.test.tsx"</automated>
  </verify>
  <done>Component tests prove avatar/time reappear, the offline copy is truthful, and actions meet the 56px floor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendered message content -> DOM | Message text is rendered as React text content (no dangerouslySetInnerHTML); grouping/actions are presentation-only. |
| Offline UX copy -> user trust | Copy must be truthful (no promise of a queue that does not exist). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-111 | Integrity (UX accuracy) | Offline banner copy | mitigate | Replace the auto-send promise with truthful reconnect/retry copy in code and in the design skill (WR-03). |
| T-09-112 | Denial of service (a11y/touch) | 40px hover-only actions | mitigate | Restore 56px min controls and a coarse-pointer reveal so touch users can reach actions (WR-04). |
| T-09-113 | Tampering | Message rendering | accept | No change to message-body rendering; still React text content, no HTML injection surface added. |
| T-09-SC | Tampering | Package installs | accept | No package installs in this plan. |
</threat_model>

<verification>
After all tasks:
- `pnpm --filter @fish/web test "app/(authenticated)/chat/message-grouping.test.ts" "app/(authenticated)/chat/chat-client.test.tsx"`
- `pnpm --filter @fish/web test` (full suite; excluded pagination test stays excluded)
- `pnpm typecheck`
- `pnpm lint`
- `pnpm build`
</verification>

<success_criteria>
- Avatar and MessageMeta reappear on long same-sender runs and across day dividers in the community room.
- The offline banner (and the design skill) no longer promise automatic send; failed sends remain manual Retry.
- Message actions meet the 56px floor and are touch-discoverable.
- Full suite, typecheck, lint, build pass.
</success_criteria>

<closing_notes>
- Depends on 09-10 only because both edit `chat-client.test.tsx` (file-ownership sequencing). Build on 09-10's committed test state.
- Design source-of-truth: this plan loads the sketch-findings-fish skill; the 56px floor supersedes the compact hover-only reference, and the truthful offline copy supersedes the states.md queue promise.
- Working-tree caution: the uncommitted Phase-10 pagination WIP near line 1290 stays untouched.
</closing_notes>

<output>
Create `.planning/phases/09-cross-platform-chat-state/09-11-SUMMARY.md` when done.
</output>
