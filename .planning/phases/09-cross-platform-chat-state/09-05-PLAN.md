---
phase: 09-cross-platform-chat-state
plan: 05
type: execute
wave: 4
depends_on:
  - "09-03"
  - "10-04"
files_modified:
  - ".planning/debug/loading-new-messages.md"
autonomous: false
gap_closure: true
requirements:
  - CSTATE-06
must_haves:
  truths:
    - "The reported new-message failure is tested through two real authenticated sessions on the current route, with the final route and conversation/channel identity recorded rather than assumed per D-09."
    - "The evidence log records realtime status transitions and raw error/timeout/close evidence for the message, read, and reaction channels, plus the sender request and receiver-visible result."
    - "No auth, realtime, route, or rendering root cause is claimed unless the live evidence isolates it; an unreproduced report remains explicitly inconclusive and receives no speculative code change per D-13 and D-14."
  artifacts:
    - path: ".planning/debug/loading-new-messages.md"
      provides: "Reproduction protocol and timestamped two-session realtime evidence"
      contains: "SUBSCRIBED"
    - path: "apps/web/app/(authenticated)/chat/realtime.ts"
      provides: "The web Supabase Realtime channel boundary being observed"
    - path: "apps/web/app/(authenticated)/chat/hooks/use-chat-realtime.ts"
      provides: "The status-to-store and incoming-message dispatch boundary being observed"
  key_links:
    - from: "sender authenticated session"
      to: "receiver authenticated session"
      via: "Supabase message insert and realtime INSERT delivery"
      pattern: "new synthetic message appears without refresh"
    - from: "realtime.ts channel callbacks"
      to: "use-chat-realtime.ts"
      via: "message callback, reconnect callback, and disconnect callback"
      pattern: "onReconnected|onDisconnected|onMessage"
    - from: "live evidence"
      to: ".planning/debug/loading-new-messages.md"
      via: "redacted timestamped reproduction record"
      pattern: "status|error|receiver"
---

<objective>
Resolve the ambiguity in UAT Test 1 by obtaining live evidence from two authenticated browser sessions, without prescribing a realtime or auth fix that the current diagnosis has not established.

Purpose: CSTATE-06 and D-09 require the existing chat behavior to remain intact, but the current report was never reproduced against a live authenticated session. D-13 and D-14 require evidence-based verification before any behavior change is proposed.
Output: An evidence-complete debug session that records the actual route, channel statuses/errors, sender delivery, receiver rendering, and an explicit reproduced/not-reproduced/inconclusive classification.
</objective>

<execution_context>
@/Users/franz/.codex/gsd-core/workflows/execute-plan.md
@/Users/franz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-cross-platform-chat-state/09-UAT.md
@.planning/debug/loading-new-messages.md
@apps/web/e2e/chat-send.spec.ts
@apps/web/playwright.config.ts
@apps/web/app/(authenticated)/chat/realtime.ts
@apps/web/app/(authenticated)/chat/hooks/use-chat-realtime.ts
@apps/web/app/(authenticated)/chat/chat-client.tsx
@apps/web/app/(authenticated)/chat/page.tsx
@apps/web/app/(authenticated)/channels/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a live two-session evidence protocol without changing product code</name>
  <files>.planning/debug/loading-new-messages.md</files>
  <read_first>
    @.planning/debug/loading-new-messages.md
    @.planning/phases/09-cross-platform-chat-state/09-UAT.md
    @apps/web/e2e/chat-send.spec.ts
    @apps/web/app/(authenticated)/chat/realtime.ts
    @apps/web/app/(authenticated)/chat/hooks/use-chat-realtime.ts
  </read_first>
  <action>
    Append a "Live reproduction protocol" section to the existing diagnose-only debug session per D-09, D-13, and D-14. Define the exact local setup (pnpm supabase:start, pnpm seed, and the pinned localhost:3001 web origin), two independent authenticated sessions using the existing local seeded client and coach accounts from apps/web/e2e/chat-send.spec.ts, and the requirement that both sessions use the current /chat entry point while recording its final redirect and the actual conversation/channel identity.

    Define an evidence table for: receiver and sender role labels; final URL; conversation id/channel names; initial channel status; every SUBSCRIBED, CHANNEL_ERROR, TIMED_OUT, and CLOSED transition; raw WebSocket error/close payloads or an explicit note that none were exposed; sender action/request result; receiver DOM message result; duplicate count; scroll/new-message indicator state; and timestamps. Require DevTools Network -> WS frames and Console/network evidence to be captured before the send, with access and refresh tokens redacted and synthetic message text only. State that route mismatch, missing auth session, and missing live services are observations to record, not root causes.

    Make the protocol explicit that a failed reproduction must leave root_cause as not established, and that this plan does not edit realtime.ts, hooks, store, route behavior, or any server boundary.
  </action>
  <verify>
    <automated>rg -q "Live reproduction protocol" .planning/debug/loading-new-messages.md && rg -q "SUBSCRIBED" .planning/debug/loading-new-messages.md && rg -q "CHANNEL_ERROR|TIMED_OUT|CLOSED" .planning/debug/loading-new-messages.md && rg -q "localhost:3001" .planning/debug/loading-new-messages.md && rg -q "root_cause.*not established|not established.*root_cause" .planning/debug/loading-new-messages.md</automated>
  </verify>
  <done>The debug session contains a deterministic, redaction-safe two-session capture protocol and explicitly forbids speculative remediation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture authenticated two-tab realtime evidence and classify the gap</name>
  <files>.planning/debug/loading-new-messages.md</files>
  <action>Execute the live reproduction protocol from Task 1. Use two simultaneously open authenticated browser tabs/contexts: client1@fish.dev as receiver and coach@fish.dev as sender, with the local credentials already used by the existing browser smoke test. Do not copy tokens into the evidence log.</action>
  <what-built>A redaction-safe protocol for reproducing the current new-message report against the actual Supabase Realtime/browser path.</what-built>
  <how-to-verify>
    1. Start local Supabase and seed data if needed, then run the web app on http://localhost:3001.
    2. Open the receiver and sender in independent authenticated browser sessions. Navigate both through /chat; record the final URL after redirect, the visible conversation/channel, and the session role labels.
    3. Before sending, enable DevTools Network -> WS with preserved logs and Console preservation. Record the message, reads, and reactions channel join/status frames and any error/timeout/close payloads. If only raw WebSocket evidence is available, record that fact instead of inventing callback statuses.
    4. Send one unique synthetic message from the coach session. Record the send response/time, then wait without refreshing in the client session. Record whether the message appears exactly once, whether the transcript/new-message indicator changes, and whether any layout/scroll state changes unexpectedly.
    5. Repeat once from a fresh receiver load to exercise the session-restoration path described by the current debug notes. Append timestamps, route/channel identity, status/error evidence, and the receiver result to .planning/debug/loading-new-messages.md.
    6. Classify only what the evidence supports: reproduced when delivery fails with a captured boundary signal, not reproduced when the message arrives without a failure signal, or inconclusive when auth/services/capture cannot complete. Keep root_cause as not established unless a direct, repeatable failure boundary was observed. Do not implement a fix in this plan.
  </how-to-verify>
  <verify>
    <automated>test -s .planning/debug/loading-new-messages.md && rg -q "receiver|sender" .planning/debug/loading-new-messages.md && rg -q "status|error|WebSocket" .planning/debug/loading-new-messages.md && rg -q "reproduced|not reproduced|inconclusive" .planning/debug/loading-new-messages.md</automated>
    <human-check>Two real authenticated sessions completed the send/receive attempt and the debug log contains the route, channel status/error evidence, sender result, receiver result, and an evidence-based classification.</human-check>
  </verify>
  <done>The first UAT gap has a real live classification and captured realtime evidence; no unsupported root cause or code fix was introduced.</done>
  <resume-signal>Type "approved" with the evidence outcome, or describe the reproduction/capture blocker. If a failure reproduced, preserve the evidence for a follow-up fix plan rather than guessing in this plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authenticated browser sessions -> Supabase Realtime | Two real user sessions cross the browser/network boundary through JWT-authenticated channels and database-change delivery. |
| Sender browser -> existing chat write boundary | The sender uses the existing composer/server/Edge Function path; this plan does not add a direct database write. |
| Debug evidence -> repository | Runtime evidence is written to a planning log and must exclude access tokens, refresh tokens, passwords, and unnecessary message data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-05-01 | Spoofing | Two-session reproduction | mitigate | Verify the receiver/sender role labels and final authenticated route in the log; use the existing seeded accounts and never treat a shared cookie as proof of two users. |
| T-09-05-02 | Information disclosure | Browser/network evidence | mitigate | Use synthetic message text, redact JWT/access/refresh tokens, and record only the status/error fields needed to classify delivery. |
| T-09-05-03 | Tampering | Message delivery result | mitigate | Send through the existing authorized chat UI and preserve the observed request/channel evidence; do not inject database rows or alter RLS/server boundaries. |
| T-09-05-04 | Repudiation | UAT/debug classification | mitigate | Store timestamps, final route, channel names, status sequence, sender result, receiver result, and explicit capture limitations in the debug session. |
| T-09-05-05 | Elevation of privilege | Realtime/auth diagnosis | mitigate | Treat route/session/channel observations as evidence only; do not move auth or membership decisions into browser state or propose a fix without a repeatable boundary signal. |
| T-09-SC | Tampering | Package installation | accept | No package-manager install is part of this diagnostic plan. |
</threat_model>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Portable chat state plus unchanged web chat behavior | 09-05, 09-06 | COVERED | 09-05 closes the unresolved live-behavior evidence gap; 09-06 closes the native documentation drift. |
| REQ | CSTATE-01 | Portable dependency-clean core | 09-01 | COVERED | Existing implementation plan remains the owner. |
| REQ | CSTATE-02 | Focused web hooks | 09-02 | COVERED | Existing implementation plan remains the owner. |
| REQ | CSTATE-03 | Web-only Zustand adapter | 09-03 | COVERED | Existing implementation plan remains the owner. |
| REQ | CSTATE-04 | Fixtures and protocol | 09-01, 09-04, 09-06 | COVERED | 09-06 synchronizes the native companion after the Phase 10 contract extension. |
| REQ | CSTATE-05 | Native architecture notes | 09-04, 09-06 | COVERED | 09-06 restores current pagination parity and canonical ownership. |
| REQ | CSTATE-06 | Existing behavior and release gates | 09-01, 09-02, 09-03, 09-05 | COVERED | 09-05 supplies the missing live authenticated evidence without asserting an unproven fix. |
| RESEARCH | realtime boundary/e2e gate | Observe realtime.ts and the existing Playwright auth pattern | 09-05 | COVERED | The plan uses the existing Supabase/browser boundary and local seeded accounts. |
| RESEARCH | canonical documentation ownership | Phase 09 native notes are the canonical native companion | 09-06 | COVERED | The research Open Question 2 resolution is preserved. |
| CONTEXT | D-09, D-13, D-14 | Preserve visible chat behavior and verify with evidence/gates | 09-05 | COVERED | Live delivery is classified before any remediation is proposed. |
| CONTEXT | D-11, D-12 | Native work remains docs/fixtures and platform-neutral | 09-06 | COVERED | No Android/iOS production source is planned. |
</source_audit>

<verification>
- `rg -q "Live reproduction protocol" .planning/debug/loading-new-messages.md`
- The blocking human checkpoint completes with two authenticated sessions and captured status/error evidence.
- `test -s .planning/debug/loading-new-messages.md` and the outcome classification is explicit.
- No production code or server boundary is changed by this diagnostic plan.
</verification>

<success_criteria>
- A live two-session test has either reproduced the failure with direct evidence, not reproduced it, or documented an environment/capture blocker.
- The evidence includes actual route/channel identity, realtime statuses/errors, sender result, receiver result, and timestamps.
- The debug record does not claim an auth race, route bug, or store/render bug without direct evidence, and no speculative fix is implemented.
</success_criteria>

<output>
Create `.planning/phases/09-cross-platform-chat-state/09-05-SUMMARY.md` when done.
</output>
