---
phase: 06-tracker-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0009_tracker.sql
  - apps/web/lib/validation/tracker.ts
  - apps/web/lib/validation/tracker.test.ts
  - scripts/seed.ts
  - scripts/verify-rls.ts
  - packages/supabase/src/database.generated.ts
  - packages/supabase/src/database.types.ts
autonomous: true
requirements: [TRAK-01, TRAK-04, TRAK-05, XC-01, XC-02]
must_haves:
  truths:
    - "A tracker config version stores fields + a daily/weekly cadence in the database, not hard-coded in the UI (TRAK-01)."
    - "A malformed tracker field config is rejected by BOTH zod (app) and pg_jsonschema (DB CHECK) (TRAK-04, XC-02)."
    - "A saved tracker entry pins version_id and a field snapshot; a used version/field cannot be mutated (TRAK-04)."
    - "A client saves and reads only their own entries through the save_tracker_entry RPC + RLS; an assigned coach reads them; an unassigned coach and a cross-client read return zero rows (XC-01)."
    - "A client's direct INSERT into tracker_assignments is rejected — no authenticated INSERT policy exists (TRAK-02 self-assign gate at the DB layer)."
    - "The ordered coach-authored milestone STEPS (plain-language labels) live in a version-scoped tracker_milestones config table, seeded neutrally, readable by the client + assigned coach — the step definitions are never auto-generated from a numeric threshold (TRAK-05, UI-SPEC A-03)."
    - "get_tracker_progress() derives entries_count + a current-step fill (0–100) at read time from tracker_entries with no stored counter; the fill is the aria-valuenow of the CURRENT step only, never a persisted counter and never a visible milestone fraction/count (TRAK-05)."
  artifacts:
    - path: "supabase/migrations/0009_tracker.sql"
      provides: "tracker_configs / tracker_config_versions / tracker_fields / tracker_milestones / tracker_assignments / tracker_entries tables, RLS, save_tracker_entry, get_tracker_progress, get_tracker_milestone_steps, freeze triggers, pg_jsonschema CHECK, partial unique indexes"
      contains: "create function public.save_tracker_entry"
    - path: "supabase/migrations/0009_tracker.sql"
      provides: "tracker_milestones version-scoped ordered-step config table + get_tracker_milestone_steps read path"
      contains: "create table public.tracker_milestones"
    - path: "apps/web/lib/validation/tracker.ts"
      provides: "saveTrackerEntrySchema + assignTrackerRequestSchema + reused fieldConfigSchema/fieldAnswerSchema/validateFieldAnswer/parseFieldConfig"
    - path: "scripts/verify-rls.ts"
      provides: "checkTracker* live RLS assertions wired into main()"
    - path: "scripts/seed.ts"
      provides: "seedTrackerConfig() — one active published version + neutral six-type fields + cadence + ordered neutral milestone steps"
    - path: "packages/supabase/src/database.types.ts"
      provides: "TrackerConfigRow / TrackerConfigVersionRow / TrackerFieldRow / TrackerMilestoneRow / TrackerAssignmentRow / TrackerEntryRow aliases"
  key_links:
    - from: "save_tracker_entry"
      to: "auth.uid()"
      via: "security definer derives client_id, joins active tracker_assignments"
      pattern: "auth\\.uid\\(\\)"
    - from: "tracker_fields"
      to: "pg_jsonschema"
      via: "jsonb_matches_schema CHECK constraint"
      pattern: "jsonb_matches_schema"
    - from: "get_tracker_milestone_steps"
      to: "tracker_milestones"
      via: "returns the ordered { id, label } steps for the caller's assigned version"
      pattern: "tracker_milestones"
  prohibitions:
    - statement: "No column named streak/streak_count/adherence/adherence_pct/completion_rate/grade/score/percent exists on ANY tracker table (TRAK-05 schema invariant)."
      status: must-not-exist
      verification: "grep -v '^--' supabase/migrations/0009_tracker.sql | grep -Eic 'streak|adherence|completion_rate|(grade|score)[a-z_]*\\b|percent' | grep -qx 0"
    - statement: "get_tracker_progress never returns a persisted milestone-fraction column and get_tracker_milestone_steps returns only { id, label } (label/order come from the coach-authored tracker_milestones config, never a computed count/5 fraction)."
      status: must-not-exist
      verification: "grep -Ei 'milestones_reached|count\\(\\*\\)\\s*/\\s*[0-9]' supabase/migrations/0009_tracker.sql | grep -qc . && exit 1 || echo ok"
    - statement: "save_tracker_entry / get_tracker_progress / get_tracker_milestone_steps signatures never accept client_id or coach_id parameters (server derives identity from auth.uid())."
      status: must-not-exist
      verification: "grep -E 'save_tracker_entry\\([^)]*(client_id|coach_id)|get_tracker_progress\\([^)]*(client_id|coach_id)|get_tracker_milestone_steps\\([^)]*(client_id|coach_id)' supabase/migrations/0009_tracker.sql | grep -qc . && exit 1 || echo ok"
    - statement: "No authenticated INSERT/UPDATE/DELETE policy or grant on tracker_assignments (writes are service-role-only via assign-tracker)."
      status: must-not-exist
      verification: "grep -Ei 'for insert|for update|for delete' supabase/migrations/0009_tracker.sql | grep -i tracker_assignments | grep -qc . && exit 1 || echo ok"
    - statement: "zod is not imported anywhere under packages/core (zod stays in apps/web + Edge Function only, XC-02)."
      status: must-not-exist
      verification: "grep -rEn \"from ['\\\"]zod['\\\"]\" packages/core/src | grep -qc . && exit 1 || echo ok"
---

<objective>
Build the tracker data + validation foundation: migration `0009_tracker.sql` (the six versioned-config tables — including the coach-authored `tracker_milestones` ordered-step config table — RLS, the `save_tracker_entry`/`get_tracker_progress`/`get_tracker_milestone_steps` command+read functions, `pg_jsonschema` CHECK backstop, freeze-on-use triggers, and the "exactly one active tracker" partial unique index), the app-side zod validation module, the new `checkTracker*` live RLS assertions, the neutral seed config (fields + ordered milestone steps), and regenerated Supabase types — followed by a [BLOCKING] local apply.

This is a near-exact structural repeat of Phase 5's `0008_onboarding.sql` + `apps/web/lib/validation/onboarding.ts` + `scripts/verify-rls.ts` extension. Copy those analogs; do not redesign. The one net-new piece is the `tracker_milestones` config table + its ordered-step read path, which mirrors the `tracker_fields` versioned-config discipline exactly.

Purpose: Establish the un-bypassable database boundary (versioned config, coach-authored milestone steps, RLS, no-streak schema invariant, RPC-only writes) that Waves 2 and 3 build the client and coach surfaces on. This is the milestone's "engines with minimal seed config" rule — config + engine, NOT a coach-authoring UI (assignment/config stays seed-only this milestone).
Output: Applied tracker schema on the local Supabase stack, green `pnpm verify:rls`, generated row aliases, and a seeded active assignment + neutral milestone steps for `client1`.
</objective>

<execution_context>
@/Users/franz/Library/Application Support/Claude/local-agent-mode-sessions/614fd504-8566-46b2-8256-66ded77bb604/43ed32e3-053c-413f-843e-d49e1f00f5e1/rpm/plugin_019FPGEfUV8XSdonegN1mWDs/workflows/execute-plan.md
@/Users/franz/Library/Application Support/Claude/local-agent-mode-sessions/614fd504-8566-46b2-8256-66ded77bb604/43ed32e3-053c-413f-843e-d49e1f00f5e1/rpm/plugin_019FPGEfUV8XSdonegN1mWDs/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tracker-engine/06-RESEARCH.md
@.planning/phases/06-tracker-engine/06-PATTERNS.md
@.planning/phases/06-tracker-engine/06-VALIDATION.md
@.planning/phases/06-tracker-engine/06-UI-SPEC.md

<interfaces>
<!-- Exact identifiers extracted from the codebase this session. Use directly. -->

From apps/web/lib/validation/onboarding.ts (the direct analog — reuse its exported symbols):
  export const fieldConfigSchema   // z.discriminatedUnion("type", [...six...])
  export const fieldAnswerSchema   // z.discriminatedUnion("type", [...six...])
  export const saveOnboardingAnswerSchema  // z.strictObject({ questionId, answer })
  export function parseFieldConfig(input: unknown): FieldConfig
  export function validateFieldAnswer(config, answer): { success: true; data } | { success: false; error }

From packages/core/src/fields.ts (imported unchanged — do NOT edit this file):
  FieldConfig, FieldAnswer, FieldOption  // the six-type discriminated unions

From scripts/seed.ts:
  const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY, {...})
  async function seedOnboardingAssessment(): Promise<void>   // upsert-with-onConflict analog
  async function main()  // calls await seedOnboardingAssessment() at line ~280; add await seedTrackerConfig() after it

From scripts/verify-rls.ts:
  async function signInAs(email, password)  // publishable/anon key session — RLS applies
  const coach = { email: "coach@fish.dev", password: "fish-coach-dev" }         // assigned to client1
  const client1 = { email: "client1@fish.dev", password: "fish-client-dev" }
  const client2 = { email: "client2@fish.dev", password: "fish-client-dev" }
  const coach2Unassigned = { email: "coach2@fish.dev", password: "fish-coach-dev" }
  const admin = createClient(url, SERVICE_ROLE_KEY, {...})  // service-role for fixture setup + DB-level assertions
  report(label, condition, detail); checkNoRecursion(label, error)  // assertion helpers
  main() wires assertions in sequence (line ~646); add await checkTracker* calls after the onboarding block

Command/read functions to author in 0009 (signatures fixed — no client_id/coach_id params):
  public.save_tracker_entry(p_field_id uuid, p_answer jsonb)
    returns table (assignment_id uuid, entry_id uuid, entry_date date, status text)
  public.get_tracker_progress()
    returns table (entries_count bigint, current_step_progress integer)   -- current_step_progress is a 0..100 fill for the CURRENT step only, never a milestone fraction
  public.get_tracker_milestone_steps()
    returns table (milestone_id uuid, step_order integer, label text)      -- ordered coach-authored steps for the caller's assigned version

RLS helper reused verbatim (do NOT redefine): private.is_coach_of(client_uuid uuid) from 0004_rls_helpers.sql
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 0009_tracker.sql — tables (incl. tracker_milestones), pg_jsonschema CHECK, RLS, command+read functions, freeze triggers, partial unique indexes</name>
  <files>supabase/migrations/0009_tracker.sql</files>
  <read_first>
    - supabase/migrations/0008_onboarding.sql (PRIMARY analog — table shape, jsonb_matches_schema CHECK string, three-branch RLS read policy, save_onboarding_answer command function, revoke/grant execute, reject_used_*_mutation freeze triggers, onboarding_one_active_version partial unique index)
    - supabase/migrations/0004_rls_helpers.sql (private.is_coach_of — reused verbatim in read policies; note it reads auth.uid() internally)
    - supabase/migrations/0003_coach_clients.sql (the "client reads own coach assignment" self-read policy — relevant to Wave 3's membership gate, not this file)
    - supabase/migrations/0007_client_profiles.sql (sibling grant/trigger freeze discipline)
    - .planning/phases/06-tracker-engine/06-RESEARCH.md § Recommended Data Model, § RLS and Authorization Strategy, § Code Examples (the fully-drafted save_tracker_entry body), Pattern 1/2/4, Pitfall 3/4
    - .planning/phases/06-tracker-engine/06-PATTERNS.md § 0009_tracker.sql (line-referenced excerpts of every shape to copy)
    - .planning/phases/06-tracker-engine/06-UI-SPEC.md § Milestone-Journey Progress — Concrete Visual Contract, Assumption A-03 (steps are coach-authored plain-language labels, NEVER a numeric threshold rendered as a fraction)
  </read_first>
  <action>
    Create `supabase/migrations/0009_tracker.sql` mirroring `0008_onboarding.sql`'s structure with these concrete objects:

    Tables (rename onboarding_* → tracker_*, question* → field*): `tracker_configs` (id, slug unique, title, timestamps); `tracker_config_versions` (adds `cadence text not null check (cadence in ('daily','weekly'))` plus the `status`/`is_active`/`published_at` columns and the `tracker_active_version_is_published` CHECK); `tracker_fields` (field_key, field_order check (>0), prompt, answer_type six-type check, `config jsonb`, unique (version_id,field_key), unique (version_id,field_order)); `tracker_assignments` (client_id → profiles on delete cascade, coach_id → profiles, version_id → tracker_config_versions on delete restrict, `status text check (status in ('active','ended')) default 'active'`, assigned_at, ended_at); `tracker_entries` (assignment_id → tracker_assignments on delete cascade, field_id → tracker_fields on delete restrict, version_id denormalized pin, the field snapshot columns field_key/field_order/field_prompt/answer_type/field_config, `answer jsonb`, `entry_date date not null default current_date`, timestamps, named unique constraint `tracker_entries_assignment_field_period_key` on (assignment_id, field_id, entry_date)).

    NEW milestone-config table (the BLOCKER fix — mirrors the tracker_fields versioned-config discipline): `tracker_milestones` (id, `version_id uuid not null references public.tracker_config_versions (id) on delete cascade`, `step_order integer not null check (step_order > 0)`, `label text not null` — a plain-language coach-authored step label, e.g. "Log three days this week", NEVER a numeric threshold — timestamps, `unique (version_id, step_order)`). This table is the ONLY source of the ordered milestone steps; the done/now/up-next STATE (computed in Wave 2) comes from these coach-authored steps + which the client has reached, NOT from an entries count-as-fraction.

    Config validation: copy the `onboarding_question_config_type_matches` CHECK ((config ->> 'type') = answer_type) and the `onboarding_question_config_schema` `jsonb_matches_schema(...)` CHECK BYTE-FOR-BYTE from 0008 (per Pitfall 3 — the six-type union is identical), renamed to `tracker_field_config_type_matches` / `tracker_field_config_schema`.

    Partial unique indexes: `tracker_one_active_version` on tracker_config_versions (tracker_config_id) where is_active; and `tracker_one_active_assignment_per_client` on tracker_assignments (client_id) where status = 'active' (per Pattern 2 — the "exactly one active tracker" DB backstop).

    RLS: enable RLS on all six tables. Config reads (versions/fields/milestones): the three-branch published-or-referenced-by-own/assigned pattern from 0008 (join through tracker_assignments instead of onboarding_attempts) — a client and their assigned coach can read the milestone steps of the client's assigned version. tracker_assignments reads: client self (client_id = auth.uid()) OR private.is_coach_of(client_id). tracker_assignments writes: NO authenticated insert/update/delete policy at all (service-role-only). tracker_entries reads: join-through-assignment (own via ta.client_id = auth.uid(); coach via private.is_coach_of(ta.client_id)). tracker_entries writes: NO direct authenticated INSERT/UPDATE grant — writes only via the RPC below. Grant `select` on all six tables to authenticated; full CRUD to service_role, matching the 0008 grant block.

    Command function `save_tracker_entry(p_field_id uuid, p_answer jsonb)` — copy the drafted body verbatim from 06-RESEARCH.md § Code Examples: security definer, set search_path = '', derives v_client_id from auth.uid() (raise if null), looks up the field, joins the client's ACTIVE tracker_assignments row + the version's cadence, raises if no active assignment, derives v_period per cadence (`date_trunc('week', current_date)::date` for weekly else `current_date` — Pitfall 4), upserts into tracker_entries `on conflict on constraint tracker_entries_assignment_field_period_key do update`, returns (assignment_id, entry_id, entry_date, status). Follow with `revoke execute ... from public; grant execute ... to authenticated;` (two-line pattern, both param signatures matching).

    Read-only step-state SOURCE `get_tracker_milestone_steps()` (the BLOCKER data-source fix) — security definer, stable, set search_path = '', SELECT-only: return the ordered `(milestone_id, step_order, label)` rows from `tracker_milestones` joined to the caller's ACTIVE assignment's version via auth.uid(), `order by step_order asc`. This is what Wave 2 consumes to render labeled steps; the done/now/up-next state is computed in Wave 2 from these coach-authored steps, not here. Same revoke/grant execute pair (`from public` → `to authenticated`).

    Read-only aggregate `get_tracker_progress()` — security definer, stable, set search_path = '', SELECT-only: return `count(*)::bigint as entries_count` and a `current_step_progress integer` that is a 0..100 fill for the CURRENT step ONLY (the aria-valuenow of the current step's Progress bar), NOT a persisted counter and NOT a `count(*)/5` milestone fraction. Compute it as a bounded expression over this-period entries for the active assignment (e.g. `least(100, (count of this-period saved entries * 100) / greatest(nullif(count of fields for the version, 0), 1))::integer`) so it never reads as a visible milestone count/fraction. Do NOT emit a `milestones_reached` column and do NOT use `count(*)/5` as a step-state driver. No counter column, no write. Same revoke/grant execute pair.

    Freeze triggers: `reject_used_tracker_version_mutation` (before update or delete on tracker_config_versions, rejects if any tracker_assignments references old.id) and `reject_used_tracker_field_mutation` (before update or delete on tracker_fields, rejects if any tracker_entries references old.id) — rename the 0008 reject_used_*_mutation function/trigger bodies. IMPORTANT (WARNING — seed idempotency fix): make BOTH freeze triggers NO-OP-AWARE — on an UPDATE, compare OLD vs NEW and only raise when a config-bearing column actually changes (for versions: any of status/is_active/cadence/published_at/version; for fields: any of field_key/field_order/prompt/answer_type/config), returning NEW unchanged when nothing config-bearing changed. A bare `set updated_at = now()` no-op re-run from `seedTrackerConfig()` (Task 2) must NOT be rejected even after an assignment references the version. Keep the DELETE branch rejecting unconditionally when referenced. (Apply this same no-op-aware discipline symmetrically to the two triggers here; the onboarding triggers in 0008 are left as-is unless a shared seed path forces the same fix — note in the summary if so.)

    Qualify all output column references in the plpgsql functions to avoid the ambiguous-column error seen in 05-01 (SUMMARY deviation 1). Do NOT add any column named streak/adherence/completion_rate/grade/score/percent anywhere, and do NOT emit a `milestones_reached` count-as-fraction column.
  </action>
  <verify>
    <automated>grep -c "jsonb_matches_schema" supabase/migrations/0009_tracker.sql; grep -c "create table public.tracker_milestones" supabase/migrations/0009_tracker.sql; grep -c "get_tracker_milestone_steps" supabase/migrations/0009_tracker.sql; grep -Ei 'milestones_reached|count\(\*\)\s*/\s*[0-9]' supabase/migrations/0009_tracker.sql | grep -qc . && echo "FRACTION-DRIVER FOUND" || echo "no milestone-fraction driver"; grep -v '^--' supabase/migrations/0009_tracker.sql | grep -Eic 'streak|adherence|completion_rate|percent' | grep -qx 0 && echo "no-streak scan passed"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0009_tracker.sql` defines all six tables (including `tracker_milestones`), both partial unique indexes, the pg_jsonschema CHECK, RLS on every table, `save_tracker_entry` + `get_tracker_progress` + `get_tracker_milestone_steps` with revoke/grant execute, and both no-op-aware freeze triggers.
    - Source assertion: `grep -c "create table public.tracker_milestones"` returns 1 and `grep -c "get_tracker_milestone_steps"` ≥ 2 (definition + grant).
    - Source assertion: `grep -Ei 'milestones_reached|count\(\*\)\s*/\s*[0-9]' supabase/migrations/0009_tracker.sql` returns nothing (no count-as-fraction step-state driver — the milestone steps come from `tracker_milestones`).
    - Source assertion: `grep -v '^--' supabase/migrations/0009_tracker.sql | grep -Eic 'streak|adherence|completion_rate|percent'` returns 0.
    - Source assertion: `grep -E 'for insert|for update|for delete' supabase/migrations/0009_tracker.sql | grep -i tracker_assignments` returns nothing (no authenticated write policy).
    - Source assertion: `grep -E 'save_tracker_entry\([^)]*(client_id|coach_id)' supabase/migrations/0009_tracker.sql` returns nothing.
    - The freeze triggers compare OLD vs NEW and return NEW unchanged on a config-unchanged UPDATE (no-op-aware) so a `seedTrackerConfig()` re-run is not rejected.
  </acceptance_criteria>
  <done>The migration file exists, is self-contained/replayable, defines the coach-authored milestone-step config table + its read path, uses no-op-aware freeze triggers, and passes all source scans above. Actual apply happens in Task 4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tracker zod validation module (reusing onboarding schemas) + seed the neutral active config (fields + milestone steps), re-run-safe</name>
  <files>apps/web/lib/validation/tracker.ts, apps/web/lib/validation/tracker.test.ts, scripts/seed.ts</files>
  <read_first>
    - apps/web/lib/validation/onboarding.ts (the exact discriminated-union + validateFieldAnswer analog; export names listed in <interfaces> — these are IMPORTED, never copied)
    - apps/web/lib/validation/onboarding.test.ts (test shape for the six-type coverage + malformed-config rejection)
    - scripts/seed.ts (seedOnboardingAssessment upsert-with-onConflict shape at ~line 306; the trackerFields seed array already stubbed at ~line 215; main() wiring)
    - .planning/phases/06-tracker-engine/06-PATTERNS.md § tracker.ts, § seed.ts
  </read_first>
  <behavior>
    - Test: `saveTrackerEntrySchema` accepts `{ fieldId: <uuid>, answer: <valid FieldAnswer> }` and rejects a non-uuid fieldId or a malformed answer.
    - Test: `assignTrackerRequestSchema` accepts `{ clientId: <uuid> }` and rejects a missing/non-uuid clientId (and rejects any coachId/versionId keys via strictObject).
    - Test: config validation rejects a malformed field config (e.g. single_select with empty options / duplicate option ids) — one case per the six types, mirroring onboarding.test.ts.
    - Test: `validateFieldAnswer` returns success for a valid answer against each of the six config types and failure for a mismatched answer.
  </behavior>
  <action>
    Create `apps/web/lib/validation/tracker.ts`. Reuse the onboarding config/answer schemas: `import { fieldConfigSchema, fieldAnswerSchema, parseFieldConfig, validateFieldAnswer } from "@/lib/validation/onboarding"` (direct import — the four symbols are imported, NEVER copied/duplicated; the six-type discriminated union is identical). Add and export `saveTrackerEntrySchema = z.strictObject({ fieldId: z.string().uuid(...), answer: fieldAnswerSchema })` and `assignTrackerRequestSchema = z.strictObject({ clientId: z.string().uuid(...) })` (strictObject so a coachId/versionId in the body is rejected — server derives those). Keep zod imports in this file only; never in packages/core.

    Create `apps/web/lib/validation/tracker.test.ts` covering the four behavior cases above.

    In `scripts/seed.ts`, add `seedTrackerConfig()` mirroring `seedOnboardingAssessment()`: upsert one `tracker_configs` row (slug e.g. `daily-check-in`, title from config), upsert one `tracker_config_versions` row (version 1, status published, is_active true, cadence 'daily', published_at now), then upsert the neutral `tracker_fields` set (the already-stubbed boolean "practice moment" / single_select "practice focus" / long_text "anything helpful" — calm non-scoring labels, NEVER numeric-grade copy), AND upsert an ordered neutral set of `tracker_milestones` rows for that version (plain-language coach-voice step labels, e.g. "Settle into a daily check-in", "Log a few days this week", "Build a steady rhythm" — 3-5 steps, step_order ascending, NEVER a numeric threshold like "3 of 5"). Use `onConflict` idempotent upserts (`tracker_config_versions` on `tracker_config_id,version`; `tracker_fields` on `version_id,field_key`; `tracker_milestones` on `version_id,step_order`).

    Idempotency (WARNING fix — pick and state ONE path): the freeze trigger is now NO-OP-AWARE (Task 1), so a `seedTrackerConfig()` re-run whose upsert leaves the config-bearing columns unchanged does NOT trip `reject_used_tracker_version_mutation` even after an assignment references the version — a bare `pnpm seed` re-run stays green and idempotent. State this in the seed block's comment: "re-run-safe because the version/field freeze triggers are no-op-aware (0009); an unchanged upsert is not a config mutation." Do NOT rely on the seed running only under `db:reset`. Wire `await seedTrackerConfig();` into `main()` after `await seedOnboardingAssessment();`. Do NOT invoke assign-tracker here (that Edge Function ships in Wave 3); leave the assignment fixture creation to verify-rls setup (Task 3) using a direct service-role insert, matching the research fallback.
  </action>
  <verify>
    <automated>pnpm --filter @fish/web test -- --run apps/web/lib/validation/tracker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @fish/web test -- --run apps/web/lib/validation/tracker.test.ts` passes.
    - `grep -n "from \"@/lib/validation/onboarding\"" apps/web/lib/validation/tracker.ts` shows `fieldConfigSchema`/`fieldAnswerSchema`/`parseFieldConfig`/`validateFieldAnswer` are IMPORTED (not re-declared) — `grep -c "z.discriminatedUnion" apps/web/lib/validation/tracker.ts` returns 0.
    - `grep -rEn "from ['\"]zod['\"]" packages/core/src` returns nothing (zod boundary held).
    - `grep -n "seedTrackerConfig\|tracker_milestones" scripts/seed.ts` shows the definition, the `tracker_milestones` upsert, and the `await` call inside main().
    - Seed field + milestone labels contain no `score|grade|streak|percent|adherence|%`: `grep -Eic 'score|grade|streak|percent|adherence' scripts/seed.ts` unchanged-or-0 for the tracker block.
  </acceptance_criteria>
  <done>tracker.ts IMPORTS (never copies) the onboarding field schemas and exports the two new schemas; tracker.test.ts is green; seedTrackerConfig() seeds fields + ordered milestone steps and is wired into main(), re-run-safe against the no-op-aware freeze triggers.</done>
</task>

<task type="auto">
  <name>Task 3: Add checkTracker* live RLS assertions + assignment fixture (incl. milestone-step read), wired into verify-rls main()</name>
  <files>scripts/verify-rls.ts</files>
  <read_first>
    - scripts/verify-rls.ts (checkOnboardingUnassignedCoachDenied / checkOnboardingCrossClientDenied at ~lines 513-563 — the signInAs → real-session query → checkNoRecursion → report template; resetOnboardingVerificationState at ~line 400 for the idempotent-fixture pattern; the coach-reads-own-assignment idiom at lines 264-269; main() wiring at ~line 646)
    - .planning/phases/06-tracker-engine/06-VALIDATION.md § Per-Task Verification Map, § Nyquist-Style Validation Dimensions (Authorization + Assignment invariant + Schema invariant rows)
    - .planning/phases/06-tracker-engine/06-PATTERNS.md § verify-rls.ts
  </read_first>
  <action>
    In `scripts/verify-rls.ts`, add a `resetTrackerVerificationState()` (service-role) that: deletes client1's tracker_entries + tracker_assignments, then re-inserts exactly one active tracker_assignment for client1 (client_id = client1's id, coach_id = the assigned coach's id, version_id = the seeded active version) via the `admin` service-role client — this stands in for the assign-tracker Edge Function during Wave 1 (the live-function proof is added in Wave 3). Make it idempotent so repeated `pnpm verify:rls` runs stay meaningful, exactly like resetOnboardingVerificationState.

    Add these `checkTracker*` assertion functions (mirroring checkOnboarding* shape, all via signInAs real sessions):
    - `checkTrackerEntrySelfSave` — client1 calls the save_tracker_entry RPC for a seeded field, gets a row back, and reads exactly their own entry (self-ownership).
    - `checkTrackerMilestoneStepsReadable` — client1 calls `get_tracker_milestone_steps()` (or selects tracker_milestones for their assigned version) and gets the ordered coach-authored `{ label }` steps back (>0 rows, ordered), proving the milestone-step SOURCE the Wave 2 surface consumes exists and is RLS-readable; the assigned coach can read the same steps; a cross-client (client2) gets zero rows.
    - `checkTrackerActiveAssignmentGate` — a client with an ENDED/absent assignment (use client2, who has none) calling save_tracker_entry is rejected (raises / returns error), proving the active-assignment gate.
    - `checkTrackerAssignedCoachReadsEntries` — the assigned coach reads client1's tracker_entries (rows returned).
    - `checkTrackerUnassignedCoachDenied` — coach2Unassigned selecting client1's tracker_entries gets zero rows, no error.
    - `checkTrackerCrossClientDenied` — client2 selecting client1's tracker_entries/assignments gets zero rows.
    - `checkTrackerSelfAssignRejected` — client1 attempts a direct INSERT into tracker_assignments (their own client_id) and is rejected by RLS (no authenticated INSERT policy → error). This is the DB-layer half of the self-assign gate; Wave 3 adds the live-Edge-Function half.

    Also add `checkTrackerMalformedConfigRejected` (service-role attempt to insert a tracker_fields row with a malformed config → DB CHECK rejects) and `checkTrackerUsedVersionImmutable` (service-role UPDATE on the used version's config-bearing column → freeze trigger rejects; AND a service-role no-op `set updated_at = now()` on the same used version is ACCEPTED — proving the no-op-aware trigger fix so the seed idempotency claim is verified, not just asserted), mirroring checkOnboardingMalformedConfigRejected / checkOnboardingUsedVersionImmutable.

    Wire all of these into `main()` after the onboarding block: call `resetTrackerVerificationState()` first, then the assertions.
  </action>
  <verify>
    <automated>grep -c "checkTracker" scripts/verify-rls.ts; grep -n "resetTrackerVerificationState\|checkTrackerSelfAssignRejected\|checkTrackerEntrySelfSave\|checkTrackerMilestoneStepsReadable" scripts/verify-rls.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-rls.ts` defines all `checkTracker*` functions (including `checkTrackerMilestoneStepsReadable`) + `resetTrackerVerificationState`, each wired into main() in order.
    - Assertions use `signInAs()` (anon/publishable key), never SET ROLE: `grep -n "SET ROLE" scripts/verify-rls.ts` returns nothing new.
    - Negative assertions assert `rows.length === 0` with no error (no enumeration side channel), matching the checkOnboarding negative pattern.
    - `checkTrackerUsedVersionImmutable` asserts BOTH that a config-bearing UPDATE is rejected AND a no-op `updated_at`-only UPDATE is accepted (the seed idempotency guarantee).
    - Actual pass/fail is proven by the [BLOCKING] apply in Task 4 — this task only authors the assertions.
  </acceptance_criteria>
  <done>All tracker assertions (incl. milestone-step readability + the no-op-aware freeze proof) + the idempotent tracker fixture reset exist and are wired into main(); they will be executed green in Task 4.</done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Apply migration locally, regenerate types, seed, and run verify:rls green</name>
  <files>packages/supabase/src/database.generated.ts, packages/supabase/src/database.types.ts</files>
  <read_first>
    - .planning/phases/04-client-profiles/04-01-SUMMARY.md (the "[BLOCKING] Apply migrations locally, seed, then run verify:rls green" gate pattern — Task 4 there)
    - .planning/phases/05-data-driven-onboarding/05-01-SUMMARY.md (same gate; note the ambiguous-column deviation fixed here-style)
    - package.json scripts: db:reset (`supabase db reset`), seed, verify:rls; type generation into packages/supabase/src/database.generated.ts
    - packages/supabase/src/database.types.ts (existing Onboarding*Row aliases — add Tracker*Row aliases in the same convention)
  </read_first>
  <action>
    This task has no new source authoring beyond the type aliases — it is the mandatory local-apply gate the phase cannot pass without.

    1. Run `pnpm db:reset` to re-apply all migrations (0001→0009) + seed to the local Docker Supabase stack. If the migration fails to apply (e.g. an ambiguous output-column reference in a plpgsql function, as happened in 05-01), fix `0009_tracker.sql` in place, and re-run — do not proceed until it applies cleanly.
    2. Regenerate Supabase types into `packages/supabase/src/database.generated.ts` (the project's existing generate step). Add `TrackerConfigRow`, `TrackerConfigVersionRow`, `TrackerFieldRow`, `TrackerMilestoneRow`, `TrackerAssignmentRow`, `TrackerEntryRow` aliases to `packages/supabase/src/database.types.ts`, mirroring the existing `Onboarding*Row` alias convention exactly.
    3. Run `pnpm seed` TWICE and confirm the tracker config + milestone steps + client1 assignment fixture exist and the SECOND run stays green (proving the no-op-aware freeze triggers make the seed re-run-safe even with the assignment referencing the version).
    4. Run `pnpm verify:rls` and confirm ALL assertions pass (the pre-existing set + the new checkTracker* set, including `checkTrackerMilestoneStepsReadable` and the no-op-aware freeze proof), ending with `All assertions passed.`
    5. Run `pnpm typecheck` and `pnpm build` green (types come from the regenerated config, so this proves the aliases compile).
  </action>
  <verify>
    <automated>pnpm db:reset && pnpm seed && pnpm seed && pnpm verify:rls && pnpm --filter @fish/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:reset` applies 0009 cleanly; a second `pnpm seed` re-run stays green (no freeze-trigger abort); `pnpm verify:rls` exits 0 with all tracker + pre-existing assertions PASS, ending `All assertions passed.`
    - `grep -c "TrackerEntryRow\|TrackerAssignmentRow\|TrackerMilestoneRow\|TrackerConfigVersionRow" packages/supabase/src/database.types.ts` ≥ 4.
    - `pnpm typecheck` passes.
    - Live schema grep: the no-streak invariant is proven both in source (Task 1) and by the schema applying without any streak/adherence column; the milestone steps read back from `tracker_milestones`, not a computed fraction.
  </acceptance_criteria>
  <done>The tracker schema is live on the local stack, types are regenerated with the six Tracker*Row aliases, seed created the fixtures (fields + milestone steps + assignment) and is re-run-safe, and `pnpm verify:rls` is fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → tracker_entries write | A client may only write their own entry, only via `save_tracker_entry`, only against their active assignment. |
| authenticated user → tracker_assignments write | No authenticated write path exists; assignment is service-role-only (Wave 3 Edge Function). |
| service-role/seed → config tables | Trusted config authoring; guarded by pg_jsonschema CHECK + no-op-aware freeze triggers so even a trusted caller cannot persist malformed or retroactively-mutate a used version, while an unchanged re-seed is allowed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | tracker_assignments direct INSERT by a client | mitigate | No authenticated INSERT policy; `checkTrackerSelfAssignRejected` proves the DB rejects it. |
| T-06-02 | Tampering | malformed tracker field config persisted | mitigate | zod (app) + `jsonb_matches_schema` DB CHECK (byte-for-byte from 0008); `checkTrackerMalformedConfigRejected`. |
| T-06-03 | Repudiation / Tampering | used version/field edited after entries exist | mitigate | `reject_used_tracker_version_mutation` / `reject_used_tracker_field_mutation` no-op-aware freeze triggers + entry field snapshot columns; `checkTrackerUsedVersionImmutable` (config-change rejected, no-op accepted). |
| T-06-04 | Tampering (design invariant) | a streak/adherence counter or count-as-fraction milestone column added | mitigate | No such column exists; milestone STEPS come from the coach-authored `tracker_milestones` table (not `count(*)/5`); grep gates in must_haves.prohibitions; progress fill derived read-time via get_tracker_progress(). |
| T-06-05 | Tampering (data integrity) | two active assignments for one client (race) | mitigate | `tracker_one_active_assignment_per_client` partial unique index rejects the second insert. |
| T-06-06 | Information Disclosure | unassigned coach / cross-client read of entries or milestone steps | mitigate | RLS via private.is_coach_of + join-through-assignment; `checkTrackerUnassignedCoachDenied` / `checkTrackerCrossClientDenied` / `checkTrackerMilestoneStepsReadable` cross-client leg (zero rows, no error). |
| T-06-SC | Tampering | npm installs | accept | Zero new npm packages this phase (zod already installed, audited Phase 5); pg_jsonschema already enabled by 0008. No install task, so no legitimacy checkpoint required. |
</threat_model>

<verification>
- `pnpm db:reset && pnpm seed && pnpm seed && pnpm verify:rls` — all tracker + pre-existing assertions PASS, second seed re-run green.
- `pnpm --filter @fish/web test -- --run apps/web/lib/validation/tracker.test.ts` — green.
- No-streak / no-fraction schema scan: `grep -v '^--' supabase/migrations/0009_tracker.sql | grep -Eic 'streak|adherence|completion_rate|percent'` → 0; `grep -Ei 'milestones_reached|count\(\*\)\s*/\s*[0-9]' supabase/migrations/0009_tracker.sql` → nothing.
- Milestone-step source exists: `grep -c "create table public.tracker_milestones"` → 1; `grep -c "get_tracker_milestone_steps"` ≥ 2.
- zod boundary: `grep -rEn "from ['\"]zod['\"]" packages/core/src` → nothing; tracker.ts imports (not copies) onboarding schemas.
- `pnpm typecheck` green.
</verification>

<success_criteria>
- Migration 0009 is applied to the local stack with all six tables (incl. `tracker_milestones`), RLS, all three functions, the pg_jsonschema CHECK, no-op-aware freeze triggers, and both partial unique indexes.
- The coach-authored milestone steps read back from `tracker_milestones` via `get_tracker_milestone_steps()` for the client + assigned coach; no count-as-fraction step-state driver exists.
- The no-streak schema invariant, the RPC-only write boundary, and the no-authenticated-assignment-write boundary are all proven by live `verify:rls` assertions.
- Malformed tracker config is rejected by both zod and the DB CHECK; a used version's config is immutable but a no-op re-seed is accepted.
- Tracker*Row aliases exist and typecheck; the seed created client1's active assignment + milestone-step fixtures and is re-run-safe.
</success_criteria>

<output>
Create `.planning/phases/06-tracker-engine/06-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
