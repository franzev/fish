---
phase: 04-client-profiles
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - apps/web/lib/auth/server.ts
  - apps/web/app/(authenticated)/coach/clients/[id]/page.tsx
  - apps/web/app/(authenticated)/coach/clients/[id]/page.test.tsx
  - apps/web/components/coach/client-list.tsx
requirements: [PROF-06]
autonomous: true
must_haves:
  truths:
    - "A coach opens an assigned client's profile as a read-only detail view from the coach client list"
    - "The coach detail shows identity + goal/role-context + level (level as data, never a grade); a11y prefs and consent are hidden"
    - "An unassigned coach (or a guessed UUID) gets a calm not-found state with no cross-client leak"
    - "Coach client-list rows are links to /coach/clients/[id]"
    - "Both release gates are green: pnpm build AND pnpm verify:rls (plus lint + typecheck)"
  artifacts:
    - path: "apps/web/app/(authenticated)/coach/clients/[id]/page.tsx"
      provides: "coach read-only client detail (RLS-gated via is_coach_of, calm not-found on deny)"
      min_lines: 20
    - path: "apps/web/components/coach/client-list.tsx"
      provides: "client rows wrapped in next/link to /coach/clients/[id]"
      contains: "coach/clients"
  key_links:
    - from: "apps/web/app/(authenticated)/coach/clients/[id]/page.tsx"
      to: "client_profiles (RLS is_coach_of)"
      via: "getCoachClientDetailData -> clientProfiles.findByIdForCoach; zero rows -> calm not-found"
      pattern: "findByIdForCoach"
    - from: "apps/web/components/coach/client-list.tsx"
      to: "/coach/clients/[id]"
      via: "next/link wrapping each client row"
      pattern: "coach/clients/"
---

<objective>
Give the coach a read-only detail view of an assigned client, reached by making the (currently inert) coach client-list rows into links. The read is RLS-gated by `private.is_coach_of` (proven in Plan 01) — an unassigned coach or a guessed UUID yields zero rows, rendered as a calm not-found state with no enumeration side channel. Close the phase with both release gates green.

Purpose: Deliver PROF-06 and finalize XC-01 (verify:rls) + XC-03 (design line) for the whole phase.
Output: `/coach/clients/[id]` route, `getCoachClientDetailData`, linked client-list rows, and green `pnpm build` + `pnpm verify:rls` + `pnpm lint` + `pnpm typecheck`.
</objective>

<execution_context>
@/Users/franz/Library/Application Support/Claude/local-agent-mode-sessions/614fd504-8566-46b2-8256-66ded77bb604/43ed32e3-053c-413f-843e-d49e1f00f5e1/rpm/plugin_019FPGEfUV8XSdonegN1mWDs/workflows/execute-plan.md
@/Users/franz/Library/Application Support/Claude/local-agent-mode-sessions/614fd504-8566-46b2-8256-66ded77bb604/43ed32e3-053c-413f-843e-d49e1f00f5e1/rpm/plugin_019FPGEfUV8XSdonegN1mWDs/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-client-profiles/04-CONTEXT.md
@.planning/phases/04-client-profiles/04-RESEARCH.md
@.planning/phases/04-client-profiles/04-PATTERNS.md
@.claude/skills/sketch-findings-fish/SKILL.md
@.claude/skills/sketch-findings-fish/references/coach-experience.md

<read_first>
LOAD the `sketch-findings-fish` skill BEFORE building the coach UI (XC-03). Then read:
- apps/web/app/(authenticated)/coach/page.tsx — the coach wrong-door guard + render shape (copy for the [id] route; this is a DYNAMIC segment reading params.id)
- apps/web/app/(authenticated)/coach/page.test.tsx — co-located route-test convention
- apps/web/components/coach/client-list.tsx — currently renders inert `<div>` rows with a comment "nothing here is tappable yet"; this phase resolves it by wrapping rows in next/link (keep the exact Card + divide-y + p-4 row shape)
- apps/web/lib/auth/server.ts — getCoachHomeData / getProfileData (from Plan 02); add getCoachClientDetailData(clientId) using clientProfiles.findByIdForCoach (from Plan 02 Task 1); RLS scopes it, no app-code id filter
- apps/web/components/ui/alert/*.tsx — Alert tone="notice" for the calm not-found state (never error/red)
</read_first>

<interfaces>
<!-- From PATTERNS coach/clients/[id] section + RESEARCH Security Domain. -->

Coach detail read (D-10/D-11): show display name + goal/role-context + level ONLY. HIDE a11y prefs + consent (client's personal settings, not coach-relevant). Level rendered calmly as data (a quiet label), never a grade/score/percentage.

Deny behavior (no side channel): getCoachClientDetailData returns null when findByIdForCoach yields zero rows (RLS default-deny). The page renders the SAME calm not-found state (Alert tone="notice") for both "no such client" and "not your client" — never a distinguishable 403/error, so a coach cannot enumerate which UUIDs exist (RESEARCH Security Domain T-04-02).

Dynamic segment (Next.js 16 App Router): params is async — `const { id } = await params;` in the Server Component.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getCoachClientDetailData + /coach/clients/[id] read-only detail + calm deny state</name>
  <files>apps/web/lib/auth/server.ts, apps/web/app/(authenticated)/coach/clients/[id]/page.tsx, apps/web/app/(authenticated)/coach/clients/[id]/page.test.tsx</files>
  <read_first>coach/page.tsx (+ .test.tsx); server.ts getCoachHomeData; clientProfiles.findByIdForCoach (Plan 02 Task 1); Alert component; loaded sketch skill</read_first>
  <action>
    - server.ts: add `getCoachClientDetailData(clientId: string)` modeled on getCoachHomeData — resolve getCurrentProfile; if role !== "coach" the caller redirects; call `services.database.clientProfiles.findByIdForCoach(clientId)`; throw on ServiceResult failure; return a typed DTO of ONLY display name + goal + level (fetch the client's display_name from profiles by id — RLS on profiles must allow the coach to read the assigned client's display name; if it does not, read display_name via the same is_coach_of-gated path). A null repository result means "not assigned / not found" -> return null so the page shows the calm not-found state.
    - `/coach/clients/[id]/page.tsx` (Server Component): mirror coach/page.tsx guard (getUser() + role==="coach"; wrong-door redirect to /home if a client reaches it; redirect(authRedirects.signedOut) if no session). `const { id } = await params;` then `getCoachClientDetailData(id)`. If null -> render a calm not-found `<Alert tone="notice">` (e.g. "We couldn't find that client.") — NOT an error tone, NOT a thrown 403 (no enumeration side channel). If present -> render identity + goal/role-context + level (quiet data label). Do NOT render a11y prefs or consent (D-10). Read-only: no primary button, no edit affordance.
    - `page.test.tsx`: co-located test asserting the detail renders level as a plain label (not a percentage/grade) and that the null-data path renders the calm notice, not an error tone. Follow the coach/page.test.tsx mocking convention.
  </action>
  <verify>
    <automated>pnpm --filter @fish/web test -- --run "coach/clients" && pnpm --filter @fish/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `getCoachClientDetailData` exists in server.ts and calls `findByIdForCoach` (grep: `findByIdForCoach`), no app-code `coach_id =`/id-based WHERE filter substituting for RLS
    - `/coach/clients/[id]/page.tsx` awaits `params`, shows display name + goal + level only, hides prefs/consent, has no `variant="primary"`
    - the deny path renders `Alert tone="notice"` (calm), not an error tone or thrown 403
    - co-located test passes with `--run` (no watch)
  </acceptance_criteria>
  <done>Coach detail reads the assigned client read-only via RLS; unassigned/guessed UUID -> identical calm not-found; level shown as data, prefs/consent hidden.</done>
</task>

<task type="auto">
  <name>Task 2: Make coach client-list rows links + phase release gates (build, verify:rls, lint, typecheck)</name>
  <files>apps/web/components/coach/client-list.tsx</files>
  <read_first>apps/web/components/coach/client-list.tsx (inert div rows + its "not tappable yet" comment)</read_first>
  <action>
    - client-list.tsx: wrap each client row in a `next/link` `<Link href={`/coach/clients/${client.id}`}>`, preserving the EXACT Card + `divide-y divide-border p-0` + `<div className="... p-4">` row shape and the existing sort. Remove/replace the "nothing here is tappable yet" comment now that a destination exists. Keep >=56px effective row height; add visible focus (the global :focus-visible outline already applies to links). Do not add color to the row (monochrome structural UI).
    - Then run the phase release gates in order and confirm each is green:
      1. `pnpm lint`
      2. `pnpm typecheck`
      3. `pnpm build` (ROADMAP Phase 4 SC #5 gate)
      4. `pnpm verify:rls` (ROADMAP Phase 4 SC #5 gate — the six client_profiles assertions from Plan 01 plus all prior). If the local stack drifted, re-run `pnpm db:reset && pnpm seed` first.
    All four must pass before the phase closes.
  </action>
  <verify>
    <automated>grep -q 'coach/clients/' "apps/web/components/coach/client-list.tsx" && pnpm lint && pnpm typecheck && pnpm build && pnpm verify:rls</automated>
  </verify>
  <acceptance_criteria>
    - `client-list.tsx` wraps rows in a `next/link` to `/coach/clients/${client.id}`; the "not tappable yet" comment is gone
    - `pnpm lint` exits 0
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0 (SC #5)
    - `pnpm verify:rls` exits 0 with the six client_profiles PASS lines (SC #5)
  </acceptance_criteria>
  <done>Coach roster rows link to the client detail; all four release gates (lint, typecheck, build, verify:rls) are green — the phase is verifiable-complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coach browser -> /coach/clients/[id] | A coach can request any client UUID; only RLS (`is_coach_of`) decides what returns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | unassigned coach enumerates client UUIDs via /coach/clients/[id] | mitigate | `findByIdForCoach` is RLS-gated by `is_coach_of`; zero rows -> identical calm not-found for both "no client" and "not yours" (no side channel). Verified by `checkUnassignedCoachDenied()` in Plan 01 |
| T-04-09 | Information Disclosure | coach detail leaks the client's a11y prefs / consent | mitigate | The coach DTO returns only display name + goal + level; prefs/consent never selected into the coach path (D-10) |
| T-04-10 | Spoofing | client reaches the coach detail route | mitigate | Wrong-door guard: role must be "coach" or redirect to /home |
</threat_model>

<verification>
- All four gates green: `pnpm lint`, `pnpm typecheck`, `pnpm build`, `pnpm verify:rls`.
- Manual (XC-03, sketch skill loaded): coach detail is read-only (no primary button), level is a quiet data label (no grade/percentage), a11y prefs + consent are absent, the not-found state uses the calm notice tone. Client-list rows are keyboard-focusable links.
</verification>

<success_criteria>
- PROF-06: coach opens an assigned client's read-only detail from the roster; unassigned coach / guessed UUID gets a calm not-found, no leak.
- XC-01: `pnpm verify:rls` green (six client_profiles assertions).
- XC-03: coach detail holds the design line (read-only, level-as-data, monochrome, calm copy).
- Phase gate: `pnpm build` AND `pnpm verify:rls` (plus lint + typecheck) all green.
</success_criteria>

<output>
Create `.planning/phases/04-client-profiles/04-03-SUMMARY.md` when done — list the route (/coach/clients/[id]), `getCoachClientDetailData`, the linked client-list change, the fields shown (identity + goal + level) and hidden (prefs, consent), and the four green release gates.
</output>
