---
phase: 12-cross-platform-data-cache-and-recovery
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/core/src/shared-content/fixtures/shared-content-vectors.json
  - packages/core/src/shared-content/shared-content.test.ts
  - apps/android/feature/chat/src/test/kotlin/space/fishhub/android/feature/chat/sharedcontent/SharedContentParityTest.kt
  - apps/ios/FishKit/Sources/TestSupport/Fixtures/SharedContentVectors.swift
  - apps/ios/FishKit/Tests/ChatCoreTests/SharedContentContractTests.swift
autonomous: true
requirements:
  - PRIV-02
  - PRIV-03
  - PAGE-03
  - OFF-01
  - OFF-02
must_haves:
  truths:
    - "The portable test corpus defines failing executable evidence for memory-only delivery, bounded intent, truthful cache state, two-attempt recovery, and generation rejection before production implementation begins, per D-04, D-07 through D-19."
    - "Kotlin and Swift strict parity tests consume the same TypeScript-owned bytes and fail on every missing Phase 12 projection rather than carrying platform-local expectations."
    - "Every TypeScript, Kotlin, and Swift paging fixture preserves the exact 40+1 invariant: `p_limit = 40` returns at most indexes 0–40, indexes 0–39 are retained, `nextCursor` comes from index 39, and optional index 40 sets `hasMore` without becoming data."
  artifacts:
    - path: packages/core/src/shared-content/fixtures/shared-content-vectors.json
      provides: "Canonical Phase 12 cache, recovery, delivery, data-saving, and identity-generation cases."
      contains: "\"cacheHydration\""
    - path: packages/core/src/shared-content/shared-content.test.ts
      provides: "Failing Node assertions for validation rows 12-W0-01, 12-W0-03, 12-W0-04, and 12-W0-05."
      contains: "identityGeneration"
    - path: apps/android/feature/chat/src/test/kotlin/space/fishhub/android/feature/chat/sharedcontent/SharedContentParityTest.kt
      provides: "Strict Kotlin decoder and complete projection assertions for the extended corpus."
      contains: "ignoreUnknownKeys = false"
    - path: apps/ios/FishKit/Tests/ChatCoreTests/SharedContentContractTests.swift
      provides: "Strict Swift decoder and complete projection assertions for the byte-synchronized corpus."
      contains: "expectedCaseCount"
  key_links:
    - from: packages/core/src/shared-content/fixtures/shared-content-vectors.json
      to: packages/core/src/shared-content/shared-content.test.ts
      via: "exact group order, case count, and complete expected projections"
      pattern: "cacheHydration|deliveryPlanning|recovery|identityGeneration"
    - from: packages/core/src/shared-content/fixtures/shared-content-vectors.json
      to: apps/ios/FishKit/Sources/TestSupport/Fixtures/SharedContentVectors.swift
      via: "pnpm ios:chat-vectors byte synchronization"
      pattern: "metadata.*version"
---

<objective>
Create the executable Wave 0 portable and native parity tests for 12-W0-01, 12-W0-03, 12-W0-04, and 12-W0-05 before any Phase 12 production contract is implemented.

Purpose: Lock the cross-platform cache, recovery, delivery-intent, and identity-generation behavior in one RED corpus so implementation cannot silently redefine the requirements.
Output: Extended canonical vectors plus failing strict Node, Kotlin, and Swift assertions.
</objective>

<execution_context>
@/Users/franz/.codex/gsd-core/workflows/execute-plan.md
@/Users/franz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-cross-platform-data-cache-and-recovery/12-CONTEXT.md
@.planning/phases/12-cross-platform-data-cache-and-recovery/12-RESEARCH.md
@.planning/phases/12-cross-platform-data-cache-and-recovery/12-VALIDATION.md
@.planning/phases/12-cross-platform-data-cache-and-recovery/12-UI-SPEC.md
@.planning/phases/11-shared-content-contract-and-privacy-boundary/11-CONTEXT.md
</context>

## Artifacts this phase produces

This Wave 0 plan modifies only test assets. It extends the existing canonical JSON with `cacheHydration`, `cacheTruth`, `eviction`, `recovery`, `deliveryPlanning`, `dataSaving`, `urlNonPersistence`, and `identityGeneration` groups; adds their strict Node projections; and adds strict Kotlin/Swift decoders and assertions. Production reducers and adapters are intentionally absent, so the targeted suites must be RED for named missing Phase 12 behavior rather than syntax or fixture-shape errors.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the failing portable Phase 12 corpus</name>
  <files>packages/core/src/shared-content/fixtures/shared-content-vectors.json, packages/core/src/shared-content/shared-content.test.ts</files>
  <read_first>
@packages/core/src/shared-content/fixtures/shared-content-vectors.json
@packages/core/src/shared-content/shared-content.test.ts
@packages/core/src/shared-content/types.ts
@packages/core/src/shared-content/state.ts
@.planning/phases/12-cross-platform-data-cache-and-recovery/12-CONTEXT.md
@.planning/phases/12-cross-platform-data-cache-and-recovery/12-RESEARCH.md
@.planning/phases/12-cross-platform-data-cache-and-recovery/12-VALIDATION.md
  </read_first>
  <behavior>
    - "12-W0-01 rejects delivery URLs/tokens/capabilities in persisted snapshots and diagnostics."
    - "12-W0-03 covers 1/49/50/51 IDs, deduplication, visible/lookahead/selected intent, and data-saving visible-only behavior."
    - "12-W0-04 distinguishes wrong-owner, cached+stale+incomplete, offline/no-cache, and authoritative-empty."
    - "12-W0-05 covers trigger coalescing, attempts 0 and 1 only, connectivity cancellation, manual retry, and new-cycle reset."
    - "The carried-forward 40+1 paging cases allow at most 41 returned rows indexed 0–40, retain indexes 0–39, derive `nextCursor` from retained index 39, and use optional index 40 only to set `hasMore = true`; no case accesses index 41 or derives a cursor from index 40."
  </behavior>
  <action>
Increment the fixture version and expected count, retaining every Phase 11 group and unknown-key assertion. Add complete inputs and expected projections for all eight named Phase 12 groups, including a recognizable fake signed-token sentinel that may appear only in an input/live-memory expectation. Encode the exact numeric constants resolved in `12-RESEARCH.md` and the approved presentation keys from `12-UI-SPEC.md`.

Extend the Node test to assert exact ordered group names, total case count, explicit nulls, full object projections, and absence of forbidden durable fields. Preserve the paging fixture with this exact invariant: the RPC request uses `p_limit = 40`; a response contains no more than 41 rows indexed 0–40; indexes 0–39 are retained data; `nextCursor` is derived from the last retained row at index 39; optional index 40 is only the continuation sentinel that sets `hasMore = true`; index 41 is never accessed and index 40 never supplies a cursor. The tests must reference the intended Phase 12 exports so the suite is RED until Plan 12-05 implements them. Do not add temporary production shims, weaken existing Phase 11 assertions, or embed platform-specific results.
  </action>
  <verify>
    <automated>test -f packages/core/src/shared-content/fixtures/shared-content-vectors.json &amp;&amp; test -f packages/core/src/shared-content/shared-content.test.ts &amp;&amp; rg -n 'cacheHydration|deliveryPlanning|recovery|identityGeneration|nextCursor|hasMore' packages/core/src/shared-content/fixtures/shared-content-vectors.json packages/core/src/shared-content/shared-content.test.ts &amp;&amp; ! node --test packages/core/src/shared-content/shared-content.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "The fixture metadata lists every old group plus all eight Phase 12 groups in one exact order and the expected count equals the literal cases."
    - "The Node suite fails only because the named Phase 12 production exports/behaviors are not implemented; JSON parsing, fixture lookup, and existing Phase 11 assertions remain valid."
    - "The 51-ID, stale+incomplete, authoritative-empty, attempts-0/1, and signed-sentinel cases are explicit rather than inferred from shallow counts."
    - "The paging fixture proves at most indexes 0–40 are returned, only indexes 0–39 are retained, index 39 supplies `nextCursor`, and optional index 40 only sets `hasMore`; index 41 is never accessed."
  </acceptance_criteria>
  <done>The canonical RED corpus completely represents validation rows 12-W0-01, 12-W0-03, 12-W0-04, and 12-W0-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make Kotlin and Swift fail against the same corpus</name>
  <files>apps/android/feature/chat/src/test/kotlin/space/fishhub/android/feature/chat/sharedcontent/SharedContentParityTest.kt, apps/ios/FishKit/Sources/TestSupport/Fixtures/SharedContentVectors.swift, apps/ios/FishKit/Tests/ChatCoreTests/SharedContentContractTests.swift</files>
  <read_first>
@packages/core/src/shared-content/fixtures/shared-content-vectors.json
@apps/android/feature/chat/src/test/kotlin/space/fishhub/android/feature/chat/sharedcontent/SharedContentParityTest.kt
@apps/ios/FishKit/Sources/TestSupport/Fixtures/SharedContentVectors.swift
@apps/ios/FishKit/Tests/ChatCoreTests/SharedContentContractTests.swift
@apps/android/feature/chat/src/main/kotlin/space/fishhub/android/feature/chat/sharedcontent/state/SharedContentState.kt
@apps/ios/FishKit/Sources/ChatCore/SharedContent/SharedContentState.swift
@.planning/phases/12-cross-platform-data-cache-and-recovery/12-PATTERNS.md
  </read_first>
  <behavior>
    - "Both native decoders reject unknown and missing keys and compare complete Phase 12 projections."
    - "Both native suites assert the same fixture version, ordered groups, and exact case count as Node."
    - "The iOS fixture copy remains byte-identical to the TypeScript-owned JSON."
    - "Both native suites replay the exact paging projection: retained indexes 0–39, `nextCursor` from index 39, optional continuation sentinel at index 40, and no access to index 41."
  </behavior>
  <action>
Extend the Kotlin serializable test DTOs and Swift fixture decoder for every new group and explicit nullable field. Add complete native assertions with vector group/case context and no platform-local expected constants, including `p_limit = 40`, at most 41 returned rows indexed 0–40, retained indexes 0–39, `nextCursor` from retained index 39, optional index 40 only setting `hasMore = true`, no index 41 access, and no cursor derived from index 40. Keep Kotlin `ignoreUnknownKeys = false`; keep Swift exhaustive decoding; synchronize iOS bytes only through `pnpm ios:chat-vectors`.

The native suites must become RED specifically because the corresponding production Kotlin/Swift types and functions are absent, while fixture synchronization and strict decoding remain valid. Do not modify either production reducer in Wave 0.
  </action>
  <verify>
    <automated>pnpm ios:chat-vectors &amp;&amp; pnpm ios:chat-vectors:check &amp;&amp; rg -n 'cacheHydration|deliveryPlanning|recovery|identityGeneration' apps/android/feature/chat/src/test/kotlin/space/fishhub/android/feature/chat/sharedcontent/SharedContentParityTest.kt apps/ios/FishKit/Sources/TestSupport/Fixtures/SharedContentVectors.swift apps/ios/FishKit/Tests/ChatCoreTests/SharedContentContractTests.swift &amp;&amp; ! scripts/android-gradle.sh :feature:chat:testDebugUnitTest --tests 'space.fishhub.android.feature.chat.sharedcontent.SharedContentParityTest' &amp;&amp; ! pnpm ios:test -- --only-testing:ChatCoreTests/SharedContentContractTests</automated>
  </verify>
  <acceptance_criteria>
    - "Kotlin and Swift decode every Phase 12 fixture field strictly and report failures with the canonical group and case name."
    - "Both native suites are RED only on missing production Phase 12 semantics, not on byte drift, malformed fixtures, or missing test resources."
    - "No Android or iOS test authors a second expected result outside the canonical JSON."
    - "Kotlin and Swift assert the same retained indexes, cursor source, sentinel index, and `hasMore` outcome as Node."
  </acceptance_criteria>
  <done>All three languages share one executable RED contract before production implementation starts.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Untrusted input crossing it | Required control |
|---|---|---|
| Canonical JSON → native decoders | Malformed, missing, or unknown fields | Strict decoding, exact group/count checks, complete projection comparisons |
| Fake signed token → test evidence | Recognizable secret-like sentinel | Input/live-memory only; later persistent and diagnostic scans require zero occurrences |
| Platform test → product contract | Divergent local expectation | TypeScript-owned expected projections only |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation and verification |
|---|---|---|---|---|
| T-12-02 | Information disclosure | URL non-persistence corpus | mitigate | Sentinel field/diagnostic absence assertions in all three suites. |
| T-12-03 | Spoofing / elevation | Owner/generation fixtures | mitigate | Wrong-owner and stale-generation cases fail closed. |
| T-12-07 | Tampering | Cross-platform projections | mitigate | Strict version/group/count and complete values prevent silent drift. |
| T-12-08 | Denial of service | Delivery/recovery cases | mitigate | Explicit 50-ID cap and attempts 0/1 only. |
| T-12-SC | Tampering | Package supply chain | accept | No package install occurs in this plan. |
</threat_model>

<verification>
- Confirm all five Wave 0 files exist and only test/fixture assets changed.
- Confirm the three targeted suites fail for missing Phase 12 production behavior while existing Phase 11 cases still parse.
- Confirm the iOS fixture copy is byte-identical to the canonical JSON.
</verification>

<success_criteria>
- Validation rows 12-W0-01, 12-W0-03, 12-W0-04, and 12-W0-05 have concrete RED tests before implementation.
- The corpus covers D-04, D-07 through D-19 without adding a route, component, setting, or provider authority.
- Node, Kotlin, and Swift cannot diverge on expected Phase 12 behavior.
</success_criteria>

<output>
Create `.planning/phases/12-cross-platform-data-cache-and-recovery/12-01-SUMMARY.md` when done.
</output>
