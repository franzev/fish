---
phase: 10-chat-message-loading-optimization
plan: 04
type: execute
wave: 3
depends_on: ["10-03"]
files_modified:
  - apps/web/vitest.setup.ts
  - apps/web/tests/intersection-observer.ts
  - apps/web/app/(authenticated)/chat/hooks/use-stick-to-bottom.ts
  - apps/web/app/(authenticated)/chat/hooks/use-stick-to-bottom.test.ts
  - apps/web/app/(authenticated)/chat/hooks/use-load-older-messages.ts
  - apps/web/app/(authenticated)/chat/chat-client.tsx
  - apps/web/app/(authenticated)/chat/chat-client.test.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [CLOAD-03, CLOAD-04]
must_haves:
  truths:
    - "An IntersectionObserver sentinel above the oldest message auto-loads older history when scrolled near the top, and a quiet focusable 'Load earlier' button loads it on demand — neither is a second primary action"
    - "Loading older messages preserves the exact reading position via a manual scrollHeight-diff restore (instant, no animation); overflow-anchor:none prevents the browser from fighting it"
    - "The stick-to-bottom hook keys new-message detection off the newest message's identity, so a prepended older page never yanks the viewport down or raises a spurious 'New messages' pill"
    - "Loading and offline/reconnect states are calm: fixed-height skeleton rows reserve their space (zero layout shift), notice tone only, never red, reduced-motion respected"
    - "IntersectionObserver is mocked in the test host with a capture-and-trigger helper so sentinel behavior is unit-testable"
  artifacts:
    - path: "apps/web/app/(authenticated)/chat/hooks/use-load-older-messages.ts"
      provides: "IntersectionObserver sentinel hook + scroll-anchor restore on prepend"
      contains: "IntersectionObserver"
    - path: "apps/web/app/(authenticated)/chat/hooks/use-stick-to-bottom.ts"
      provides: "newest-identity new-message detection (prepend-safe)"
    - path: "apps/web/app/globals.css"
      provides: "skeleton-pulse utility + .chat-log-viewport overflow-anchor:none"
      contains: "overflow-anchor"
    - path: "apps/web/vitest.setup.ts"
      provides: "IntersectionObserver capture-and-trigger mock"
      contains: "IntersectionObserver"
  key_links:
    - from: "apps/web/app/(authenticated)/chat/chat-client.tsx"
      to: "use-load-older-messages sentinel + loadOlderMessages (Plan 03)"
      via: "sentinel ref observed above the oldest message; button onClick calls loadOlderMessages"
      pattern: "loadOlderMessages"
    - from: "apps/web/app/(authenticated)/chat/hooks/use-load-older-messages.ts"
      to: "the chat log viewport scrollHeight/scrollTop"
      via: "capture before prepend, restore after paint via requestAnimationFrame"
      pattern: "scrollHeight"
---

<objective>
Render the pagination behavior from Plan 03 as a calm, jank-free chat surface: an
IntersectionObserver sentinel plus a quiet "Load earlier" affordance (both, per CONTEXT), manual
scroll-position preservation on prepend, the required fix to the stick-to-bottom hook so a prepend
is no longer misread as a new message, monochrome skeleton/offline states that reserve their space,
and the small CSS additions (skeleton pulse + `overflow-anchor: none`). This plan also lands the
test-infra gap (IntersectionObserver mock) the sentinel needs.

Purpose: Deliver CLOAD-03's user-facing load-earlier + infinite scroll and CLOAD-04's reading-
position preservation without adding a choice, a color, or a layout shift — holding the FISH design
line (one primary action = send; calm `--notice` states; `@theme` tokens only).
Output: A sentinel hook with scroll-anchor restore, the stick-to-bottom fix + its first isolated
unit test, the wired component with skeleton/notice states, the CSS utilities, and the IO mock.
</objective>

<execution_context>
@/Users/franz/Library/Application Support/Claude/local-agent-mode-sessions/614fd504-8566-46b2-8256-66ded77bb604/43ed32e3-053c-413f-843e-d49e1f00f5e1/rpm/plugin_019FPGEfUV8XSdonegN1mWDs/workflows/execute-plan.md
@/Users/franz/Library/Application Support/Claude/local-agent-mode-sessions/614fd504-8566-46b2-8256-66ded77bb604/43ed32e3-053c-413f-843e-d49e1f00f5e1/rpm/plugin_019FPGEfUV8XSdonegN1mWDs/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-chat-message-loading-optimization/10-CONTEXT.md
@.planning/phases/10-chat-message-loading-optimization/10-RESEARCH.md
@.planning/phases/10-chat-message-loading-optimization/10-PATTERNS.md
@.claude/skills/sketch-findings-fish/references/states.md
@.claude/skills/sketch-findings-fish/references/chat.md
@docs/ui-ux-agent-guidelines.md

<interfaces>
<!-- From Plan 03 — the hook/store surface this component consumes: -->

```
// use-chat-messages (Plan 03) now returns:
{ messages, setMessages, refreshMessages, applyGapBackfill,
  loadOlderMessages: () => void, hasMoreOlder: boolean, isLoadingOlder: boolean }
```

<!-- Existing surfaces to reuse (do not reinvent): -->
- useStickToBottom returns `{ viewportRef, showNewMessages, scrollToBottom }` (use-stick-to-bottom.ts).
- The message list renders inside `<ScrollArea viewportRef={viewportRef} viewportClassName="px-md py-md">` with a `role="log"` container; the `<ol>` of bubbles sits inside it (chat-client.tsx:226-265).
- Existing calm patterns in chat-client.tsx: the floating pill (511-520, `absolute inset-x-0 bottom-sm ... rounded-pill border border-border bg-surface ... shadow-popover`) and the notice banner (523-527, `{notice && <Alert tone="notice" className="mx-md mb-xs">...`).
- Button (apps/web/components/ui/button/button.tsx) has a built-in `loading` prop that overlays a spinner WITHOUT resizing (104-113); variants `secondary`/`ghost` — never `primary`.
- globals.css animation pattern (290-306): comment → `@keyframes` → `@utility animate-*`; the global reduced-motion clamp (265-271) already sets `animation-iteration-count: 1 !important` (covers a pulse) — no per-animation guard needed.
- IntersectionObserver mock precedent: the ResizeObserver no-op stub in vitest.setup.ts (6-12), but the IO mock must be capture-and-trigger (RESEARCH Code Example D) so tests can fire `isIntersecting: true`.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land the IntersectionObserver test mock and fix the stick-to-bottom prepend-vs-append misfire (with its first isolated unit test)</name>
  <files>apps/web/vitest.setup.ts, apps/web/tests/intersection-observer.ts, apps/web/app/(authenticated)/chat/hooks/use-stick-to-bottom.ts, apps/web/app/(authenticated)/chat/hooks/use-stick-to-bottom.test.ts</files>
  <read_first>
    - apps/web/vitest.setup.ts (whole file 1-40; ResizeObserver stub 6-12)
    - apps/web/app/(authenticated)/chat/hooks/use-stick-to-bottom.ts (whole file 1-110; the length-comparison new-message effect 93-107; the ResizeObserver re-pin effect 75-89)
    - .planning/phases/10-chat-message-loading-optimization/10-RESEARCH.md (Pitfall #1 length heuristic; Pitfall #10 + Code Example D IO mock)
    - apps/web/tests/chat-state-fixtures.test.ts (nearest vitest describe/it conventions for a new test file in apps/web/tests)
  </read_first>
  <behavior>
    - Appending a new message whose identity differs from the previous newest still follows own-sends / near-bottom readers (scrollToBottom) and raises the pill for a far-scrolled reader — unchanged happy path.
    - Prepending an older page (newest message identity unchanged, only the head grows) does NOT call scrollToBottom and does NOT set showNewMessages — the hook is inert to prepends.
    - triggerIntersection(target, true) invokes the observed callback with an entry whose isIntersecting is true, so a sentinel test can simulate scroll-into-view.
  </behavior>
  <action>
    In vitest.setup.ts add an IntersectionObserver stub next to the ResizeObserver one, but capture-and-trigger (RESEARCH Code Example D): a class storing each `observe(target)`'s callback in a module `Map<Element, cb>`, with no-op `unobserve`/`disconnect`/`takeRecords`. Put the exported `triggerIntersection(target, isIntersecting)` helper in a new `apps/web/tests/intersection-observer.ts` (not in setup) so component/hook tests can import it; the setup file installs the global, the helper drives it. In use-stick-to-bottom.ts, replace the `previousCountRef` length comparison (93-107) with newest-message identity tracking: keep a `previousLastIdRef` of the last message's `id` (fall back to `clientRequestId`); in the effect, read the current newest id, and only run the follow/pill logic when the newest id CHANGED and is newer than before (a grow at the head — same newest id — is ignored). Preserve the own-send (`lastMessage.senderId === currentUserId`) and `isNearBottomRef` behavior and the mount `useLayoutEffect` jump-to-newest. Create use-stick-to-bottom.test.ts using `@testing-library/react` `renderHook` against a minimal viewport ref stub: assert (a) appending a new newest message triggers scrollToBottom when near bottom, (b) prepending (same newest id, larger array) does NOT call scrollToBottom and does NOT set showNewMessages, (c) a far-scrolled reader receiving a new newest message sees showNewMessages true.
  </action>
  <verify>
    <automated>pnpm --filter @fish/web exec vitest run "app/(authenticated)/chat/hooks/use-stick-to-bottom.test.ts" && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "IntersectionObserver" apps/web/vitest.setup.ts` and `grep -q "triggerIntersection" apps/web/tests/intersection-observer.ts`.
    - use-stick-to-bottom.ts no longer compares `messages.length` for new-message detection (`grep -c "previousCountRef" apps/web/app/(authenticated)/chat/hooks/use-stick-to-bottom.ts` is 0) and instead tracks the newest message id.
    - use-stick-to-bottom.test.ts exists and its prepend case asserts scrollToBottom was NOT called and showNewMessages stayed false.
    - The append/near-bottom and append/far-scrolled cases pass.
    - `pnpm typecheck` passes.
  </acceptance_criteria>
  <done>Prepends are inert to stick-to-bottom; the test host can simulate the sentinel; CLOAD-04's core hook bug is fixed and covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the sentinel hook with IntersectionObserver auto-load and manual scroll-anchor restore on prepend</name>
  <files>apps/web/app/(authenticated)/chat/hooks/use-load-older-messages.ts</files>
  <read_first>
    - apps/web/app/(authenticated)/chat/hooks/use-chat-realtime.ts (subscription useEffect + cleanup shape 43-61, 92-125 — structural analog for observer lifecycle)
    - apps/web/app/(authenticated)/chat/hooks/use-stick-to-bottom.ts (ResizeObserver effect 75-89 — closest "construct Observer, observe(node), return () => disconnect()" analog)
    - apps/web/tests/intersection-observer.ts (the triggerIntersection helper from Task 1)
    - .planning/phases/10-chat-message-loading-optimization/10-RESEARCH.md (Pattern 3 scrollHeight-diff restore; Pattern 4 IO sentinel + accessibility notes)
  </read_first>
  <behavior>
    - When the sentinel enters the viewport (or approaches within ~200px) AND hasMoreOlder is true AND no load is in flight, onLoadOlder fires exactly once per intersection.
    - The observer is disconnected on unmount and re-created when hasMoreOlder/isLoadingOlder change; it never fires when hasMoreOlder is false.
    - Before triggering a load the hook captures viewport.scrollHeight and scrollTop; after the prepend paints (requestAnimationFrame following the load), it sets scrollTop = newScrollHeight - previousScrollHeight + previousScrollTop, restoring the reading position instantly (no animated scroll), and does not move focus.
  </behavior>
  <action>
    Create `use-load-older-messages.ts` exporting `useLoadOlderMessages({ viewportRef, sentinelRef, hasMoreOlder, isLoadingOlder, onLoadOlder })`. Implement an `IntersectionObserver` effect modeled on the ResizeObserver effect in use-stick-to-bottom.ts (75-89): guard `if (!node || !hasMoreOlder || isLoadingOlder) return;`, construct `new IntersectionObserver(entries => { if (entries[0]?.isIntersecting) triggerLoad(); }, { root: null, rootMargin: "200px 0px 0px 0px" })`, `observe(node)`, and `return () => observer.disconnect()`; deps include hasMoreOlder, isLoadingOlder, onLoadOlder. `triggerLoad` wraps `onLoadOlder` in the scroll-anchor restore (RESEARCH Pattern 3): capture `previousScrollHeight`/`previousScrollTop` from `viewportRef.current` before calling `onLoadOlder`, then in a `requestAnimationFrame` after the returned promise resolves set `viewport.scrollTop = viewport.scrollHeight - previousScrollHeight + previousScrollTop`. Because `onLoadOlder` (Plan 03's `loadOlderMessages`) is synchronous-dispatch, also expose a companion `restoreAfterPrepend()` the component can call, or accept an async `onLoadOlder` and await it — pick the shape that lets the rAF run after React paints the prepended rows (document the choice in a WHY comment). Do not move focus on load (W3C feed guidance + CONTEXT no-jump). No animated scrolling anywhere in this hook.
  </action>
  <verify>
    <automated>pnpm --filter @fish/web exec vitest run "app/(authenticated)/chat" && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - use-load-older-messages.ts constructs an `IntersectionObserver` with `rootMargin` and disconnects on cleanup; it early-returns when `!hasMoreOlder || isLoadingOlder`.
    - The hook captures scrollHeight/scrollTop before the load and restores `scrollTop = newScrollHeight - previousScrollHeight + previousScrollTop` inside a `requestAnimationFrame` (grep shows `scrollHeight` and `requestAnimationFrame`).
    - No `behavior: "smooth"` or animated scroll appears in this hook (restore is instant).
    - `pnpm typecheck` passes; the chat test suite stays green.
  </acceptance_criteria>
  <done>Scrolling near the top auto-loads older history and the reading position stays pinned (CLOAD-03 sentinel + CLOAD-04 anchor).</done>
</task>

<task type="auto">
  <name>Task 3: Wire the sentinel + quiet "Load earlier" affordance + calm skeleton/reconnect states into the component, add the CSS utilities, and extend the component test</name>
  <files>apps/web/app/(authenticated)/chat/chat-client.tsx, apps/web/app/globals.css, apps/web/app/(authenticated)/chat/chat-client.test.tsx</files>
  <read_first>
    - apps/web/app/(authenticated)/chat/chat-client.tsx (ScrollArea/message-list block 226-265; the floating pill 511-520; the notice banner 523-527; how refreshConversationAction is threaded to useChatMessages / useChatRealtime today)
    - apps/web/app/globals.css (animation pattern 290-306; reduced-motion clamp 258-271; fade-in 274-288)
    - apps/web/components/ui/button/button.tsx (loading prop 104-113; variants 16-40)
    - apps/web/components/ui/alert/alert.tsx (tone system — `notice` is the only monochrome tier)
    - apps/web/app/(authenticated)/chat/chat-client.test.tsx (existing describe blocks e.g. "displays new messages received through realtime")
    - apps/web/tests/intersection-observer.ts (triggerIntersection for the sentinel test)
    - .claude/skills/sketch-findings-fish/references/states.md (loading = soft skeleton, offline = notice-tone banner, reconnecting = quiet muted line; never red, never spinner-on-blank)
  </read_first>
  <action>
    In chat-client.tsx: consume `loadOlderMessages`, `hasMoreOlder`, `isLoadingOlder` from useChatMessages and wire `useLoadOlderMessages({ viewportRef, sentinelRef, hasMoreOlder, isLoadingOlder, onLoadOlder: loadOlderMessages })` (create a `sentinelRef`). Above the `<ol>` of bubbles, inside the `role="log"` container (226-265), render, only when `hasMoreOlder`: a near-zero-height sentinel `<div ref={sentinelRef} aria-hidden="true">` AND a focusable quiet affordance — a `Button variant="ghost"` (or `secondary`) labelled "Load earlier messages" with the built-in `loading={isLoadingOlder}` prop (never `variant="primary"`; send stays the only primary action). While `isLoadingOlder`, render 2–3 fixed-height skeleton rows (using the new `animate-skeleton-pulse` utility) sized like real message bubbles so appearing/disappearing loaders never shift layout (zero CLS); when there are no older messages render nothing (no dead-end). Add `className="chat-log-viewport"` to the ScrollArea viewport (via `viewportClassName`) so the CSS override applies. Add a calm reconnect/offline state: when realtime status is `disconnected`/`connecting`, show a quiet muted "Reconnecting…" pill (reuse the existing floating-pill positioning idiom, `--notice`/muted tone, never red) and/or an `Alert tone="notice"` for a persistent offline state matching states.md copy ("You're offline. Messages will send when you're back."); de-emphasize only the composer, keep the transcript at full opacity. Thread the new `loadOlderMessagesAction` and `backfillMessagesAction` (Plan 02 exports) into `useChatMessages` exactly the way `refreshConversationAction` is threaded today, and pass the bounded `applyGapBackfill` into `useChatRealtime` per Plan 03's changed option.

    In globals.css: add a `@keyframes skeleton-pulse` (opacity oscillation only — never a shimmer sweep, per states.md calm rule) and a `@utility animate-skeleton-pulse`, following the message-in comment→keyframes→utility shape; rely on the existing global reduced-motion clamp (no per-animation guard). Add a `.chat-log-viewport { overflow-anchor: none; }` rule (RESEARCH Pattern 3 companion) so Chromium/Firefox native scroll anchoring does not fight the manual restore. Use only `@theme` tokens/utilities — no raw hex, no one-off spacing.

    In chat-client.test.tsx: add cases asserting (a) the "Load earlier messages" button renders when hasMoreOlder is true and is absent when false; (b) firing the sentinel via `triggerIntersection` calls the injected loadOlder path (older messages appear, no duplicate); (c) skeleton rows render while isLoadingOlder and disappear after; (d) the reconnect/offline state uses `Alert tone="notice"` (or the muted pill) and never renders an error/red tone. Do not use Claude Preview MCP or a dev server — assert via vitest + Testing Library only.
  </action>
  <verify>
    <automated>pnpm --filter @fish/web exec vitest run "app/(authenticated)/chat/chat-client.test.tsx" && pnpm build && pnpm lint</automated>
    <human-check>After `pnpm seed`, open the general channel and scroll up through ≥3 older pages: the message under the eye must not move (instant restore, no jump/jank), skeleton rows must not shift layout, and — with the network toggled offline→online — the reconnect/offline state must read calm (notice/muted tone only, never red, non-scolding copy) with the reading position preserved. (VALIDATION.md Manual-Only rows; perceptual, not automatable.)</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -q "overflow-anchor" apps/web/app/globals.css` and a `.chat-log-viewport` rule exists; `grep -q "animate-skeleton-pulse" apps/web/app/globals.css` and its `@keyframes skeleton-pulse` is opacity-only.
    - chat-client.tsx renders a `Button` labelled "Load earlier" that is variant `ghost`/`secondary` and uses `loading={isLoadingOlder}`; there is still exactly one `variant="primary"` on the screen (the send button) — `grep -c 'variant="primary"' apps/web/app/(authenticated)/chat/chat-client.tsx` is 1.
    - A `sentinelRef` element renders above the `<ol>` only when `hasMoreOlder`; the ScrollArea viewport carries the `chat-log-viewport` class.
    - The offline/reconnect state uses `Alert tone="notice"` or a muted pill; `grep -c 'tone="error"\|bg-error\|text-error\|red' apps/web/app/(authenticated)/chat/chat-client.tsx` is 0 for any new client-facing state.
    - chat-client.test.tsx new cases pass: button visibility by hasMoreOlder, sentinel-triggered load, skeleton presence while loading, notice-tone reconnect state.
    - `pnpm build` and `pnpm lint` pass.
  </acceptance_criteria>
  <done>The chat opens fast, loads earlier via sentinel + quiet button with the reading position pinned, and shows calm monochrome loading/offline states — one primary action, zero layout shift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendered message body → DOM | Message bodies (already sanitized by the existing MessageBody renderer) are displayed; this plan adds no new rendering of untrusted content. |
| Client scroll/observer state → DOM measurement | Sentinel/scroll-anchor logic reads/writes local scroll geometry only; no network trust boundary is crossed in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Information Disclosure | Skeleton/offline/reconnect copy | mitigate | Copy is generic and non-scolding (states.md), reveals no account/other-user data; notice tone only, never red — matches XC-03 and the design line. |
| T-10-10 | Tampering (XSS) | Message body rendering in new skeleton/affordance markup | accept | New markup renders only static labels/tokens and the existing MessageBody component (already handles the markdown-subset safely); no new `dangerouslySetInnerHTML` or raw HTML path is introduced. |
| T-10-SC | Tampering | npm/pnpm installs | accept | Zero new packages installed this plan (virtualization deferred; native IntersectionObserver + CSS only). |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (new symbols created by this plan)

- Test infra: `apps/web/vitest.setup.ts` IntersectionObserver capture-and-trigger stub; new file `apps/web/tests/intersection-observer.ts` exporting `triggerIntersection`.
- Hook: `apps/web/app/(authenticated)/chat/hooks/use-load-older-messages.ts` (`useLoadOlderMessages`) — sentinel + scroll-anchor restore.
- Hook: revised `use-stick-to-bottom.ts` newest-identity detection (`previousLastIdRef`); new file `use-stick-to-bottom.test.ts`.
- Component: `chat-client.tsx` sentinel element, `sentinelRef`, "Load earlier" ghost Button, skeleton rows, reconnect/offline notice, `chat-log-viewport` viewport class, new action props threaded.
- CSS: `@keyframes skeleton-pulse` + `@utility animate-skeleton-pulse`; `.chat-log-viewport { overflow-anchor: none }` in globals.css.
- Tests: new cases in `chat-client.test.tsx`.

Deferred with documented threshold (not built this phase): full virtualization (`react-virtuoso`) and `content-visibility: auto` — bounded window + pagination keep DOM modest; revisit if a loaded conversation regularly holds several hundred+ messages and profiling shows jank (RESEARCH Standard Stack / Pitfall #8). Prefetch-ahead of the next older page is Claude's-discretion and is NOT shipped this phase.
</artifacts_produced>

<verification>
- `pnpm --filter @fish/web exec vitest run "app/(authenticated)/chat"` passes (hooks + component).
- `pnpm build`, `pnpm lint`, `pnpm typecheck` pass.
- The two Manual-Only VALIDATION rows (reading-position smoothness, calm state tone) are covered by the Task 3 `<human-check>` and hold at the phase gate.
</verification>

<success_criteria>
- CLOAD-03: sentinel auto-load + explicit quiet "Load earlier" button, both present; one primary action preserved.
- CLOAD-04: reading position preserved on prepend (manual restore + overflow-anchor:none), stick-to-bottom no longer misfires, zero layout shift from reserved-space loaders.
- Calm monochrome loading/offline/reconnect states per states.md; reduced-motion respected; `@theme` tokens only.
</success_criteria>

<output>
Create `.planning/phases/10-chat-message-loading-optimization/10-04-SUMMARY.md` when done.
</output>
