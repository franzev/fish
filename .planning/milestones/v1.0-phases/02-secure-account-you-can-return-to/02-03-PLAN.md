---
phase: 02-secure-account-you-can-return-to
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - scripts/seed.ts
  - scripts/verify-rls.ts
  - package.json
  - docs/deploy-checklist.md
autonomous: false
requirements: [DB-02, DB-03, DB-04]
user_setup: []

must_haves:
  truths:
    - "Running the seed script creates one coach account and ~3 pre-verified client accounts, all assigned to that coach, with fixed documented dev credentials (D-10)"
    - "The seed is idempotent — running it twice does not error and does not duplicate accounts or assignments — and its existing-user lookup is pagination-safe, not reliant on the first page of listUsers()"
    - "Seed accounts pass through the real auth machinery so the DB-01 handle_new_user trigger fires (profiles rows exist for every seeded user) (D-11)"
    - "A scripted RLS check logs in each seeded user with the anon key and asserts the exact rows each can and cannot see — repeatable, not a service-role Studio walk that would silently bypass RLS"
    - "A documented deploy-time checklist exists for the hosted Supabase setup deferred this phase (D-14)"
  artifacts:
    - path: "scripts/seed.ts"
      provides: "TypeScript admin seed using the service-role key + admin.createUser, pagination-safe idempotent lookup"
      contains: "admin.createUser"
    - path: "scripts/verify-rls.ts"
      provides: "anon-session RLS boundary check for the seeded accounts (DB-03/DB-04)"
      contains: "signInWithPassword"
    - path: "package.json"
      provides: "pnpm seed / verify:rls / supabase workflow scripts"
      contains: "seed"
    - path: "docs/deploy-checklist.md"
      provides: "D-14 hosted setup checklist"
      contains: "Site URL"
  key_links:
    - from: "scripts/seed.ts"
      to: "supabase.auth.admin.createUser"
      via: "service-role client, email_confirm: true"
      pattern: "admin\\.createUser"
    - from: "scripts/seed.ts"
      to: "public.coach_clients"
      via: "service-role insert (bypasses RLS), idempotent upsert"
      pattern: "coach_clients"
    - from: "scripts/verify-rls.ts"
      to: "supabase.auth.signInWithPassword"
      via: "anon-key client per seeded user, assert visible rows"
      pattern: "signInWithPassword"
---

<objective>
Deliver DB-02's seed half: an idempotent TypeScript admin script that creates one coach account plus ~3 pre-verified client accounts, all assigned to that coach in `coach_clients`, using the service-role key and `supabase.auth.admin.createUser` (D-10, D-11) — NOT raw inserts into `auth.users`. Wire it as a pnpm script alongside start/reset helpers. Add a SCRIPTED RLS/escalation check (`scripts/verify-rls.ts`) that logs in each seeded account with the anon key and asserts the exact rows it can and cannot read (review MEDIUM — service-role Studio walks silently bypass RLS; anon sessions are the real boundary). Write the deferred hosted-setup deploy checklist (D-14).

Purpose: Phase 3's coach home needs real rows on day one, and any seeded account must be loginnable to test the RLS boundaries. Seeded users must pass through GoTrue so the DB-01 trigger fires exactly as in production.

Output: `scripts/seed.ts`, `scripts/verify-rls.ts`, pnpm workflow scripts, `docs/deploy-checklist.md`, and verified seeded RLS/escalation boundaries.

Review-driven changes in this revision (from 02-REVIEWS.md):
- MEDIUM: RLS verification is now a repeatable script (`scripts/verify-rls.ts`) using anon-key sessions per seeded user — the manual browser walk stays as a human sanity pass, but the DB boundary is asserted in code, not via a service-role Studio session that bypasses RLS.
- MEDIUM: the seed's idempotent existing-user lookup is pagination-safe — it pages through `admin.listUsers({ page, perPage })` until the target email is found or pages are exhausted, rather than assuming the account is on page 1.
</objective>

<execution_context>
@/Users/franz/Library/Application Support/Claude/local-agent-mode-sessions/614fd504-8566-46b2-8256-66ded77bb604/43ed32e3-053c-413f-843e-d49e1f00f5e1/rpm/plugin_019FPGEfUV8XSdonegN1mWDs/workflows/execute-plan.md
@/Users/franz/Library/Application Support/Claude/local-agent-mode-sessions/614fd504-8566-46b2-8256-66ded77bb604/43ed32e3-053c-413f-843e-d49e1f00f5e1/rpm/plugin_019FPGEfUV8XSdonegN1mWDs/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-secure-account-you-can-return-to/02-CONTEXT.md
@.planning/phases/02-secure-account-you-can-return-to/02-RESEARCH.md
@.planning/phases/02-secure-account-you-can-return-to/02-PATTERNS.md

<interfaces>
<!-- Seed skeleton (RESEARCH.md Code Examples, D-10/D-11). Extract identifiers, do not invent a different shape. -->

Service-role client construction:
```
createClient(NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY,
  { auth: { autoRefreshToken: false, persistSession: false } })
```
Admin create (pre-verified): `supabase.auth.admin.createUser({ email, password, email_confirm: true, user_metadata: { display_name } })`.
Idempotency (pagination-safe): on `error.message.includes('already been registered')`, look up the existing user id by paging `admin.listUsers({ page, perPage: 1000 })` and scanning each page's `data.users` for the matching email until found or the page is empty — never assume page 1. Return the found id instead of throwing.
The DB-01 trigger populates `public.profiles` on createUser; the seed then promotes the coach via a service-role `update public.profiles set role='coach'` (service_role bypasses the escalation guard by design) and inserts `public.coach_clients (coach_id, client_id)` with the service-role client (bypasses RLS). The `enforce_coach_client_roles` trigger (plan 02) requires the coach to already be role='coach' before assignment — so promote the coach BEFORE inserting coach_clients rows.

Anon-key RLS check client (verify-rls.ts): `createClient(NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY, { auth: { persistSession: false } })` then `signInWithPassword({ email, password })` per seeded account — this session is subject to RLS (unlike the service-role key).

Root package.json scripts today: dev, dev:web, build, build:web, lint, typecheck (add seed + verify:rls + supabase helpers alongside).
Packages are `"type": "module"`; Node 25 supports `node --experimental-strip-types scripts/seed.ts` to run TS directly. Load env from `apps/web/.env.local`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Idempotent service-role seed script (pagination-safe) + pnpm workflow scripts</name>
  <files>scripts/seed.ts, package.json</files>
  <read_first>
    - .planning/phases/02-secure-account-you-can-return-to/02-RESEARCH.md (Code Examples seed skeleton, D-10/D-11, Assumption A2, Assumption A3, Don't Hand-Roll admin API row)
    - .planning/phases/02-secure-account-you-can-return-to/02-PATTERNS.md (scripts/seed.ts section — repo conventions overlay, idempotency requirement)
    - package.json (current root scripts — add alongside, remove none)
    - supabase/functions/send-message/index.ts (calm known-error-string handling precedent)
    - supabase/migrations/0003_coach_clients.sql (enforce_coach_client_roles — coach must be promoted before assignment)
  </read_first>
  <action>
    Create `scripts/seed.ts` — an ESM Node/TS script with a `main()` + top-level `await main()`. Build the service-role client per the interface skeleton (service-role key from `SUPABASE_SERVICE_ROLE_KEY`, `autoRefreshToken: false`, `persistSession: false`). Define fixed, documented dev credentials: one coach (e.g. `coach@fish.dev`) and ~3 clients (e.g. `client1@fish.dev`, `client2@fish.dev`, `client3@fish.dev`), each with a documented password (≥ 8 chars per D-16) and a real `display_name` (D-04 — so Phase 3's coach home lists by name). An `upsertUser(email, password, displayName)` helper calls `admin.createUser({ ..., email_confirm: true, user_metadata: { display_name } })`; on the `'already been registered'` error it looks the user up PAGINATION-SAFELY (page through `admin.listUsers({ page, perPage: 1000 })`, scan each page for the email, stop when found or a page returns zero users — review MEDIUM: do not assume page 1) and returns the existing id rather than throwing — idempotency is load-bearing (D-11), not optional. ORDER MATTERS: after all users exist, FIRST promote the coach via a service-role `update public.profiles set role='coach'` (service_role bypasses the escalation guard — Assumption A3), THEN upsert `coach_clients` rows assigning each client to the coach (the `enforce_coach_client_roles` trigger from plan 02 rejects the assignment if the coach isn't yet role='coach'). Make the coach_clients upsert idempotent (insert with `on conflict do nothing` semantics or delete-the-client's-row-then-insert per D-12 reassignment-replaces). Print a summary of created/existing accounts and their credentials.
    In root `package.json`, add scripts wrapping the local workflow (names are Claude's discretion, e.g.): `"supabase:start": "supabase start"`, `"db:reset": "supabase db reset"`, `"seed": "node --experimental-strip-types scripts/seed.ts"` (confirm the flag works in this Node 25 environment; load env from `apps/web/.env.local`). Do not remove existing scripts.
  </action>
  <verify>
    <automated>cd /Users/franz/Work/Personal/fish && grep -q "admin.createUser" scripts/seed.ts && grep -q "already been registered" scripts/seed.ts && grep -q "listUsers" scripts/seed.ts && grep -q "coach_clients" scripts/seed.ts && grep -q "email_confirm" scripts/seed.ts && grep -q "\"seed\"" package.json && ! grep -q "insert into auth.users" scripts/seed.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/seed.ts` contains `admin.createUser`, `email_confirm: true`, an `'already been registered'` idempotency branch, a pagination-safe `admin.listUsers({ page` lookup loop, a service-role `update public.profiles set role='coach'` (or equivalent) for the coach BEFORE the assignment, and a `coach_clients` assignment insert
    - `scripts/seed.ts` does NOT contain any raw `insert into auth.users` (barred by D-11); `grep -c "insert into auth.users" scripts/seed.ts` returns 0
    - Root `package.json` contains a `"seed"` script and still contains `dev`, `build`, `lint`, `typecheck`
  </acceptance_criteria>
  <done>The seed creates a coach + 3 clients via the real admin API, promotes the coach then assigns them, uses a pagination-safe idempotent lookup, and is safe to run twice.</done>
</task>

<task type="auto">
  <name>Task 2: Scripted anon-session RLS/escalation check (scripts/verify-rls.ts)</name>
  <files>scripts/verify-rls.ts, package.json</files>
  <read_first>
    - .planning/phases/02-secure-account-you-can-return-to/02-RESEARCH.md (Validation Architecture DB-03/DB-04 rows; Pitfall 1 getUser; anon vs service-role distinction)
    - scripts/seed.ts (the seeded credentials this script logs in as — Task 1)
    - .planning/phases/02-secure-account-you-can-return-to/02-REVIEWS.md (the MEDIUM concern: service role bypasses RLS in Studio; script the boundary with anon sessions)
  </read_first>
  <action>
    Create `scripts/verify-rls.ts` — an ESM Node/TS script (`main()` + top-level `await main()`) that asserts the DB-03/DB-04 boundary using ANON-KEY sessions (subject to RLS), NOT the service-role key (review MEDIUM — service role silently bypasses RLS, so a Studio/service-role walk proves nothing). For each seeded account, build an anon-key client (`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `persistSession: false`), `signInWithPassword({ email, password })`, then run assertions and exit non-zero on any failure (so `pnpm verify:rls` is a real gate):
      - DB-03 client boundary: as `client1@fish.dev`, `select * from profiles` returns exactly one row (client1's own); it does NOT include client2/client3/coach.
      - DB-03 coach boundary: as `coach@fish.dev`, `select * from profiles` returns the coach's own row PLUS its assigned clients (3), and NOT any unassigned/other user.
      - DB-04 escalation: as `client1@fish.dev`, attempt `update profiles set role='coach'` on the own row → must fail (guard trigger). Attempt a safe-field update (`display_name`) → must succeed (proves the update reached the trigger, not blocked by absent RLS).
      - No `42P17`: any recursion error is a hard failure.
    Print a PASS/FAIL line per assertion and set the process exit code to 1 if any assertion fails. Add a `"verify:rls": "node --experimental-strip-types scripts/verify-rls.ts"` script to root `package.json` (load env from `apps/web/.env.local`). Do not remove existing scripts.
  </action>
  <verify>
    <automated>cd /Users/franz/Work/Personal/fish && grep -q "signInWithPassword" scripts/verify-rls.ts && grep -q "NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY" scripts/verify-rls.ts && ! grep -q "SERVICE_ROLE" scripts/verify-rls.ts && grep -q "\"verify:rls\"" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-rls.ts` uses `signInWithPassword` with the anon/publishable key and NEVER the service-role key (`grep -c "SERVICE_ROLE" scripts/verify-rls.ts` returns 0)
    - It asserts the client boundary, the coach boundary, the escalation rejection, AND the safe-field success, and exits non-zero on any failure
    - Root `package.json` contains a `"verify:rls"` script
  </acceptance_criteria>
  <done>A repeatable script proves the RLS boundary and no-escalation using real anon-key sessions that are actually subject to RLS — replacing the service-role Studio walk the review flagged as unreliable.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Run seed + verify:rls, confirm DB-02/DB-03/DB-04 against seeded accounts</name>
  <what-built>The seed is executed against local Supabase and the seeded accounts prove DB-02 (coach + assigned clients exist), DB-03 (RLS boundary between the real seeded roles, asserted by the anon-session script), and DB-04 (no self-escalation). These require the running local stack. The scripted `verify:rls` gate does the heavy lifting; the human confirms the run and idempotency.</what-built>
  <how-to-verify>
    1. Ensure local Supabase is running (`supabase status`). Run the seed twice: `pnpm seed` then `pnpm seed` again. The second run must complete with exit 0 and create zero new accounts (idempotency — D-11), which exercises the pagination-safe lookup.
    2. DB-02: `select count(*) from public.coach_clients` → 3 rows (each of the 3 clients assigned to the one coach). `select role from public.profiles where display_name = '<coach name>'` → `coach`. All 4 seeded users have a `public.profiles` row (DB-01 trigger fired for each).
    3. DB-03 + DB-04 (scripted): run `pnpm verify:rls` → exits 0 with every assertion PASS (client sees only own row; coach sees own + 3 assigned; authenticated role-escalation rejected; safe-field update accepted; no `42P17`). This is the authoritative boundary proof (anon sessions, RLS in force).
    4. Human sanity pass (optional but recommended): log in as `client1@fish.dev` in Studio/an anon session and eyeball that `select * from public.profiles` returns only client1 — confirming the scripted result matches a manual look.
  </how-to-verify>
  <read_first>
    - .planning/phases/02-secure-account-you-can-return-to/02-RESEARCH.md (Validation Architecture DB-02/DB-03/DB-04 rows; Assumption A3)
    - scripts/seed.ts and scripts/verify-rls.ts (credentials + assertion logic just written)
  </read_first>
  <acceptance_criteria>
    - Running `pnpm seed` twice succeeds both times; the second run adds zero accounts (idempotent, pagination-safe)
    - `select count(*) from public.coach_clients` returns 3; the coach profile has `role='coach'`; all 4 seeded users have a profiles row
    - `pnpm verify:rls` exits 0 with all assertions PASS (client boundary, coach boundary, escalation rejected, safe-field accepted, no `42P17`)
  </acceptance_criteria>
  <resume-signal>Type "approved" once the seed runs idempotently and `pnpm verify:rls` passes all DB-02/DB-03/DB-04 assertions against the real seeded accounts.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Deploy-time checklist doc (D-14)</name>
  <files>docs/deploy-checklist.md</files>
  <read_first>
    - .planning/phases/02-secure-account-you-can-return-to/02-CONTEXT.md (D-14, D-15 — what deploy-time work is deferred)
    - .planning/phases/02-secure-account-you-can-return-to/02-RESEARCH.md (Environment Availability, Pitfall 6 template URLs, deferred custom SMTP note)
  </read_first>
  <action>
    Create `docs/deploy-checklist.md` — a plain prose checklist (no code pattern) of the hosted-Supabase setup deferred this phase per D-14, to be executed when the app first deploys. Cover: creating/linking the hosted Supabase project (`supabase link`); setting the production `Site URL` and the redirect allow-list to include the deployed `/auth/confirm` origin; pushing migrations to the hosted DB (`supabase db push`); uploading/confirming the two production email templates point at `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=...&next=...` (Pitfall 6 — never `{{ .ConfirmationURL }}`; recovery template carries `type=recovery&next=/reset-password`); setting hosted env vars (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SERVICE_ROLE_KEY`) in the deploy platform; confirming the hosted `[auth]` config carries `minimum_password_length = 8` (verify against the hosted project's config, same CLI-version-sensitivity note as local); and a note that the built-in email sender has rate limits, with custom SMTP (e.g. Resend) deferred until deliverability matters (deferred idea from CONTEXT.md). Confirm the seed is NOT run against production (dev credentials are local-only).
  </action>
  <verify>
    <automated>cd /Users/franz/Work/Personal/fish && grep -q "Site URL" docs/deploy-checklist.md && grep -q "auth/confirm" docs/deploy-checklist.md && grep -q "token_hash" docs/deploy-checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/deploy-checklist.md` mentions linking the hosted project, `Site URL`, the redirect allow-list, `supabase db push`, both email templates pointing at `/auth/confirm?token_hash=...` (not `{{ .ConfirmationURL }}`), hosted env vars, the `minimum_password_length` config check, and the built-in-sender rate-limit / deferred-SMTP note
    - The doc states the dev seed is not run in production
  </acceptance_criteria>
  <done>A single documented checklist captures every hosted-setup step deferred by D-14 so first deploy is a checklist, not a rediscovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → Supabase admin API | The service-role key grants full bypass of RLS; must never leak to the client |
| seed → auth.users | Seeded users must pass through GoTrue, not raw SQL, to fire the real trigger |
| service_role → profiles.role | The one legitimate path that may set role='coach' |
| verify-rls anon session → RLS | The check must use an anon session (RLS in force), never the service-role key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Elevation of Privilege | seed role promotion | mitigate | Coach role set only via service_role update (bypasses the escalation guard by design); the guard still blocks authenticated callers (verified by `verify:rls`, Assumption A3) |
| T-02-10 | Information Disclosure | service-role key in seed | mitigate | Key read from `SUPABASE_SERVICE_ROLE_KEY` (never `NEXT_PUBLIC_`), env confined to the seed's runtime; `.env.local` git-ignored (plan 01) |
| T-02-11 | Tampering | raw auth.users insert bypassing GoTrue | mitigate | Seed uses `admin.createUser` only; grep gate forbids `insert into auth.users` (Task 1) |
| T-02-12 | Information Disclosure | dev credentials reaching production | accept | Dev credentials are fixed and documented for local only; deploy checklist explicitly bars running the seed in prod (Task 4) |
| T-02-26 | Elevation of Privilege | RLS verified via service role (false pass) | mitigate | `verify-rls.ts` uses anon-key sessions subject to RLS, never the service-role key (grep gate, Task 2) — the review's exact concern |
</threat_model>

<verification>
- `pnpm seed` runs idempotently (twice, zero dupes, pagination-safe).
- `pnpm verify:rls` exits 0 asserting DB-03/DB-04 with anon sessions (Task 2/3 — the scripted boundary the review asked for).
- No raw `insert into auth.users`; no service-role key in verify-rls.ts.
- Deploy checklist covers every D-14 item.
</verification>

<success_criteria>
The seed creates a coach + ~3 assigned clients through the real auth machinery, idempotently and pagination-safely (DB-02); a scripted anon-session check proves RLS and no-escalation for the real seeded accounts (DB-03/DB-04); the hosted-setup checklist (D-14) is written.
</success_criteria>

<output>
Create `.planning/phases/02-secure-account-you-can-return-to/02-03-SUMMARY.md` when done.
</output>
</content>
