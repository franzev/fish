---
phase: 03-role-aware-home
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/web/app/(authenticated)/coach/page.tsx
  - apps/web/app/(authenticated)/coach/page.test.tsx
  - apps/web/components/coach/client-list.tsx
  - apps/web/components/coach/client-list.test.tsx
  - scripts/verify-rls.ts
autonomous: true
requirements: [ROUT-03, ROUT-04, SHEL-02]
user_setup: []

must_haves:
  truths:
    - "A signed-in coach landing on /coach sees the 'Your clients' heading inside the shell"
    - "The coach home lists that coach's assigned (seeded) clients — and only theirs — alphabetically by display name"
    - "Each client row shows the display name (main) and email (quiet/muted); no dates, no hover/cursor affordance"
    - "A coach with zero assigned clients sees a calm empty state"
    - "A client visiting /coach is silently redirected to /home"
    - "verify:rls proves a client can read only their own coach's name via the new is_client_of policy"
  artifacts:
    - path: "apps/web/app/(authenticated)/coach/page.tsx"
      provides: "Coach home: role guard (client→/home), own-clients read, ClientList or empty state"
      min_lines: 30
    - path: "apps/web/components/coach/client-list.tsx"
      provides: "Alphabetical stacked rows (name + muted email) in one Card with hairline dividers, inert (no hover/cursor)"
      exports: ["ClientList"]
    - path: "scripts/verify-rls.ts"
      provides: "checkClientReadsCoachName() assertion for the new is_client_of policy"
      contains: "checkClientReadsCoachName"
  key_links:
    - from: "apps/web/app/(authenticated)/coach/page.tsx"
      to: "coach_clients → profiles"
      via: ".from('coach_clients').select('client_id, profiles(...)') scoped by RLS to the coach's own assignments"
      pattern: "coach_clients"
    - from: "apps/web/app/(authenticated)/coach/page.tsx"
      to: "components/coach/client-list.tsx"
      via: "renders <ClientList clients={...}> when clients exist, else EmptyState"
      pattern: "ClientList"
    - from: "apps/web/app/(authenticated)/coach/page.tsx"
      to: "role guard"
      via: "if role === 'client' redirect('/home')"
      pattern: "redirect\\(\"/home\"\\)"
---

<objective>
Build the coach home at `/coach`: a "Your clients" heading over an alphabetical, inert list of the coach's own assigned clients (name + quiet email) in one calm Card with hairline dividers — or a calm empty state when the coach has no clients. A client who lands here is silently forwarded to /home. Extend verify:rls to prove the new client→coach-name RLS policy live.

Purpose: This is the coach's landing and the payoff of the seeded coach-client relationship — the seed's Coach Dana sees exactly Alex, Priya, and Sam and no one else's clients. Delivers ROUT-03, ROUT-04, and the coach half of SHEL-02.
Output: The `(authenticated)/coach/page.tsx` coach home, the `ClientList` component, both tests, and a new `checkClientReadsCoachName()` assertion in verify-rls.ts.
</objective>

<execution_context>
@/Users/franz/.claude/gsd-core/workflows/execute-plan.md
@/Users/franz/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-role-aware-home/03-CONTEXT.md
@.planning/phases/03-role-aware-home/03-RESEARCH.md
@.planning/phases/03-role-aware-home/03-PATTERNS.md
@.planning/phases/03-role-aware-home/03-01-PLAN.md

<interfaces>
<!-- Contracts produced by Plan 01 (this plan's dependency). Use directly. -->

From apps/web/components/home/empty-state.tsx (Plan 01):
```
export function EmptyState({ Icon, children }: { Icon: Icon; children: React.ReactNode }): JSX.Element
```

From apps/web/app/(authenticated)/layout.tsx (Plan 01): already redirected signed-out visitors, resolved role, wrapped this page in <AppShell>. This page re-reads getUser() for the id + role.

Existing Card (apps/web/components/ui/card.tsx): `export function Card` — accepts className.

Existing coach_clients RLS (0004): "coach reads own assignments" policy scopes `select from coach_clients` to `coach_id = auth.uid()`. "coach reads assigned clients" (is_coach_of) scopes `select from profiles` to own row + assigned clients.
Existing profiles columns AFTER Plan 01: id, role, display_name, email, created_at, updated_at.

Existing verify-rls.ts helpers to reuse (scripts/verify-rls.ts):
```
async function signInAs(email, password)  // anon-key session, RLS in force
function report(label, ok, detail?)        // logs PASS/FAIL, increments failures
function checkNoRecursion(label, error)    // hard-fails on 42P17
const coach = { email: "coach@fish.dev", password: "fish-coach-dev" };
const client1 = { email: "client1@fish.dev", password: "fish-client-dev" };
// existing calls in main(): checkClientBoundary, checkCoachBoundary, checkEscalationRejected
```
</interfaces>
</context>

## Phase Goal

**As a** logged-in coach, **I want to** land on a home listing exactly the clients assigned to me, **so that** I can see who I'm working with — without any other coach's clients leaking in and nothing on screen pretending to be tappable before it leads anywhere.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClientList component (alphabetical, inert rows) + its test</name>
  <files>apps/web/components/coach/client-list.tsx, apps/web/components/coach/client-list.test.tsx</files>
  <read_first>
    - apps/web/components/ui/card.tsx (the Card container this list uses; className passthrough)
    - .planning/phases/03-role-aware-home/03-PATTERNS.md (the ClientList Pattern 4 block — copy directly: Card with `divide-y divide-border p-0`, alphabetical sort by displayName, name + muted email rows)
    - apps/web/components/ui/alert.tsx (the typed-props + inline-map convention for a config-driven render component)
  </read_first>
  <behavior>
    - Test (alphabetical — D-15): given clients out of order (e.g. Sam, Alex, Priya) → rendered order is Alex, Priya, Sam.
    - Test (name + email — D-13): each row shows the displayName as main text and the email as muted secondary text.
    - Test (inert — D-14): the component source contains no `cursor-pointer` and no `hover:` class.
    - Test (single container — D-12): the list is one Card with `divide-y` dividers, not one Card per row.
  </behavior>
  <action>
    Create `apps/web/components/coach/client-list.tsx` exporting `ClientList({ clients }: { clients: Client[] })` where `interface Client { id: string; displayName: string; email: string }`. Import `Card` from `@/components/ui/card`. Sort a copy alphabetically by `displayName` using `localeCompare` (D-15 — never mutate the prop). Render one `<Card className="divide-y divide-border p-0">` containing `sorted.map((client) => <div key={client.id} className="flex flex-col gap-0.5 p-4"><span className="text-foreground">{client.displayName}</span><span className="text-[14px] text-muted">{client.email}</span></div>)`. NO `hover:` or `cursor-pointer` classes anywhere (D-14 — rows are inert, nothing looks tappable). No `assigned_at`/dates (D-13). Named export, no forwardRef (not focusable).
    Create `apps/web/components/coach/client-list.test.tsx`: render with an out-of-order clients array; assert the DOM order is alphabetical (query all name spans, assert their textContent sequence). Assert each email renders. Add the D-14 source grep gate: `const source = readFileSync(resolve(__dirname, "./client-list.tsx"), "utf-8"); expect(source).not.toMatch(/cursor-pointer|hover:/)`.
  </action>
  <verify>
    <automated>pnpm --filter @fish/web test -- --run components/coach/client-list.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components/coach/client-list.tsx exports `ClientList`, defines `interface Client` with `id`, `displayName`, `email`
    - The component sorts by `displayName` with `localeCompare` and does not mutate the incoming `clients` prop (uses a spread/copy before sort)
    - The component renders exactly one `Card` with `divide-y` (single container, D-12) and contains no `cursor-pointer` or `hover:` substring
    - The test asserts alphabetical DOM order for an out-of-order input and asserts email text renders
    - The test's D-14 grep gate asserts the source matches neither `cursor-pointer` nor `hover:`
    - `pnpm --filter @fish/web test -- --run components/coach/client-list.test.tsx` exits 0
  </acceptance_criteria>
  <done>ClientList renders one calm Card of alphabetical, inert name+email rows with hairline dividers — tested for order, email presence, and the no-affordance constraint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Coach home page (role guard, own-clients read, list/empty state)</name>
  <files>apps/web/app/(authenticated)/coach/page.tsx, apps/web/app/(authenticated)/coach/page.test.tsx</files>
  <read_first>
    - apps/web/app/home/page.tsx (the getUser() server-component shape; note the signed-out redirect is now owned by the layout — do NOT repeat it here)
    - apps/web/app/home/page.test.tsx (the mock scaffold to copy: redirect mock re-throwing NEXT_REDIRECT, getUser mock, createClient .from().select().eq() chain mock)
    - scripts/verify-rls.ts (checkCoachBoundary — the proven "trust RLS, no manual coach-id filter" query discipline this page mirrors)
    - .planning/phases/03-role-aware-home/03-PATTERNS.md (the `(authenticated)/coach/page.tsx` block + Open Question 3 resolution: query coach_clients joined to profiles, avoiding the self-row edge case)
    - apps/web/components/coach/client-list.tsx (Task 1 — the list this page renders)
    - apps/web/components/home/empty-state.tsx (Plan 01 — the empty state for zero clients)
    - apps/web/tests/icon-source.test.ts (only @tabler/icons-react — pick a calm icon, e.g. IconUsers, for the empty state)
  </read_first>
  <behavior>
    - Test (client wrong-door — D-03): mocked profile role 'client' → `redirect("/home")` (mock re-throws NEXT_REDIRECT).
    - Test (coach with clients — ROUT-03/ROUT-04): mocked role 'coach', coach_clients join returns three client rows → renders `ClientList` with those three and the "Your clients" heading; renders no empty state.
    - Test (coach with zero clients — SHEL-02): mocked role 'coach', join returns [] → renders the calm empty-state copy, no ClientList.
    - Test (no manual coach-id filter): page source queries `coach_clients` (RLS-scoped) and does NOT contain a hand-written `.eq("coach_id"` filter (RLS is the boundary).
  </behavior>
  <action>
    Create `apps/web/app/(authenticated)/coach/page.tsx` as an async Server Component. Do NOT repeat the signed-out redirect (layout owns it). Do:
    (1) `createClient()` → `getUser()` for the id; read role via `.from("profiles").select("role").eq("id", user.id).single()`. If `role === "client"` → `redirect("/home")` (D-03 wrong-door).
    (2) Read the coach's OWN clients by querying `coach_clients` (already RLS-scoped to `coach_id = auth.uid()` by the "coach reads own assignments" policy) joined to `profiles` for name+email — resolving RESEARCH.md Open Question 3 (query coach_clients, not profiles-minus-self): `.from("coach_clients").select("client_id, profiles:client_id(id, display_name, email)")` (embedded-resource select; the FK from coach_clients.client_id → profiles.id enables the join, and the is_coach_of profiles policy authorizes reading each joined client row). Map the result rows to `{ id, displayName, email }`. Do NOT add a manual `.eq("coach_id", user.id)` — RLS scopes it (AGENTS.md API rule; checkCoachBoundary precedent). If the embedded-select shape is rejected by the generated types, fall back to: read `client_id`s from coach_clients, then `.from("profiles").select("id, display_name, email").in("id", clientIds)` — still no coach-id filter, still RLS-scoped, and it sidesteps the self-row inclusion that a bare `profiles` select would cause.
    (3) Render `<h1 className="mb-6 text-3xl">Your clients</h1>` (D-19). If `clients.length === 0` → `<EmptyState Icon={IconUsers}>` with calm copy ("Clients assigned to you will show up here." — sentence case, no scolding, no action, D-17/D-18). Else → `<ClientList clients={clients} />`. No `Button variant="primary"` on this page (SHEL-01 — logout is in the shell).
    Create `apps/web/app/(authenticated)/coach/page.test.tsx`: copy the mock scaffold from home/page.test.tsx; extend the createClient mock so `.from(table)` returns table-keyed chainable stubs resolving the role query and the coach_clients/profiles read. Assert the four behaviors above.
  </action>
  <verify>
    <automated>pnpm --filter @fish/web test -- --run app/\\(authenticated\\)/coach/page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/(authenticated)/coach/page.tsx is `async function`, calls `getUser()` (not `getSession()`), and contains `redirect("/home")` guarded by `role === "client"`
    - The page queries `coach_clients` and does NOT contain the substring `.eq("coach_id"` (RLS is the boundary)
    - The page renders `Your clients` heading, renders `ClientList` when clients exist and `EmptyState` when empty, and contains no `variant="primary"`
    - The page uses only `@tabler/icons-react` for its icon (icon-source guard stays green)
    - The test asserts: client role → redirect('/home'); coach with 3 rows → ClientList with 3 clients; coach with 0 rows → empty-state copy
    - `pnpm --filter @fish/web test -- --run app/\\(authenticated\\)/coach/page.test.tsx` exits 0
  </acceptance_criteria>
  <done>The coach home guards against clients, lists exactly the coach's own assigned clients (via RLS-scoped coach_clients, no manual filter), shows a calm empty state at zero, and carries no primary action.</done>
</task>

<task type="auto">
  <name>Task 3: Extend verify-rls.ts with the client-reads-coach-name assertion</name>
  <files>scripts/verify-rls.ts</files>
  <read_first>
    - scripts/verify-rls.ts (the full existing script — copy the shape of checkClientBoundary()/checkCoachBoundary(): signInAs helper, report(), checkNoRecursion(), and the main() call list at the bottom)
    - supabase/migrations/0006_client_reads_coach_name.sql (Plan 01 — the is_client_of policy this assertion proves live)
    - scripts/seed.ts (the seeded ids: coach@fish.dev "Coach Dana"; client1@fish.dev "Alex Rivera" is assigned to that coach — the assertion resolves the coach id via the coach_clients row the client can read)
  </read_first>
  <action>
    Add `async function checkClientReadsCoachName(): Promise<void>` to `scripts/verify-rls.ts`, mirroring checkClientBoundary's shape. Sign in as client1 via `signInAs(client1.email, client1.password)`. First resolve the assigned coach id the way a client legitimately can: `.from("coach_clients").select("coach_id").eq("client_id", <own id from getUser()>).single()` (the "client reads own coach assignment" policy from 0003 permits this). Then `.from("profiles").select("display_name").eq("id", assignment.coach_id)`. Call `checkNoRecursion("D-16 client reads coach name", error)` on the profiles read. `report(...)` PASS when the select succeeds and returns exactly one row whose display_name is non-empty (the coach's). Also add a negative assertion: the client must NOT be able to read a DIFFERENT coach's row or an unrelated client's row — reuse the existing checkClientBoundary logic already proves the client sees only their own profile among un-related rows, so here just assert the coach read returns exactly one row (the count-of-1 is the scoping proof). Call the new function in `main()` alongside the existing three (`await checkClientReadsCoachName();`).
    Do not change the existing three assertions. Keep the process.exit(failures === 0 ? 0 : 1) gate intact.
  </action>
  <verify>
    <automated>grep -q "checkClientReadsCoachName" scripts/verify-rls.ts &amp;&amp; grep -q "await checkClientReadsCoachName" scripts/verify-rls.ts</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-rls.ts defines `async function checkClientReadsCoachName` and calls it in `main()` (`await checkClientReadsCoachName()`)
    - The new function signs in as client1, resolves the coach id via a `coach_clients` read, then reads the coach's `profiles.display_name`, and calls `checkNoRecursion` on the profiles read
    - The function `report`s PASS when the coach read returns exactly one non-empty display_name
    - The existing checkClientBoundary/checkCoachBoundary/checkEscalationRejected functions and calls are unchanged
    - `pnpm verify:rls` exits 0 (all four assertions pass against the live seeded DB — run after Plan 01 Task 2's db reset + seed)
  </acceptance_criteria>
  <done>verify:rls proves the new is_client_of policy lets a seeded client read exactly their own coach's name and no other, with no recursion — a live RLS gate, not just a UI check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → coach home Server Component | Session cookie crosses here; the client-guard and the clients read must trust only server-resolved role + RLS |
| authenticated coach → assigned clients' profile rows | RLS (coach_clients "coach reads own assignments" + is_coach_of) is the boundary scoping the list to the coach's own clients |
| authenticated client → coach's profile row (verify-rls) | is_client_of policy scoping — proven live by the new assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-06 | Information Disclosure | coach client-list read | mitigate | The list query trusts RLS (no manual coach-id filter); a coach can only ever read their own assignments (coach_clients policy) + their assigned clients' profiles (is_coach_of). Proven live by checkCoachBoundary (sees exactly own row + 3 clients). |
| T-3-07 | Information Disclosure | is_client_of over-read | mitigate | The new verify-rls assertion proves a client reads exactly one coach row (count===1); the count-of-1 IS the scoping proof that the policy does not leak other coaches. |
| T-3-08 | Elevation of Privilege | client visiting /coach | mitigate | Server-side role read redirects a client to /home; even without the redirect, RLS denies a client any coach's client list (is_coach_of requires the caller's own role to be coach). |
</threat_model>

<verification>
- `pnpm --filter @fish/web test -- --run components/coach/client-list.test.tsx app/(authenticated)/coach/page.test.tsx` exits 0.
- `pnpm verify:rls` exits 0 with the new D-16 assertion (after Plan 01's db reset + seed).
- `pnpm build` exits 0; full suite green; icon-source guard green.
- Manual (phase verification): log in as coach@fish.dev → /coach lists Alex Rivera, Priya Nair, Sam Okafor (alphabetical) with emails, no other coach's clients, rows inert. Log in as client1@fish.dev → hitting /coach silently forwards to /home.
</verification>

<success_criteria>
- A coach lands on the coach home after login (ROUT-03).
- The coach home lists that coach's assigned seeded clients and only theirs, alphabetically (ROUT-04 / D-15).
- Each row shows name + quiet email, no dates, inert (D-12/13/14).
- A coach with zero clients sees a calm empty state (SHEL-02).
- A client visiting /coach is silently redirected to /home (D-03).
- verify:rls proves the is_client_of policy scoping live.
</success_criteria>

<output>
Create `.planning/phases/03-role-aware-home/03-03-SUMMARY.md` when done.
</output>
