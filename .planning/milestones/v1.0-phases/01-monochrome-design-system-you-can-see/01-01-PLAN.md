---
phase: 01-monochrome-design-system-you-can-see
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/vitest.config.ts
  - apps/web/vitest.setup.ts
  - apps/web/tests/contrast.test.ts
  - apps/web/app/globals.css
  - apps/web/components/ui/button.tsx
  - apps/web/components/ui/button.test.tsx
  - apps/web/components/kit/theme-toggle.tsx
  - apps/web/app/kit/page.tsx
autonomous: false
requirements:
  - TOKN-01
  - TOKN-02
  - TOKN-03
  - TOKN-04
  - TOKN-05
  - TOKN-06
  - KIT-01
  - KIT-04
  - KIT-05
  - KIT-06
user_setup: []

must_haves:
  truths:
    - "A person can open /kit and see it render in pure monochrome (black/white/grey only) (D-01, D-02, D-12)"
    - "Toggling the /kit dev control between system/light/dark switches the whole page cleanly and every token resolves in both themes (D-14)"
    - "On first paint the page follows the OS theme with no flash of the wrong theme (zero JS for the default) (D-16)"
    - "The Button renders default, hover, focus, disabled, and loading states in both themes, primary is a full-contrast inverted block (D-03)"
    - "The keyboard focus ring is visible on the inverted primary button in both light and dark (D-05)"
    - "pnpm test runs and the WCAG AA contrast assertion passes for every token pair in both themes (D-07, D-19)"
  artifacts:
    - path: "apps/web/app/globals.css"
      provides: "Semantic-role monochrome light-dark() token ladder + color-scheme + two-tone focus ring, hand-written in @theme (D-04, D-17)"
      contains: "light-dark("
    - path: "apps/web/vitest.config.ts"
      provides: "First test runner config (jsdom + React plugin)"
      contains: "jsdom"
    - path: "apps/web/tests/contrast.test.ts"
      provides: "WCAG AA contrast assertions for every token pair, both themes"
      contains: "contrast"
    - path: "apps/web/components/kit/theme-toggle.tsx"
      provides: "Dev-only system/light/dark override setting color-scheme on html"
      contains: "colorScheme"
    - path: "apps/web/app/kit/page.tsx"
      provides: "The /kit demo route rendering Button + theme toggle"
      exports: ["default"]
    - path: "apps/web/components/ui/button.tsx"
      provides: "Hardened Button with loading + disabled states"
      contains: "loading"
  key_links:
    - from: "apps/web/components/ui/button.tsx"
      to: "apps/web/app/globals.css"
      via: "bg-primary / text-on-primary semantic tokens"
      pattern: "bg-primary"
    - from: "apps/web/components/kit/theme-toggle.tsx"
      to: "document.documentElement"
      via: "colorScheme style mutation"
      pattern: "colorScheme"
    - from: "apps/web/tests/contrast.test.ts"
      to: "colorjs.io"
      via: "WCAG21 contrast ratio"
      pattern: "contrast"
---

<objective>
Deliver the first working vertical slice of the monochrome design system: a person can open `/kit`, see it rendered in pure monochrome (dual-theme via native `light-dark()`), toggle system/light/dark with a dev control, and see the hardened Button in every state — with the project's first test infrastructure asserting WCAG AA contrast on every token pair.

Purpose: Establish the theme mechanism (D-16), the monochrome token ladder (TOKN-01..05), the icon dependency (TOKN-06), the test harness (D-19), and one real component end-to-end. Everything else in this phase widens this slice.
Output: Installed deps, Vitest config, contrast test, replaced token block in globals.css, hardened Button, dev theme toggle, and the `/kit` route showing them.
</objective>

<execution_context>
@/Users/franz/Library/Application Support/Claude/local-agent-mode-sessions/614fd504-8566-46b2-8256-66ded77bb604/43ed32e3-053c-413f-843e-d49e1f00f5e1/rpm/plugin_019FPGEfUV8XSdonegN1mWDs/workflows/execute-plan.md
@/Users/franz/Library/Application Support/Claude/local-agent-mode-sessions/614fd504-8566-46b2-8256-66ded77bb604/43ed32e3-053c-413f-843e-d49e1f00f5e1/rpm/plugin_019FPGEfUV8XSdonegN1mWDs/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/01-monochrome-design-system-you-can-see/01-CONTEXT.md
@.planning/phases/01-monochrome-design-system-you-can-see/01-RESEARCH.md
@.planning/phases/01-monochrome-design-system-you-can-see/01-PATTERNS.md
@.planning/phases/01-monochrome-design-system-you-can-see/01-UI-SPEC.md

<interfaces>
<!-- Extracted from codebase. Use directly — no exploration. -->

Current Button contract (apps/web/components/ui/button.tsx) — harden in place, do NOT rewrite the structure:
- `type Variant = "primary" | "secondary" | "ghost"`
- `interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> { variant?: Variant; fullWidth?: boolean }`
- `const variants: Record<Variant, string>` — primary uses `bg-primary text-on-primary hover:bg-primary-press active:bg-primary-press font-semibold`
- `export const Button = forwardRef<HTMLButtonElement, ButtonProps>(...)` + `Button.displayName = "Button"`
- Base classes include `min-h-[var(--size-control)]` (56px, KIT-04) and `disabled:opacity-50 disabled:pointer-events-none`

cn() helper (apps/web/lib/utils.ts): `cn(...inputs: ClassValue[])` = `twMerge(clsx(inputs))`. Ordering: static base classes first, conditional/prop-driven next, caller `className` last.

globals.css current color block (lines 17–52) — the ENTIRE aquatic-blue `@theme` color block is replaced wholesale. Hue tokens (`--color-accent-pink`, `--color-accent-yellow`, `--color-primary-press`, sea-green `--color-success`, amber `--color-notice`) do NOT carry forward. `--font-*`, `--radius-*`, `--size-control: 56px` are preserved.

Proposed monochrome token ladder (starting values per D-07 — the contrast test is the acceptance gate; adjust values until it passes, do not treat these as immutable):
- `--color-bg: light-dark(oklch(0.98 0 0), oklch(0.15 0 0))`
- `--color-surface: light-dark(oklch(1 0 0), oklch(0.20 0 0))`
- `--color-surface-2: light-dark(oklch(0.94 0 0), oklch(0.26 0 0))`
- `--color-border: light-dark(oklch(0.85 0 0), oklch(0.34 0 0))`
- `--color-border-strong: light-dark(oklch(0.75 0 0), oklch(0.44 0 0))`
- `--color-foreground: light-dark(oklch(0.15 0 0), oklch(0.97 0 0))`
- `--color-body: light-dark(oklch(0.32 0 0), oklch(0.88 0 0))`
- `--color-muted: light-dark(oklch(0.50 0 0), oklch(0.68 0 0))`
- `--color-primary: light-dark(oklch(0.15 0 0), oklch(0.98 0 0))`
- `--color-on-primary: light-dark(oklch(0.98 0 0), oklch(0.15 0 0))`
- `--color-notice: light-dark(oklch(0.40 0 0), oklch(0.80 0 0))`
- `--color-error: light-dark(oklch(0.20 0 0), oklch(0.95 0 0))`
- `--color-success: light-dark(oklch(0.40 0 0), oklch(0.80 0 0))`
- primary press = a lightness shift within the neutral ladder (e.g. light `oklch(0.25 0 0)`, dark `oklch(0.90 0 0)`) — Claude's discretion, verify contrast

Tabler icons (D-18): `import { IconInfoCircle, IconAlertCircle, IconCircleCheck } from "@tabler/icons-react"` — named imports only, never wildcard.

colorjs.io contrast (D-19): `new Color(oklchString).contrast(otherColor, "WCAG21")` returns a numeric ratio; assert ≥ 4.5 for text pairs, ≥ 3.0 for UI-component pairs.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify + install new dependencies (package legitimacy gate)</name>
  <read_first>
    - apps/web/package.json (current deps: no test framework, no icon set)
    - .planning/phases/01-monochrome-design-system-you-can-see/01-RESEARCH.md (Package Legitimacy Audit — all 7 packages tagged [ASSUMED] because slopcheck could not run this session)
  </read_first>
  <action>Present the 7 new packages to the human for legitimacy verification BEFORE installing (T-01-SC supply-chain gate; slopcheck was unavailable so this is non-auto-approvable, ignore auto_advance). After the human approves, install with pnpm only: `pnpm add @tabler/icons-react colorjs.io --filter @fish/web` then `pnpm add -D vitest @testing-library/react @testing-library/jest-dom jsdom @vitejs/plugin-react --filter @fish/web`. If any package looks wrong, stop and report rather than installing.</action>
  <what-built>
    Nothing is automated yet — this is a blocking human-verify gate REQUIRED before installing because RESEARCH.md's Package Legitimacy Audit tagged every new package `[ASSUMED]` (slopcheck was unavailable). Threat T-01-SC (supply-chain tampering). Do NOT auto-approve.
  </what-built>
  <how-to-verify>
    Confirm each package exists and is the legitimate, widely-used library by visiting its npm page:
    1. https://www.npmjs.com/package/@tabler/icons-react (expect v3.44.0, repo github.com/tabler/tabler-icons)
    2. https://www.npmjs.com/package/colorjs.io (expect v0.6.1, repo github.com/color-js/color.js, maintainer Lea Verou)
    3. https://www.npmjs.com/package/vitest (expect v4.x)
    4. https://www.npmjs.com/package/@testing-library/react (expect v16.x)
    5. https://www.npmjs.com/package/@testing-library/jest-dom
    6. https://www.npmjs.com/package/jsdom
    7. https://www.npmjs.com/package/@vitejs/plugin-react
    After you approve, the executor runs the two `pnpm add` commands in <action>.
  </how-to-verify>
  <verify>
    <automated>node -e "const p=require('./apps/web/package.json'); const need=['@tabler/icons-react','colorjs.io','vitest','@testing-library/react','@testing-library/jest-dom','jsdom','@vitejs/plugin-react']; const have={...p.dependencies,...p.devDependencies}; const missing=need.filter(n=>!have[n]); if(missing.length){console.error('missing',missing);process.exit(1)} console.log('all present')"</automated>
  </verify>
  <acceptance_criteria>
    - Human explicitly approves each of the 7 packages
    - After install, `apps/web/package.json` contains `@tabler/icons-react` and `colorjs.io` under `dependencies`
    - After install, `apps/web/package.json` contains `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom`, `@vitejs/plugin-react` under `devDependencies`
    - `pnpm-lock.yaml` updates with no unexpected additional top-level packages
  </acceptance_criteria>
  <done>All 7 packages verified on npmjs.com and installed via pnpm; package.json and lockfile reflect them with no surprise additions.</done>
  <resume-signal>Type "approved" after verifying all 7 npm pages, or name any package that looks wrong</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stand up Vitest + write the WCAG AA contrast test (RED then GREEN via tokens)</name>
  <files>apps/web/vitest.config.ts, apps/web/vitest.setup.ts, apps/web/package.json, apps/web/tests/contrast.test.ts, apps/web/app/globals.css</files>
  <read_first>
    - apps/web/app/globals.css (the existing aquatic-blue @theme block being replaced; preserve --font-*, --radius-*, --size-control and the base layer body/h1-h3/reduced-motion rules)
    - .planning/phases/01-monochrome-design-system-you-can-see/01-RESEARCH.md (Validation Architecture, "Code Examples" assertAA pattern, Pitfall 2 color-scheme, Pitfall 3 stale-token drift)
    - .planning/phases/01-monochrome-design-system-you-can-see/01-PATTERNS.md (globals.css section — exact base layer to preserve, token naming convention)
    - .planning/phases/01-monochrome-design-system-you-can-see/01-UI-SPEC.md (Color table — full token ladder with light/dark values)
    - apps/web/package.json (add "test": "vitest" script)
  </read_first>
  <behavior>
    - contrast.test.ts imports Color from "colorjs.io" and, for the light and dark resolved values of every token pair, asserts a WCAG21 ratio: body-on-bg ≥ 4.5, body-on-surface ≥ 4.5, foreground-on-bg ≥ 4.5, foreground-on-surface ≥ 4.5, on-primary-on-primary ≥ 4.5, muted-on-bg ≥ 4.5, notice/error/success text-on-surface ≥ 4.5, and border/border-strong-on-surface ≥ 3.0 (UI-component threshold)
    - The test extracts the two oklch values from each `light-dark(a, b)` token so it verifies the SAME source as globals.css (Pitfall 3): either parse globals.css at test time, or keep the light/dark value pairs in one shared module both the test and a comment in globals.css point to. Do not silently hardcode a second divergent copy.
    - A chroma assertion: every color token's oklch chroma channel is 0 (TOKN-01 — pure monochrome, no hue)
    - Test fails first (RED) if any value violates AA, then the token values in globals.css are tuned until GREEN
  </behavior>
  <action>Create `vitest.config.ts` in apps/web using `@vitejs/plugin-react`, `environment: "jsdom"`, and `setupFiles: ["./vitest.setup.ts"]`. Create `vitest.setup.ts` importing `@testing-library/jest-dom`. Add `"test": "vitest"` to the apps/web package.json scripts. Create `apps/web/tests/contrast.test.ts` implementing the behaviors above using `colorjs.io`'s `new Color(oklchString).contrast(other, "WCAG21")`. In `globals.css`, replace the ENTIRE aquatic-blue `@theme` color block (current lines 17–52) with the monochrome `light-dark()` ladder from the interfaces block above — remove `--color-accent-pink`, `--color-accent-yellow`, `--color-primary-press`, sea-green `--color-success`, amber `--color-notice`; add `--color-border-strong`, `--color-error`, and the primary-press lightness-shift token. Preserve `--font-sans`, `--font-serif`, `--radius-card/control/pill`, `--size-control: 56px`. In `@layer base`, add `html { color-scheme: light dark; }` (Pitfall 2 — without this light-dark() silently resolves to light only). Tune token lightness values until the contrast test passes for every pair in both themes. Do NOT add `@custom-variant dark` and do NOT introduce any `dark:` utility (Pitfall 1 / D-16).</action>
  <verify>
    <automated>pnpm --filter @fish/web test -- --run contrast.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/vitest.config.ts` exists and contains `jsdom` and the react plugin
    - `apps/web/vitest.setup.ts` imports `@testing-library/jest-dom`
    - `apps/web/package.json` scripts contains `"test": "vitest"`
    - `apps/web/app/globals.css` contains `light-dark(` and `color-scheme: light dark`
    - `grep -vE '^\s*/\*|^\s*\*' apps/web/app/globals.css | grep -Ec "accent-pink|accent-yellow|grey-[0-9]"` returns 0 (no hue-named or numbered-ramp tokens — TOKN-02/D-04)
    - `grep -c "@custom-variant" apps/web/app/globals.css` returns 0 (Pitfall 1 — no dark: variant mechanism)
    - `pnpm --filter @fish/web test -- --run contrast.test.ts` exits 0 with all AA + chroma-0 assertions passing
  </acceptance_criteria>
  <done>Vitest runs, the contrast test passes for every token pair in both themes, and globals.css holds the pure-monochrome light-dark() ladder with color-scheme set and no hue/ramp tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden Button (loading + two-tone focus), build dev theme toggle, and render the /kit slice</name>
  <files>apps/web/components/ui/button.tsx, apps/web/components/ui/button.test.tsx, apps/web/app/globals.css, apps/web/components/kit/theme-toggle.tsx, apps/web/app/kit/page.tsx</files>
  <read_first>
    - apps/web/components/ui/button.tsx (full current file — harden in place, keep forwardRef/displayName/Record<Variant,string> structure)
    - apps/web/app/page.tsx (existing showcase — its <main>/<section>/<h2> structure is the template for /kit; note it still references removed accent-pink/accent-yellow swatches which must NOT be copied)
    - apps/web/app/globals.css (add the two-tone :focus-visible rule per D-05)
    - .planning/phases/01-monochrome-design-system-you-can-see/01-PATTERNS.md (button.tsx extension points, theme-toggle.tsx pattern, kit/page.tsx structural pattern, two-tone focus-ring rule)
    - .planning/phases/01-monochrome-design-system-you-can-see/01-UI-SPEC.md (Component Inventory — Button states required; KitThemeToggle spec; two-tone focus ring Pitfall 4)
  </read_first>
  <behavior>
    - button.test.tsx (RTL + jsdom): renders `<Button>` and asserts the primary variant applies `bg-primary` and `text-on-primary`; asserts `min-h-[var(--size-control)]` class is present (KIT-04, ≥56px); asserts `disabled` sets `disabled:opacity-50`/pointer-events-none behavior (button is disabled); asserts a new `loading` prop renders a busy indicator and sets `aria-busy="true"` and prevents pointer events; asserts secondary and ghost variants render their expected token classes
    - Tests fail first against the un-hardened Button (no loading prop), then pass after hardening
  </behavior>
  <action>Harden `button.tsx` in place: add a `loading?: boolean` prop that, when true, appends `opacity-70 pointer-events-none`, sets `aria-busy="true"`, and renders a spinner element (the global `prefers-reduced-motion` rule in globals.css already suppresses its animation — do NOT add a component-level reduced-motion guard). Keep the existing `Record<Variant, string>` map, `forwardRef`, `Button.displayName`, and `min-h-[var(--size-control)]`. Update the `hover:bg-primary-press`/`active:bg-primary-press` class references to whatever the primary-press token is named in globals.css (Task 2 renamed it). In `globals.css` `@layer base`, replace the single-color `:focus-visible` rule with the two-tone ring per D-05: `outline: 2px solid light-dark(oklch(1 0 0), oklch(0.15 0 0)); outline-offset: 2px; box-shadow: 0 0 0 4px light-dark(oklch(0.15 0 0), oklch(1 0 0)); border-radius: 6px;` — this guarantees visibility on the inverted primary fill in both themes (Pitfall 4). Create `apps/web/components/kit/theme-toggle.tsx` as a `"use client"` component `KitThemeToggle` with `useState<"system"|"light"|"dark">` that sets `document.documentElement.style.colorScheme = next === "system" ? "light dark" : next` on click, `role="group"` + `aria-label="Preview theme"`, `aria-pressed` on each button. Create `apps/web/app/kit/page.tsx` as a Server Component default export: `<main className="mx-auto max-w-[...] px-5 py-12">` mounting `<KitThemeToggle />` once at the top, then a Button section showing all three variants (primary/secondary/ghost) plus disabled and loading states. Do NOT copy the removed accent-pink/accent-yellow swatches from page.tsx. Do NOT mark the whole /kit page `"use client"` — only the toggle is a client island.</action>
  <verify>
    <automated>pnpm --filter @fish/web test -- --run button.test.tsx && pnpm --filter @fish/web build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/ui/button.tsx` contains `loading` and `aria-busy` and still contains `Button.displayName = "Button"` and `min-h-[var(--size-control)]`
    - `apps/web/components/kit/theme-toggle.tsx` starts with `"use client"` and contains `document.documentElement.style.colorScheme`
    - `apps/web/app/kit/page.tsx` exports a default function and imports `KitThemeToggle` and `Button`
    - `grep -c "use client" apps/web/app/kit/page.tsx` returns 0 (page is a Server Component; only the toggle is a client island)
    - `apps/web/app/globals.css` `:focus-visible` rule contains `box-shadow` (two-tone ring — D-05)
    - `pnpm --filter @fish/web test -- --run button.test.tsx` exits 0
    - `pnpm --filter @fish/web build` succeeds (per AGENTS.md pre-commit requirement) and `/kit` is a valid route
  </acceptance_criteria>
  <done>Button renders all states (default/hover/focus/disabled/loading) in both themes with a visible two-tone focus ring on the inverted primary fill; the /kit route renders the Button section and a working dev theme toggle; the build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local build | New third-party dependencies enter the build; slopcheck could not run this session |
| dev/local only | No network, auth, DB, or user-submitted data crosses any boundary this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | pnpm installs (7 new packages, all `[ASSUMED]`) | mitigate | Blocking `checkpoint:human-verify` (Task 1) verifies each package on npmjs.com before install; slopcheck unavailable so gate is non-auto-approvable |
| T-01-02 | Tampering | @tabler/icons-react SVG rendering | accept | Icons render as compiled React components with library-fixed SVG paths; no `dangerouslySetInnerHTML`, no user-supplied SVG — no injection surface |
| T-01-03 | Information Disclosure | `/kit` ships in production unlinked (D-15) | accept | Page shows only design-system contract (tokens, component states, example copy) — no real user data, tokens, or secrets; deliberate product decision |
| T-01-04 | Denial of Service (user harm) | Contrast/focus regressions harming visually-sensitive users | mitigate | D-19 WCAG AA contrast test (Task 2) blocks AA failures; two-tone focus ring (Task 3) verified against inverted primary fill (Pitfall 4) |
</threat_model>

<verification>
- `pnpm --filter @fish/web test -- --run` — full suite green (contrast + button tests)
- `pnpm --filter @fish/web build` — production build succeeds
- `pnpm --filter @fish/web typecheck` — no type errors
- Manual (deferred to Plan 03 phase checkpoint): hard-refresh /kit in OS-light and OS-dark, confirm no flash of wrong theme and no hydration warning (TOKN-04)
</verification>

<success_criteria>
- A person can open `/kit`, see it in pure monochrome, and toggle system/light/dark cleanly
- The Button shows default/hover/focus/disabled/loading in both themes with a visible two-tone focus ring on the inverted primary fill
- `pnpm test` runs (project's first tests) and the WCAG AA contrast assertion passes for every token pair in both themes
- globals.css holds the monochrome light-dark() ladder with no hue tokens, no numbered ramp, no `@custom-variant dark`
</success_criteria>

<output>
Create `.planning/phases/01-monochrome-design-system-you-can-see/01-01-SUMMARY.md` when done
</output>
