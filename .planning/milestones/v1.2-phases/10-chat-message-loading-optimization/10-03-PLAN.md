---
phase: 10-chat-message-loading-optimization
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - apps/web/app/(authenticated)/chat/store/chat-store.ts
  - apps/web/app/(authenticated)/chat/store/chat-selectors.ts
  - apps/web/app/(authenticated)/chat/store/chat-store.test.ts
  - apps/web/app/(authenticated)/chat/hooks/use-chat-messages.ts
  - apps/web/app/(authenticated)/chat/hooks/use-chat-realtime.ts
  - apps/web/app/(authenticated)/chat/realtime.ts
autonomous: true
requirements: [CLOAD-02, CLOAD-03, CLOAD-05, CLOAD-06]
must_haves:
  truths:
    - "The Zustand store exposes conversation-keyed dispatch wrappers for hydrateWindow, olderMessagesRequested, olderPageLoaded, and olderPageLoadFailed, plus pagination selectors (hasMoreOlder, isLoadingOlder, oldestLoadedCursor)"
    - "The message hook exposes a loadOlderMessages callback (Promise-returning, guarded against double-firing) that requests, fetches an older keyset page, and dispatches olderPageLoaded through the single reducer merge path"
    - "The initial/SSR hydration flows through hydrateWindow carrying hasMoreOlder/oldestCursor from ClientChatData"
    - "Realtime reconnect runs ONE coalesced bounded gap-backfill instead of three concurrent full-history refetches; each of the three channels' FIRST post-mount SUBSCRIBED is treated as an initial subscribe (no backfill), and only genuine re-subscribes after a drop backfill; when the gap exceeds the bound it resets to the newest window via loadNewestMessagesAction"
    - "The messages realtime channel surfaces CHANNEL_ERROR/TIMED_OUT/CLOSED so the store realtime status becomes 'disconnected' (the signal Plan 04's calm offline/reconnect UI renders); 'connecting'/'connected' behavior is unchanged"
    - "The store still holds no auth/role/assignment/Supabase-client/service-role authority — pagination fields are plain client-cache values"
  artifacts:
    - path: "apps/web/app/(authenticated)/chat/store/chat-store.ts"
      provides: "hydrateWindow + older-page dispatch wrappers on ChatStoreState"
      contains: "olderPageLoaded"
    - path: "apps/web/app/(authenticated)/chat/store/chat-selectors.ts"
      provides: "selectHasMoreOlderForConversation, selectIsLoadingOlderForConversation, selectOldestCursorForConversation"
      contains: "selectHasMoreOlder"
    - path: "apps/web/app/(authenticated)/chat/hooks/use-chat-messages.ts"
      provides: "loadOlderMessages + applyGapBackfill callbacks; hydrateWindow-based initial hydration; loadNewest reset fallback"
      contains: "loadOlderMessages"
    - path: "apps/web/app/(authenticated)/chat/hooks/use-chat-realtime.ts"
      provides: "single coalesced reconnect backfill with per-channel first-subscribe tracking, replacing the triplicated refreshConversation; sets 'disconnected' status on channel error/close"
    - path: "apps/web/app/(authenticated)/chat/realtime.ts"
      provides: "subscribeToConversationMessages surfaces non-SUBSCRIBED terminal statuses (CHANNEL_ERROR/TIMED_OUT/CLOSED) via an onDisconnected callback"
      contains: "CHANNEL_ERROR"
  key_links:
    - from: "apps/web/app/(authenticated)/chat/hooks/use-chat-messages.ts"
      to: "loadOlderMessagesAction (Plan 02)"
      via: "injected server action prop, result dispatched as olderPageLoaded"
      pattern: "olderPageLoaded"
    - from: "apps/web/app/(authenticated)/chat/hooks/use-chat-realtime.ts"
      to: "applyGapBackfill (bounded, coalesced)"
      via: "per-channel first-subscribe tracking + one shared in-flight lock across all three channels' onReconnected"
      pattern: "onReconnected|Reconnect"
---

<objective>
Wire the portable pagination contract (Plan 01) and the read actions (Plan 02) into the web state
layer: the Zustand adapter gets pagination dispatch wrappers + selectors, the message hook gets a
guarded `loadOlderMessages` and a `applyGapBackfill`, initial hydration switches to `hydrateWindow`,
and the triplicated full-refetch-on-reconnect in the realtime hook is replaced by one coalesced,
bounded gap-backfill with per-channel first-subscribe tracking. This is the behavior "brain" — Plan
04 renders it.

Purpose: Make merge-in-place (CLOAD-02), single-path dedup (CLOAD-05), keyset load-earlier
(CLOAD-03), and reconnect backfill (CLOAD-06) real in the running app while keeping the CSTATE-03
web-only boundary intact.
Output: New store methods/selectors, message-hook callbacks (incl. the loadNewest reset fallback), a
consolidated reconnect path, a "disconnected" status emission for Plan 04's offline UI, and extended
store tests.
</objective>

<review_incorporation>
## Cross-AI review (REVIEWS.md) — incorporated (this was flagged the HIGH-risk plan)

- HIGH (single `hasSubscribedOnceRef` skips only the first of three initial SUBSCRIBED callbacks — realtime.ts:189/219/258 each fire onReconnected on the first mount): INCORPORATED. Task 3 tracks first-subscribe PER CHANNEL so all three initial subscribes skip backfill; only a channel's genuine re-subscribe backfills.
- HIGH (needsReset had no bounded newest-window fetch — falling back to refreshConversationAction hits the unbounded actions.ts:541): INCORPORATED. `applyGapBackfill` now calls Plan 02's `loadNewestMessagesAction` on needsReset and re-hydrates via hydrateWindow.
- MEDIUM (plan claimed backfill reuses `subscribeAfterAuth`, which is private to realtime.ts and not how server actions authenticate): INCORPORATED. Task 3 + threat model T-10-06 corrected: the backfill is a server action authenticated server-side; subscribeAfterAuth already gates the channel subscribe; RLS re-checks per request.
- MEDIUM (`createChatHydrationKey` includes only messages/read states — pagination key strategy unclear): INCORPORATED. Task 2 keeps the key on messages + read states and documents WHY pagination metadata rides the same payload (no key change).
- Suggestion (keep `refreshConversationAction` as a legacy fallback; assert reconnect no longer calls it): INCORPORATED as a deep-fallback-only retention + acceptance assertion.
- Related Plan 04 MEDIUM (offline UI depends on a "disconnected" status nothing sets): INCORPORATED here — this plan emits "disconnected" from the messages channel (RealtimeConnectionState already permits it at types.ts:8-12) so Plan 04's CONTEXT-locked calm offline/reconnect state is real, not dead code.

No findings deferred or rejected.
</review_incorporation>

<execution_context>
@/Users/franz/Library/Application Support/Claude/local-agent-mode-sessions/614fd504-8566-46b2-8256-66ded77bb604/43ed32e3-053c-413f-843e-d49e1f00f5e1/rpm/plugin_019FPGEfUV8XSdonegN1mWDs/workflows/execute-plan.md
@/Users/franz/Library/Application Support/Claude/local-agent-mode-sessions/614fd504-8566-46b2-8256-66ded77bb604/43ed32e3-053c-413f-843e-d49e1f00f5e1/rpm/plugin_019FPGEfUV8XSdonegN1mWDs/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-chat-message-loading-optimization/10-CONTEXT.md
@.planning/phases/10-chat-message-loading-optimization/10-RESEARCH.md
@.planning/phases/10-chat-message-loading-optimization/10-PATTERNS.md

<interfaces>
<!-- From Plan 01 (packages/core/src/chat-state) — the events/selectors to dispatch/read: -->

```
// ChatEvent variants (Plan 01)
{ type: "hydrateWindow"; conversationId; messages; readStates; hasMoreOlder; oldestCursor }
{ type: "olderMessagesRequested"; conversationId }
{ type: "olderPageLoaded"; conversationId; messages; hasMoreOlder; oldestCursor }
{ type: "olderPageLoadFailed"; conversationId }
// ChatConversationState.pagination: { oldestLoadedCursor: ChatMessageCursor | null; hasMoreOlder; isLoadingOlder }
type ChatMessageCursor = { createdAt: string; id: string };
// RealtimeConnectionState already includes "disconnected" (packages/core/src/chat-state/types.ts:8-12)
```

<!-- From Plan 02 (actions.ts) — the read actions to inject/dispatch: -->

```
loadOlderMessagesAction(input): Promise<{ status: "sent"|"notice"; values; notice?;
  messages?: ClientChatMessage[]; hasMoreOlder?: boolean }>
backfillMessagesAction(input): Promise<{ status; values; notice?;
  messages?: ClientChatMessage[]; needsReset?: boolean }>
loadNewestMessagesAction(input): Promise<{ status; values; notice?;
  messages?: ClientChatMessage[]; readStates?: ClientChatReadState[];
  hasMoreOlder?: boolean; oldestCursor?: { createdAt: string; id: string } | null }>
```

Existing store method precedent (chat-store.ts): every action is a one-line
`dispatchChatEvent({...})` wrapper (149-166); `hydrateConversation` (110-134) is the richer
precedent that also updates `hydrationKeys`; `createChatHydrationKey` (67-94) hashes ONLY messages +
read states. Existing selector precedent (chat-selectors.ts:59-64) returns a referentially-stable
fallback for SSR. Existing hook merge loop (use-chat-messages.ts:186-222) dispatches
`mergeRemoteMessage` per returned row. The realtime hook's three `onReconnected` callbacks all call
`refreshConversation()` today (use-chat-realtime.ts:52, 75, 87); the messages channel also sets
`setRealtimeStatus(..., "connecting"/"connected")` at 44/51.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pagination dispatch wrappers + selectors to the Zustand adapter and extend the web-only boundary test</name>
  <files>apps/web/app/(authenticated)/chat/store/chat-store.ts, apps/web/app/(authenticated)/chat/store/chat-selectors.ts, apps/web/app/(authenticated)/chat/store/chat-store.test.ts</files>
  <read_first>
    - apps/web/app/(authenticated)/chat/store/chat-store.ts (ChatStoreState interface 14-57; createChatStoreState 96-179; hydrateConversation richer method 110-134; one-line wrappers 135-169)
    - apps/web/app/(authenticated)/chat/store/chat-selectors.ts (whole file 1-64; stable-fallback comment 17-20; selectRealtimeStatusForConversation 59-64)
    - apps/web/app/(authenticated)/chat/store/chat-store.test.ts (authority-boundary describe block 54-89 — the CSTATE-03 conformance test)
    - packages/core/src/chat-state/types.ts (final event/pagination shapes from Plan 01)
  </read_first>
  <behavior>
    - store.hydrateWindow(conversationId, messages, readStates, hasMoreOlder, oldestCursor, hydrationKey?) dispatches the hydrateWindow event AND updates hydrationKeys when a key is passed (mirrors the existing hydrateConversation method).
    - store.requestOlderMessages / applyOlderPage / markOlderPageFailed dispatch olderMessagesRequested / olderPageLoaded / olderPageLoadFailed respectively (one-line wrappers).
    - selectHasMoreOlderForConversation returns pagination.hasMoreOlder ?? false; selectIsLoadingOlderForConversation returns pagination.isLoadingOlder ?? false; selectOldestCursorForConversation returns pagination.oldestLoadedCursor ?? null.
    - The authority-boundary test still passes: the store exposes no auth/role/assignment/Supabase-client/service-role fields, and the new pagination values are plain JSON-serializable client-cache data.
  </behavior>
  <action>
    In chat-store.ts, add to `ChatStoreState`: `hydrateWindow(conversationId, messages, readStates, hasMoreOlder: boolean, oldestCursor: ChatMessageCursor | null, hydrationKey?: string)`, `requestOlderMessages(conversationId)`, `applyOlderPage(conversationId, messages: ChatMessageState[], hasMoreOlder: boolean, oldestCursor: ChatMessageCursor | null)`, `markOlderPageFailed(conversationId)`. Implement `hydrateWindow` on the `hydrateConversation` (110-134) pattern — dispatch the `hydrateWindow` event via a `reduceChatState` `set(...)` and update `hydrationKeys` when `hydrationKey` is defined. Implement the other three as one-line `dispatchChatEvent({ type, ... })` wrappers like `mergeRemoteMessage` (149-151). Import `ChatMessageCursor` from `@fish/core/chat-state`. In chat-selectors.ts, add `selectHasMoreOlderForConversation`, `selectIsLoadingOlderForConversation`, `selectOldestCursorForConversation` following `selectRealtimeStatusForConversation`'s exact shape (read `selectConversationState(...)?.pagination.<field> ?? <stable-default>`); a `null`/boolean fallback is inherently stable so no module-level constant is needed (add one only if a returned object/array is introduced). In chat-store.test.ts, extend the authority-boundary describe block (54-89) with an assertion that after `applyOlderPage`/`hydrateWindow` the conversation's `pagination` contains only `oldestLoadedCursor`/`hasMoreOlder`/`isLoadingOlder` and no auth/role/assignment/token/service-role keys; add a test that `applyOlderPage` with an overlapping message id does not duplicate (reducer dedup surfaced through the store).
  </action>
  <verify>
    <automated>pnpm --filter @fish/web exec vitest run "app/(authenticated)/chat/store/chat-store.test.ts" && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hydrateWindow\|requestOlderMessages\|applyOlderPage\|markOlderPageFailed" apps/web/app/(authenticated)/chat/store/chat-store.ts` >= 8 (interface + implementation for each).
    - `grep -n "selectHasMoreOlderForConversation\|selectIsLoadingOlderForConversation\|selectOldestCursorForConversation" apps/web/app/(authenticated)/chat/store/chat-selectors.ts` shows all three exported.
    - The authority-boundary test still passes and now asserts pagination fields carry no auth/permission data.
    - A store test proves `applyOlderPage` dedups an overlapping id (message count unchanged).
    - `pnpm typecheck` passes.
  </acceptance_criteria>
  <done>The web adapter can drive and read pagination without leaking authority; boundary test proves CSTATE-03 still holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add loadOlderMessages + applyGapBackfill (with loadNewest reset fallback) to the message hook and switch initial hydration to hydrateWindow</name>
  <files>apps/web/app/(authenticated)/chat/hooks/use-chat-messages.ts</files>
  <read_first>
    - apps/web/app/(authenticated)/chat/hooks/use-chat-messages.ts (whole file 1-231; hydration effect 99-112; setMessages shim 114-136; refreshMessages cooldown-guard 142-184; refreshConversation merge loop 186-222; RefreshMessagesActionState 29-38)
    - apps/web/app/(authenticated)/chat/store/chat-store.ts (the new methods from Task 1; createChatHydrationKey 67-94)
    - apps/web/app/(authenticated)/chat/store/chat-selectors.ts (the new pagination selectors from Task 1)
    - apps/web/lib/services/supabase/types.ts (ClientChatData.hasMoreOlder/oldestCursor from Plan 02)
  </read_first>
  <behavior>
    - On mount/SSR-data-change the hook hydrates via hydrateWindow carrying chat.hasMoreOlder and chat.oldestCursor (falling back to hasMoreOlder=false / oldestCursor=null when absent), preserving the existing hydrationKey caching so re-entry does not refetch.
    - loadOlderMessages() returns Promise<void> (so Plan 04 can await it and restore scroll after the prepend paints — review HIGH 10-04). It is a no-op (resolved promise) when hasMoreOlder is false or a load is already in flight (ref guard, same idiom as refreshingMessageIdsRef); otherwise it dispatches requestOlderMessages, calls loadOlderMessagesAction with the current oldestLoadedCursor, and on success dispatches applyOlderPage(messages, hasMoreOlder, newOldestCursor); on failure/notice it dispatches markOlderPageFailed.
    - applyGapBackfill() calls backfillMessagesAction with the newest locally-known message's { createdAt, id }; on needsReset it calls loadNewestMessagesAction and re-hydrates the returned bounded window via hydrateWindow (messages, readStates, hasMoreOlder, oldestCursor) — the reset-to-newest-window fallback, never the unbounded refreshConversationAction (review HIGH 10-03); otherwise it merges each returned row through the existing mergeRemoteMessage dispatch (reusing the single merge path).
    - refreshConversation (the prop the realtime hook calls today) is re-pointed to applyGapBackfill so reconnect uses the bounded path, not the full refetch; the injected refreshConversationAction is retained ONLY as a deep fallback when no backfill/reset actions are wired, and reconnect no longer calls it in the normal path (review Suggestion 10-03).
  </behavior>
  <action>
    Add `loadOlderMessagesAction?`, `backfillMessagesAction?`, and `loadNewestMessagesAction?` to `UseChatMessagesOptions` typed as injected server actions (add a `LoadOlderMessagesActionState` interface mirroring `RefreshMessagesActionState` plus `hasMoreOlder?`/`needsReset?`/`oldestCursor?`/`readStates?`). Replace the `hydrateConversation(...)` call in the SSR hydration effect (99-112) with `hydrateWindow(chat.conversationId, initialMessages, initialReadStates, chat.hasMoreOlder ?? false, chat.oldestCursor ?? null, hydrationKey)`; keep the `setMessages` shim (114-136) on `hydrateConversation` (it must preserve pagination — Plan 01's reducer does this via its spread). Leave `createChatHydrationKey` keyed off messages + read states only (chat-store.ts:67) — pagination metadata (hasMoreOlder/oldestCursor) is derived server-side from the same SSR payload and travels with it, so it needs no place in the key; add a WHY comment so re-entry caching is provably unchanged (review MEDIUM 10-03). Read the current cursor/loading via the new selectors (`selectOldestCursorForConversation`, `selectIsLoadingOlderForConversation`, `selectHasMoreOlderForConversation`) and the store methods `requestOlderMessages`/`applyOlderPage`/`markOlderPageFailed`. Implement `loadOlderMessages` as a `useCallback` returning `Promise<void>`, with an `isLoadingOlderRef` in-flight guard (same ref-guard shape as `refreshingMessageIdsRef`), guarding on `hasMoreOlder` (return an already-resolved promise on no-op); call the injected `loadOlderMessagesAction({ conversationId, cursor })`, then dispatch `applyOlderPage` on `status === "sent"` (using `result.messages` and `result.hasMoreOlder`, deriving the new oldest cursor from the first returned row) or `markOlderPageFailed` otherwise; resolve the promise only after the dispatch so callers can restore scroll afterward. Implement `applyGapBackfill` as a `useCallback` that computes the newest known message from `messageIdsRef`/`messages`, calls `backfillMessagesAction({ conversationId, afterCreatedAt, afterMessageId })`, and on `needsReset` calls `loadNewestMessagesAction({ conversationId })` and re-hydrates via `hydrateWindow(chat.conversationId, result.messages, result.readStates ?? [], result.hasMoreOlder ?? false, result.oldestCursor ?? null)`, else loops `dispatchChatEvent({ type: "mergeRemoteMessage", message })` over the backfilled rows. Re-point `refreshConversation` to call `applyGapBackfill` (keep the old full-refresh action prop only as the deep fallback when no backfill action is injected). Return `loadOlderMessages` (Promise-returning), `applyGapBackfill`, `hasMoreOlder`, `isLoadingOlder` alongside the existing returns.
  </action>
  <verify>
    <automated>pnpm --filter @fish/web exec vitest run "app/(authenticated)/chat/chat-client.test.tsx" "app/(authenticated)/chat/store/chat-store.test.ts" && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The SSR hydration effect calls `hydrateWindow` (not `hydrateConversation`) with hasMoreOlder/oldestCursor sourced from `chat`; `createChatHydrationKey` still hashes only messages + read states (a WHY comment records that pagination metadata rides the same payload).
    - `loadOlderMessages` returns a `Promise<void>`, dispatches `requestOlderMessages` before the fetch and `applyOlderPage`/`markOlderPageFailed` after; a second call while a load is in flight is a no-op (in-flight ref guard present).
    - `applyGapBackfill` branches on `needsReset` → calls `loadNewestMessagesAction` then `hydrateWindow` (bounded reset, never the unbounded refreshConversationAction) vs a `mergeRemoteMessage` merge loop; `refreshConversation` now routes through `applyGapBackfill`.
    - The hook returns `loadOlderMessages` (Promise-returning), `applyGapBackfill`, `hasMoreOlder`, `isLoadingOlder`.
    - Existing chat-client tests still pass (no behavior regression to send/optimistic/realtime merge); `pnpm typecheck` passes.
  </acceptance_criteria>
  <done>The hook can page older history through one merge path and backfill a reconnect gap without a full refetch; an oversized gap resets to the bounded newest window.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Consolidate the triplicated reconnect refetch into one per-channel-guarded coalesced backfill and emit a "disconnected" status</name>
  <files>apps/web/app/(authenticated)/chat/hooks/use-chat-realtime.ts, apps/web/app/(authenticated)/chat/realtime.ts</files>
  <read_first>
    - apps/web/app/(authenticated)/chat/hooks/use-chat-realtime.ts (whole file 1-201; three onReconnected callbacks calling refreshConversation() at 50-54, 74-77, 86-89; typing/voice ref-guard precedent 92-156; setRealtimeStatus "connecting"/"connected" at 44/51)
    - apps/web/app/(authenticated)/chat/realtime.ts (subscribeToConversationMessages 152-196 — the `.subscribe((status) => { if (status === "SUBSCRIBED") onReconnected?.() })` at 189-193; subscribeAfterAuth 68-101 is PRIVATE to this module and gates the channel on auth before it ever subscribes; the read/reaction helpers' subscribe callbacks at 219-223 / 258-262)
    - apps/web/app/(authenticated)/chat/hooks/use-chat-messages.ts (applyGapBackfill/refreshConversation from Task 2 — the prop this hook receives)
    - packages/core/src/chat-state/types.ts (RealtimeConnectionState already includes "disconnected" at 8-12 — no type change needed, only emission)
    - .planning/phases/10-chat-message-loading-optimization/10-RESEARCH.md (Pitfall #2 triplicated full-refetch; Pattern 2 coalesced backfill + skip-first-SUBSCRIBED; Security V6 stale-token wait-for-session)
  </read_first>
  <behavior>
    - The messages, read-states, and reaction channels no longer each call a full refreshConversation() on reconnect; near-simultaneous reconnects trigger exactly ONE backfill (coalesced via a shared in-flight lock).
    - Each of the three channels' FIRST post-mount SUBSCRIBED is treated as that channel's initial subscribe and does NOT backfill (SSR data is already current). A single shared boolean is insufficient because three channels each fire an initial SUBSCRIBED — track first-subscribe PER CHANNEL so none of the three initial callbacks backfills; only a channel's SECOND+ SUBSCRIBED (a genuine re-subscribe after a drop) is eligible to backfill (review HIGH 10-03).
    - The single reconnect handler routes through the bounded applyGapBackfill (from Task 2), inheriting its loadNewestMessagesAction reset-to-window fallback; realtime live-insert merge (mergeRemoteMessage) is unchanged.
    - When the messages channel reports CHANNEL_ERROR/TIMED_OUT/CLOSED, the store realtime status becomes "disconnected" (the calm-offline signal Plan 04 renders); SUBSCRIBED restores "connected". The type RealtimeConnectionState already permits "disconnected" (no Plan 01 change).
    - The backfill is a Server Action (backfillMessagesAction/loadNewestMessagesAction) that authenticates server-side via getLocalFallbackContext/getAccessToken; it does NOT reuse the client-only, module-private subscribeAfterAuth helper. onReconnected only fires after the channel has (re)subscribed, which subscribeAfterAuth already gates on a fresh session, and RLS re-checks the session per request — so a stale token fails toward missing data, never disclosure (corrected mitigation, review MEDIUM 10-03).
  </behavior>
  <action>
    In use-chat-realtime.ts, introduce one coalescing reconnect handler in this hook (it owns all three subscriptions): add a `backfillInFlightRef` (`useRef<Promise<void> | null>`) and a PER-CHANNEL first-subscribe tracker — either `useRef<Set<string>>(new Set())` keyed by a channel id ("messages" | "reads" | "reactions") or three separate boolean refs. Replace each of the three `() => { ...; void refreshConversation(); }` onReconnected callbacks (50-54, 74-77, 86-89) with a shared `handleReconnected(channelKey)` that: if this channelKey has not been seen before, records it and returns WITHOUT backfilling (initial subscribe, SSR fresh); otherwise (a genuine re-subscribe) coalesces — if `backfillInFlightRef.current` is set, return; else assign `backfillInFlightRef.current = applyGapBackfill().finally(() => { backfillInFlightRef.current = null; })`. Wire the messages channel's callback to `handleReconnected("messages")`, read-states to `handleReconnected("reads")`, reactions to `handleReconnected("reactions")`. Keep `setRealtimeStatus(chat.conversationId, "connected")` on the messages channel's SUBSCRIBED path. Change the hook option from `refreshConversation` to accept the bounded `applyGapBackfill` (keep a `refreshConversation` deep-fallback name only if no backfill action is wired); update `UseChatRealtimeOptions`; the caller wiring in Plan 04's component passes `applyGapBackfill`. Do not touch the typing/voice/read-state LIVE callbacks other than removing their redundant reconnect refetch.

    In realtime.ts, extend `subscribeToConversationMessages` so its `.subscribe((status) => { ... })` (189-193) ALSO surfaces terminal/error states: add an optional `onDisconnected?: () => void` parameter and call it when `status === "CHANNEL_ERROR" || status === "TIMED_OUT" || status === "CLOSED"` (keep the existing `if (status === "SUBSCRIBED") onReconnected?.()`). In use-chat-realtime.ts's messages effect, pass `onDisconnected: () => setRealtimeStatus(chat.conversationId, "disconnected")`. Do NOT alter subscribeAfterAuth (the private wait-for-session gate stays as-is) and do NOT add error handling to the presence/typing/voice channels — only the messages channel drives the user-visible status. The read-states and reaction subscribe helpers keep their SUBSCRIBED-only signature (only their onReconnected wiring changes, in the hook).
  </action>
  <verify>
    <automated>pnpm --filter @fish/web exec vitest run "app/(authenticated)/chat/chat-client.test.tsx" && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "refreshConversation()" apps/web/app/(authenticated)/chat/hooks/use-chat-realtime.ts` is 0 (the three full-refetch calls are gone); reconnect no longer calls refreshConversationAction in the normal path.
    - A single shared `handleReconnected(channelKey)` exists, guarded by `backfillInFlightRef` and a PER-CHANNEL first-subscribe tracker; all three channels' initial post-mount SUBSCRIBED produce ZERO backfills, and three simultaneous genuine re-subscribes produce exactly ONE backfill call.
    - `grep -q "CHANNEL_ERROR" apps/web/app/(authenticated)/chat/realtime.ts` — subscribeToConversationMessages surfaces terminal states via `onDisconnected`; the messages effect sets realtime status to "disconnected" on those.
    - The messages channel still sets realtime status to "connected" on SUBSCRIBED; live-insert `mergeRemoteMessage` path is unchanged.
    - `pnpm typecheck` passes and existing chat-client realtime tests stay green.
  </acceptance_criteria>
  <done>One genuine reconnect = one bounded backfill (per-channel first-subscribe guarded); the redundant triple full-history refetch is eliminated; the messages channel now emits "disconnected" for Plan 04's calm offline UI (CLOAD-06 / Pitfall #2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Realtime WS → client reducer | Realtime INSERT/UPDATE payloads (already RLS-filtered server-side) enter the client merge path. |
| Client → PostgREST (backfill/reset read) | The reconnect backfill/reset issue bounded reads via Server Actions authenticated server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering / Information Disclosure | Backfill/reset fetch on a stale/expired token after wake-from-sleep | mitigate | The backfill/reset are Server Actions authenticated server-side (getLocalFallbackContext/getAccessToken), NOT the client-only private subscribeAfterAuth helper. They only fire after the channel re-subscribes (which subscribeAfterAuth already gates on a fresh session), and RLS re-checks the session per request — so a stale token fails toward missing data, never toward other users' data. |
| T-10-07 | Denial of Service | Reconnect storm triggering concurrent backfills | mitigate | Coalesce all three channels' reconnects behind one `backfillInFlightRef` lock and treat each channel's first SUBSCRIBED as an initial subscribe (per-channel tracking), replacing three concurrent unbounded full-history refetches with at most one bounded backfill. |
| T-10-08 | Spoofing | Client store making an authorization decision | accept | The store/hooks make no auth/membership/assignment decision (CSTATE-03 boundary reasserted by the Task 1 boundary test); server RLS/Edge Functions remain authoritative. |
| T-10-SC | Tampering | npm/pnpm installs | accept | Zero new packages installed this plan. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (new symbols created by this plan)

- Store (chat-store.ts): `hydrateWindow`, `requestOlderMessages`, `applyOlderPage`, `markOlderPageFailed` methods on `ChatStoreState`.
- Selectors (chat-selectors.ts): `selectHasMoreOlderForConversation`, `selectIsLoadingOlderForConversation`, `selectOldestCursorForConversation`.
- Message hook (use-chat-messages.ts): `LoadOlderMessagesActionState` interface; `loadOlderMessages` (Promise-returning), `applyGapBackfill` callbacks; `isLoadingOlderRef` guard; `loadOlderMessagesAction`/`backfillMessagesAction`/`loadNewestMessagesAction` option props; hydrateWindow-based initial hydration; returned `hasMoreOlder`/`isLoadingOlder`.
- Realtime hook (use-chat-realtime.ts): shared `handleReconnected(channelKey)`, `backfillInFlightRef`, per-channel first-subscribe tracker; `applyGapBackfill` option (replacing the full `refreshConversation` reconnect wiring); `onDisconnected` → setRealtimeStatus("disconnected") on the messages effect.
- Realtime helpers (realtime.ts): `onDisconnected?` parameter on `subscribeToConversationMessages` surfacing CHANNEL_ERROR/TIMED_OUT/CLOSED.
- Tests: extended cases in `chat-store.test.ts`.
</artifacts_produced>

<verification>
- `pnpm --filter @fish/web exec vitest run "app/(authenticated)/chat"` passes (store + client component tests).
- `pnpm typecheck` passes.
- `grep -c "refreshConversation()" apps/web/app/(authenticated)/chat/hooks/use-chat-realtime.ts` is 0.
- `grep -q "CHANNEL_ERROR" apps/web/app/(authenticated)/chat/realtime.ts` (disconnected emission present).
</verification>

<success_criteria>
- CLOAD-02: realtime inserts still merge in place through mergeRemoteMessage (no full reload).
- CLOAD-03: loadOlderMessages pages older history through applyOlderPage.
- CLOAD-05: every source (optimistic/realtime/paginated/backfill) funnels through the one reducer merge path.
- CLOAD-06: a genuine reconnect = one coalesced bounded backfill (per-channel first-subscribe guarded) with a bounded loadNewestMessagesAction reset-to-window fallback; the messages channel emits "disconnected" for the calm offline UI.
</success_criteria>

<output>
Create `.planning/phases/10-chat-message-loading-optimization/10-03-SUMMARY.md` when done.
</output>
