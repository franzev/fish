---
phase: 10-chat-message-loading-optimization
plan: 05
type: execute
wave: 4
depends_on: ["10-04"]
files_modified:
  - apps/web/app/globals.css
  - apps/web/components/chat/message-row/community-message-row-layout.tsx
  - apps/web/components/chat/message-row/message-rows-skeleton.tsx
  - apps/web/components/chat/message-row/index.ts
  - apps/web/components/chat/index.ts
  - apps/web/app/(authenticated)/chat/chat-client.tsx
  - apps/web/app/(authenticated)/chat/chat-client.test.tsx
autonomous: true
gap_closure: true
requirements: [CLOAD-03, CLOAD-04]
must_haves:
  truths:
    - "While an older page is loading, the reserved pagination slot shows only a two-row message-shaped skeleton: no visible loading copy, load button, Button spinner, or generic full-width loading bars"
    - "The first skeleton row represents a new community author group with a 32px avatar, author, timestamp, and message-content placeholders; the second represents a same-author continuation with the same avatar spacer and content alignment as a final row"
    - "Final community messages and loading skeletons use one shared CommunityMessageRowLayout primitive, so gap-md, px-md, py-sm/py-2xs, avatar placement, content start, and fluid min-w-0 geometry cannot drift independently"
    - "The pagination slot uses one fixed h-pagination-slot geometry backed by a source-verified 112px semantic token across idle, loading, and failure states, and the loading transition does not introduce a second primary action, programmatically move focus, or steal focus when loading completes"
    - "The message log exposes aria-busy during an older-page request, visual skeleton atoms are aria-hidden, and one sr-only status announces 'Loading earlier messages' without adding visible copy"
    - "The opacity-only skeleton pulse remains governed by the existing global reduced-motion clamp; no shimmer, transform, stagger, raw color, arbitrary visual utility, or new dependency is introduced"
  artifacts:
    - path: "apps/web/app/globals.css"
      provides: "Source-verified 112px fixed pagination-slot size token that contains the tallest idle/loading/error state"
      contains: "--size-pagination-slot: 112px"
    - path: "apps/web/components/chat/message-row/community-message-row-layout.tsx"
      provides: "Shared final-row/skeleton geometry primitive for community messages"
      contains: "CommunityMessageRowLayout"
    - path: "apps/web/components/chat/message-row/message-rows-skeleton.tsx"
      provides: "Two-row one-to-one older-history loading skeleton"
      contains: "MessageRowsSkeleton"
    - path: "apps/web/app/(authenticated)/chat/chat-client.tsx"
      provides: "Mutually exclusive idle/loading/error pagination states and aria-busy wiring"
      contains: "MessageRowsSkeleton"
    - path: "apps/web/app/(authenticated)/chat/chat-client.test.tsx"
      provides: "Focused anatomy, mutual-exclusion, stable-slot, and accessibility regression coverage"
  key_links:
    - from: "apps/web/app/(authenticated)/chat/chat-client.tsx final community message rendering"
      to: "apps/web/components/chat/message-row/community-message-row-layout.tsx"
      via: "Every final community row delegates its outer row/avatar/content geometry to CommunityMessageRowLayout"
      pattern: "CommunityMessageRowLayout"
    - from: "apps/web/components/chat/message-row/message-rows-skeleton.tsx"
      to: "apps/web/components/chat/message-row/community-message-row-layout.tsx"
      via: "Both skeleton rows render through the same shared primitive as final community rows"
      pattern: "CommunityMessageRowLayout"
    - from: "apps/web/app/(authenticated)/chat/chat-client.tsx pagination loading branch"
      to: "apps/web/components/chat/message-row/message-rows-skeleton.tsx"
      via: "isLoadingOlder replaces the ghost button with MessageRowsSkeleton inside the unchanged reserved slot"
      pattern: "isLoadingOlder|MessageRowsSkeleton"
---

<objective>
Close the single actionable Phase 10 UAT gap by replacing the current pair of generic loading bars with a one-to-one, message-shaped skeleton that shares its geometry with the final community message rows.

Purpose: Make older-page loading visually seamless and layout-stable under CLOAD-03/CLOAD-04 while preserving the already-passing pagination, scroll restoration, retry, offline, and reconnect behavior. The UAT issue is cosmetic and narrowly scoped; blocked UAT Tests 3 and 4 are verification constraints, not implementation failures, so this plan does not change reconnect/backfill behavior.
Output: A shared community-message row layout primitive, a two-row named skeleton component, ChatClient wiring with visible-state mutual exclusion and busy semantics, and focused regression tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/ui-ux-agent-guidelines.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-chat-message-loading-optimization/10-CONTEXT.md
@.planning/phases/10-chat-message-loading-optimization/10-RESEARCH.md
@.planning/phases/10-chat-message-loading-optimization/10-PATTERNS.md
@.planning/phases/10-chat-message-loading-optimization/10-VERIFICATION.md
@.planning/phases/10-chat-message-loading-optimization/10-UAT.md
@.planning/phases/10-chat-message-loading-optimization/10-UI-SPEC.md
@.planning/phases/10-chat-message-loading-optimization/10-04-SUMMARY.md

<interfaces>
- `chat-client.tsx` currently renders final community rows with `-mx-md items-start gap-md px-md py-2xs`, upgrading a new author group after the first row to `py-sm`; the avatar or transparent spacer is `size-8 shrink-0`; the content column is `min-w-0 flex-1 items-start`.
- `MessageMeta` uses `mb-2xs flex items-baseline gap-xs`; author is `text-ui-sm font-medium leading-none`, timestamp is `text-ui-xs`. Skeleton metadata must occupy the same baseline relationship without rendering fake text.
- `Avatar size="sm"` is exactly `size-8`; skeleton Row 1 uses a visible `size-8 rounded-pill bg-surface-2` atom, and Row 2 uses a transparent `size-8 shrink-0` spacer.
- `load-older-slot` currently uses `min-h-pagination-slot` with `--size-pagination-slot: 104px`, but source geometry shows that is insufficient for the new skeleton: the author row is approximately 66.5px (metadata/body line boxes plus `py-sm`), the continuation row approximately 28.3px (`text-ui-sm` plus `py-2xs`), and the wrapper retains `pb-md` (16px), for approximately 110.8px total. Raise the semantic token to 112px and use fixed `h-pagination-slot` geometry so idle/loading/error cannot expand differently.
- `animate-skeleton-pulse` already exists in globals.css and is opacity-only; the global `prefers-reduced-motion` rule clamps duration and iteration count. Reuse it unchanged.
- `Button loading` creates an `animate-spin` child. The older-history loading branch must not render a Button at all and the idle `Load earlier messages` button must not receive `loading={isLoadingOlder}`.
- The sentinel and `loadOlderAndPreserveScroll` callback from Plan 10-04 remain unchanged; both trigger paths continue to share that callback.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared community-row geometry and build the one-to-one message skeleton</name>
  <files>apps/web/app/globals.css, apps/web/components/chat/message-row/community-message-row-layout.tsx, apps/web/components/chat/message-row/message-rows-skeleton.tsx, apps/web/components/chat/message-row/index.ts, apps/web/components/chat/index.ts, apps/web/app/(authenticated)/chat/chat-client.tsx</files>
  <read_first>
    - apps/web/app/(authenticated)/chat/chat-client.tsx (current pagination slot at the start of the log and the complete final community-row branch; preserve all direct-message, action, reaction, reply, status, and grouping behavior)
    - apps/web/components/chat/avatar/avatar.tsx (`size="sm"` is the canonical 32px avatar geometry)
    - apps/web/components/chat/message-meta/message-meta.tsx (canonical author/timestamp baseline, gap, and bottom margin)
    - apps/web/components/chat/index.ts (barrel export convention)
    - apps/web/components/chat/typing-indicator/typing-indicator.tsx (named, non-focusable chat-component convention)
    - apps/web/app/globals.css (`--size-pagination-slot`, spacing/text tokens, `animate-skeleton-pulse`, and the global reduced-motion clamp; update only the pagination-slot token after applying the source-grounded tallest-state calculation below)
    - .planning/phases/10-chat-message-loading-optimization/10-UI-SPEC.md (Loading Skeleton Contract, Interaction Contract, State Matrix, and Verification Contract are binding)
    - .planning/phases/10-chat-message-loading-optimization/10-UAT.md (Test 1 report is the only actionable gap)
    - docs/ui-ux-agent-guidelines.md (component-state stability, skeleton loading, accessibility, token-only styling)
  </read_first>
  <action>
    In `apps/web/app/globals.css`, update `--size-pagination-slot` from 104px to 112px. This is the smallest whole token value above the source-grounded tallest-state total: author skeleton row ~66.5px + continuation row ~28.3px + the existing slot `pb-md` 16px = ~110.8px. Keep `--spacing-pagination-slot: var(--size-pagination-slot)` and all other tokens unchanged. This deliberate semantic-token adjustment is required before replacing the slot's minimum height with a fixed height; do not add a one-off component height.

    Create `apps/web/components/chat/message-row/community-message-row-layout.tsx` with a named `CommunityMessageRowLayout` export and a props interface accepting `avatarSlot`, `startsGroup`, `hasPrecedingRow`, `interactive`, `children`, and ordinary div attributes. It is non-focusable, so do not use `forwardRef`. The component must own the shared outer geometry `relative flex -mx-md items-start gap-md px-md py-2xs`; add `py-sm` only when `hasPrecedingRow && startsGroup`; add `group transition-colors hover:bg-surface` only for `interactive=true`. It must render `avatarSlot` followed by one content wrapper with `flex min-w-0 flex-1 flex-col items-start`. Use `cn()` for conditional classes and add `data-layout="community-message-row"` so tests can prove both final and skeleton rows use the same primitive.

    Create `apps/web/components/chat/message-row/message-rows-skeleton.tsx` with a named `MessageRowsSkeleton` export composed from exactly two `CommunityMessageRowLayout` instances. Row 1 is a new-author row (`startsGroup=true`, `hasPrecedingRow=true`, `interactive=false`) containing: one visible `size-8 shrink-0 rounded-pill bg-surface-2 animate-skeleton-pulse` avatar atom; one metadata line using the same `mb-2xs flex items-baseline gap-xs` wrapper as `MessageMeta`, with distinct author and timestamp atoms; and one message-content atom inside the same `text-ui-sm` line geometry as a final community message. Row 2 is a continuation (`startsGroup=false`, `hasPrecedingRow=true`, `interactive=false`) containing a transparent `size-8 shrink-0` avatar spacer and one content atom aligned to Row 1's content start. Use only existing named token utilities (`gap-md`, `px-md`, `py-sm`, `py-2xs`, `gap-xs`, `mb-2xs`, `size-8`, `bg-surface-2`, `rounded-pill`/existing radii, text tokens, and named width/spacing utilities); do not add raw colors, bracket/arbitrary visual utilities, shimmer/gradient/transform/stagger motion, or a dependency. Mark the visual skeleton subtree `aria-hidden="true"`. Add stable data attributes for `skeleton-row-author`, `skeleton-row-continuation`, `skeleton-avatar`, `skeleton-author`, `skeleton-timestamp`, and both `skeleton-content` atoms.

    Add `apps/web/components/chat/message-row/index.ts` exporting both named components, then export that folder from `apps/web/components/chat/index.ts`.

    In `chat-client.tsx`, refactor only the community branch of each final message row so its outer avatar/content geometry is rendered by `CommunityMessageRowLayout`; direct-message markup and all message-body/action/reaction/reply/status behavior remain unchanged. Pass the real `Avatar size="sm"` or transparent `size-8` spacer through `avatarSlot`, and pass `startsCommunityGroup`, `index > 0`, and `interactive=true`. The final row and skeleton must therefore share the exact layout component rather than two copied class strings.

    Replace the current generic two-bar `load-older-skeleton` contents with `MessageRowsSkeleton` inside the existing `load-older-slot`. Change the wrapper from `min-h-pagination-slot` to `h-pagination-slot` so the source-verified 112px border-box height is invariant across idle/loading/error; retain its existing `pb-md`, centering, gap, DOM identity, and state contents. While `isLoadingOlder`, render no visible loading string, no `Load earlier messages`/`Try again` button, no `Alert`, and no Button spinner; remove `loading={isLoadingOlder}` from the idle ghost button. Set the `role="log"` container's `aria-busy` to true only while `isLoadingOlder`, and add exactly one `role="status"`/`sr-only` node with the text `Loading earlier messages` for assistive technology. Keep skeleton visual atoms `aria-hidden`. Do not call `.focus()` or otherwise move focus programmatically when loading begins, and do not focus the log, skeleton, status, or newly prepended messages when loading completes; browser fallback after removal of the clicked button is acceptable. Leave the sentinel, failure gate, retry callback, scroll restoration, offline banner, reconnect pill, and one-primary-action behavior untouched.
  </action>
  <verify>
    <automated>pnpm --filter @fish/web exec vitest run "app/(authenticated)/chat/chat-client.test.tsx" &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `community-message-row-layout.tsx` exports `CommunityMessageRowLayout`, contains `data-layout="community-message-row"`, and owns `gap-md`, `px-md`, `py-2xs`, conditional `py-sm`, avatar-slot-before-content ordering, and `min-w-0 flex-1` content geometry; callers supply the canonical `size-8` avatar or spacer through `avatarSlot`.
    - `message-rows-skeleton.tsx` exports `MessageRowsSkeleton` and renders exactly two `CommunityMessageRowLayout` instances: one author row and one continuation row.
    - The skeleton contains distinct `skeleton-avatar`, `skeleton-author`, `skeleton-timestamp`, and two `skeleton-content` atoms; the continuation row contains a transparent `size-8` spacer.
    - The final community-message branch in `chat-client.tsx` imports and renders `CommunityMessageRowLayout`; the loading branch imports and renders `MessageRowsSkeleton`.
    - `apps/web/app/globals.css` defines `--size-pagination-slot: 112px`, derived from the ~110.8px tallest loading-state geometry, and retains `--spacing-pagination-slot: var(--size-pagination-slot)` with no other token changes.
    - `load-older-slot` uses fixed `h-pagination-slot` (not `min-h-pagination-slot`), so idle, loading, and error share the same 112px border-box height; the idle button remains `variant="ghost"`, has no `loading` prop, and still calls `loadOlderAndPreserveScroll`.
    - While `isLoadingOlder`, no Button/Alert is rendered inside `load-older-slot`; the visual subtree is aria-hidden, the log has `aria-busy="true"`, and the only loading copy is the sr-only `Loading earlier messages` status.
    - Loading start performs no programmatic focus movement, and loading completion does not focus the log, skeleton/status region, or prepended messages; no promise is made that the removed trigger button remains the active element while loading.
    - No new `variant="primary"`, `animate-spin`, shimmer/gradient, raw color, arbitrary visual utility, numeric spacing utility, package dependency, or `tailwind.config.js` is introduced.
    - Focused chat tests and `pnpm typecheck` pass.
  </acceptance_criteria>
  <done>Loading older history now renders a two-row, structurally shared message skeleton as the only visible loading treatment, with stable geometry and accessible busy semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Lock skeleton anatomy, mutual exclusion, shared geometry, and stable dimensions with focused tests</name>
  <files>apps/web/app/(authenticated)/chat/chat-client.test.tsx</files>
  <read_first>
    - apps/web/app/(authenticated)/chat/chat-client.test.tsx (existing deferred older-page test around `load-older-skeleton`, shared-callback sentinel/button tests, and WR-07 idle/loading/error slot identity test)
    - apps/web/app/(authenticated)/chat/chat-client.tsx (Task 1 final markup and data attributes)
    - apps/web/components/chat/message-row/community-message-row-layout.tsx (Task 1 shared geometry contract)
    - apps/web/components/chat/message-row/message-rows-skeleton.tsx (Task 1 skeleton anatomy contract)
    - apps/web/app/globals.css (Task 1's deliberate `--size-pagination-slot: 112px` token and its generated `h-pagination-slot` utility)
    - apps/web/components/ui/button/button.tsx (`loading` creates the spinner that this loading state must not render)
    - .planning/phases/10-chat-message-loading-optimization/10-UI-SPEC.md (focused test requirements)
    - .planning/phases/10-chat-message-loading-optimization/10-UAT.md (reported visible-loading gap wording)
  </read_first>
  <action>
    Replace the weak existing test that only checks `load-older-skeleton` appears/disappears with focused assertions against a deferred `loadOlderMessagesAction`. During the unresolved request, assert the unchanged `load-older-slot` contains exactly two nodes with `data-layout="community-message-row"`; Row 1 contains one visible avatar atom plus distinct author, timestamp, and content atoms; Row 2 contains the transparent avatar spacer plus one content atom. Assert Row 1 and a rendered final new-author community row share the geometry tokens `gap-md`, `px-md`, `py-sm`, and the same `data-layout` primitive; assert Row 2 and a rendered final continuation row share `gap-md`, `px-md`, `py-2xs`, `size-8` spacer alignment, and `min-w-0 flex-1` content structure.

    In the same loading state, assert mutual exclusion: no button named `Load earlier messages`, no `Try again` button, no `Alert`, and no `.animate-spin`. Assert there is exactly one DOM text match beginning with `Loading`, that it is the `role="status"` node with exact text `Loading earlier messages` and class `sr-only`, and that there is no second loading copy (`Loading messages`, `Please wait`, or similar) outside that status. Assert the `role="log"` has `aria-busy="true"` and skeleton visual nodes are aria-hidden. After resolving the deferred action, assert the skeleton, status, and busy state disappear and the returned state renders normally. Assert loading completion does not programmatically focus the log, skeleton/status region, or a prepended message; do not assert that the removed trigger button remains focused during the pending request because browser focus fallback is expected when that node leaves the DOM.

    Extend the existing WR-07 reserved-slot test rather than duplicating it: preserve the same DOM `load-older-slot` node across idle/loading/error and assert `h-pagination-slot` is present and `min-h-pagination-slot` is absent in every state. Add a source-contract assertion for `apps/web/app/globals.css` that proves the generated semantic utility is backed by `--size-pagination-slot: 112px` and `--spacing-pagination-slot: var(--size-pagination-slot)`; this locks both the fixed-height class and the verified token value instead of inferring layout stability from DOM identity alone. Keep the existing sentinel/button shared-callback tests green to prove the visual correction did not change the scroll-preserving load paths. Do not add a browser-preview/dev-server step and do not create implementation changes for blocked UAT Tests 3 or 4.
  </action>
  <verify>
    <automated>pnpm --filter @fish/web exec vitest run "app/(authenticated)/chat/chat-client.test.tsx" &amp;&amp; pnpm --filter @fish/web exec vitest run "app/(authenticated)/chat" &amp;&amp; pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - A focused deferred-load test asserts two shared-layout skeleton rows and all required atoms: avatar, author, timestamp, two message-content placeholders, and continuation avatar spacer.
    - The focused test proves the author skeleton/final row share `data-layout="community-message-row"` plus `gap-md px-md py-sm`, and continuation skeleton/final rows share the same primitive plus `gap-md px-md py-2xs` and `size-8` spacer alignment.
    - The loading-state test asserts the only loading text node is the exact `Loading earlier messages` status carrying `sr-only`; it also asserts zero pagination/retry buttons, zero Alert, and zero `.animate-spin` within `load-older-slot`.
    - The loading-state test asserts `aria-busy="true"` on the log, one sr-only `Loading earlier messages` status, and aria-hidden visual skeletons; it asserts busy/skeleton state clears after resolution.
    - The extended slot-identity test proves the same `load-older-slot` DOM node carries fixed `h-pagination-slot` and never `min-h-pagination-slot` through idle, loading, and error, while a source-contract assertion locks `--size-pagination-slot: 112px` and its spacing alias.
    - The loading-completion test proves no focus is programmatically moved into the log, skeleton/status region, or prepended rows, without claiming that a removed trigger button can remain focused.
    - Existing sentinel and button tests still prove both loading paths call the same scroll-preserving callback, and the focused/full chat tests, lint, typecheck, and production build all pass.
  </acceptance_criteria>
  <done>The UAT correction is regression-locked: skeleton-only loading has one-to-one anatomy, shared final-row geometry, stable reserved dimensions, and accessible non-visible status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendered chat data → shared row layout | Existing message author/time/body data continues through the existing `Avatar`, `MessageMeta`, and `MessageBody` components; the shared layout primitive controls geometry only. |
| Local pagination state → loading DOM | `isLoadingOlder` selects mutually exclusive static skeleton markup; it does not authorize reads, alter the cursor, or cross a server boundary. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-11 | Tampering / XSS | `CommunityMessageRowLayout` children | low | mitigate | Keep message rendering in the existing `MessageBody`/`MessageMeta`/`Avatar` path; the new component accepts React nodes for layout only and introduces no raw HTML, `dangerouslySetInnerHTML`, URL parsing, or interpolation into attributes. |
| T-10-12 | Information Disclosure | Skeleton/loading status | low | accept | Skeleton atoms and the exact sr-only string `Loading earlier messages` are static and reveal no message body, participant identity, cursor, count, account, or authorization state. |
| T-10-13 | Denial of Service | Loading render loop | low | mitigate | Preserve Plan 10-04's one-request-per-conversation guard and shared sentinel/button callback; this plan changes presentation only and does not add observers, timers, network calls, or retry loops. |
| T-10-SC | Tampering | Dependency supply chain | low | accept | Add no package and do not modify package manifests or the pnpm lockfile. |

No high-severity threat is introduced. If implementation reveals a high-severity issue, stop execution and block completion until it is remediated and the security gate re-runs.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

- New path `apps/web/components/chat/message-row/community-message-row-layout.tsx` with named component `CommunityMessageRowLayout` and its props interface; owns the shared community final-row/skeleton geometry.
- New path `apps/web/components/chat/message-row/message-rows-skeleton.tsx` with named component `MessageRowsSkeleton`; owns the two deterministic skeleton rows and their atom data attributes.
- New path `apps/web/components/chat/message-row/index.ts` exporting both message-row components.
- Updated `apps/web/components/chat/index.ts` barrel export for `message-row`.
- Updated `apps/web/app/(authenticated)/chat/chat-client.tsx` final-community-row wiring, skeleton-only pagination loading branch, `aria-busy`, and one sr-only loading status.
- Updated `apps/web/app/(authenticated)/chat/chat-client.test.tsx` focused skeleton anatomy, shared-geometry, mutual-exclusion, accessibility, and stable-slot assertions.
- Updated `apps/web/app/globals.css` semantic pagination-slot token from 104px to the source-verified fixed 112px height.

No new API route, server action, database symbol, environment variable, CLI flag, package, CSS animation, design-token name, or authentication/authorization surface is created; the existing pagination-slot token receives one verified value correction.
</artifacts_produced>

<verification>
- `pnpm --filter @fish/web exec vitest run "app/(authenticated)/chat/chat-client.test.tsx"` passes the focused UAT-gap regression coverage.
- `pnpm --filter @fish/web exec vitest run "app/(authenticated)/chat"` passes the surrounding pagination, scroll-anchor, store, and realtime regressions.
- `pnpm lint`, `pnpm typecheck`, and `pnpm build` pass.
- Source review confirms the loading branch has no Button, Alert, spinner, visible loading copy, raw color, arbitrary visual utility, numeric spacing utility, or extra primary action.
- Security gate: no high-severity STRIDE/ASVS L1 finding remains open; no auth/RLS/server boundary changed.
</verification>

<success_criteria>
- UAT Test 1's cosmetic gap is closed: older-history loading visibly shows only a two-row skeleton that represents the final message UI one-to-one.
- Avatar, author, timestamp, message content, continuation alignment, spacing, padding, and total reserved slot dimensions are structurally shared or regression-asserted against final community rows.
- Loading, idle, and failure transitions keep one fixed 112px reserved slot stable; its height is source-derived from the ~110.8px tallest state and regression-locked by both token and utility-class assertions. Scroll-preserving sentinel/button behavior remains unchanged.
- Loading start does not programmatically move focus, and completion does not steal focus into the transcript or loading region; browser fallback when the clicked trigger is removed is explicitly allowed.
- One primary action, 56px manual controls, token-only Tailwind v4 styling, notice-tone failures, visible focus, and reduced-motion behavior remain intact.
- Blocked UAT Tests 3 and 4 remain documented verification constraints; this gap plan introduces no speculative reconnect/backfill feature changes.
</success_criteria>

<output>
Create `.planning/phases/10-chat-message-loading-optimization/10-05-SUMMARY.md` when done.
</output>
