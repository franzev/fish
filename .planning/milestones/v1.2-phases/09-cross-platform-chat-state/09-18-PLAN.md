---
phase: 09-cross-platform-chat-state
plan: 18
type: execute
wave: 9
depends_on: []
files_modified:
  - apps/web/components/shell/app-shell.tsx
  - apps/web/components/shell/app-shell.test.tsx
autonomous: true
gap_closure: true
requirements: [CSTATE-06]

must_haves:
  truths:
    - "The app-shell logo link meets the non-negotiable 56px interaction floor on the canonical chat shell."
    - "No stale /chat route reference remains in app-shell.tsx."
    - "The logo link has an accessible name describing its destination."
  artifacts:
    - path: "apps/web/components/shell/app-shell.tsx"
      provides: "56px logo anchor (min-h-control/min-w-control, centered) + cleaned comments"
      contains: "min-w-control"
    - path: "apps/web/components/shell/app-shell.test.tsx"
      provides: "Logo 56px target test + no-/chat source assertion"
  key_links:
    - from: "app-shell logo Link"
      to: "--size-control (56px) tokens"
      via: "min-h-control + min-w-control utilities"
      pattern: "min-w-control"
---

<objective>
Close WR-09: enlarge the app-shell logo link to the non-negotiable 56px
interaction floor and remove the stale `/chat` route reference from the shell.

Today the logo anchor has only `shrink-0`, and its images are 32px/40px
(09-VERIFICATION WR-09 CONFIRMED BLOCKING PRODUCT GATE) — below the 56px floor
that AGENTS.md marks non-negotiable, on the canonical community chat shell. A
non-runtime `/chat` comment also lingers at app-shell.tsx:117 after the route
removal.

Purpose: the 56px floor is a hard product gate for this neurodivergent-focused
UI, and the shell is the frame every chat screen sits in. Output: a 56px
logo target with an accessible name, cleaned comments, and a regression test.
Tokens only — no `tailwind.config.js`, no raw numeric utilities.
</objective>

<execution_context>
Gap-closure round 4 for Phase 09. Shell component + its test only. Reuse the
existing `--size-control` (56px) tokens via `min-h-control`/`min-w-control` (the
same utilities Plan 09-11 used for message actions). On completion, write the
SUMMARY in <output>.
</execution_context>

<context>
@.planning/phases/09-cross-platform-chat-state/09-VERIFICATION.md
@apps/web/components/shell/app-shell.tsx
@apps/web/components/shell/app-shell.test.tsx
@apps/web/app/globals.css

<interfaces>
<!-- Contracts the executor edits against. Extracted from source. -->
app-shell.tsx:
- Logo Link (135-153): href={role === "coach" ? "/coach" : "/home"}, className="shrink-0", wrapping two next/image logos (alt="FISH") — one md:hidden 32px, one hidden md:block 40px. This anchor is the interaction target and is below 56px.
- Immersive comment (114-118) ends with "The `/chat` route was removed 2026-07-10 (community room supersedes it)." — a stale route reference.
- Descriptive comment (173) says "immersive channel/chat surface" — the "/chat" substring here should also go so the shell has no "/chat" text.
- Nav links already use min-h-control (line 77); --size-control is 56px (globals.css:101). min-h-control and min-w-control are both valid Tailwind v4 utilities from the --size-* namespace.

app-shell.test.tsx:
- Renders AppShell with next/navigation usePathname mocked and @/lib/auth/browser mocked; queries links via getByRole("link", { name }). Also uses readFileSync + regex source assertions (see the D-09/SHEL-01 test).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enlarge the logo link to the 56px floor and clean stale /chat comments</name>
  <files>apps/web/components/shell/app-shell.tsx</files>
  <read_first>
    - apps/web/components/shell/app-shell.tsx (logo Link 135-153; immersive comment 114-118; descriptive comment 173)
    - apps/web/app/globals.css (--size-control: 56px at line 101; how min-h-control is generated)
    - .planning/phases/09-cross-platform-chat-state/09-VERIFICATION.md (WR-09 CONFIRMED BLOCKING PRODUCT GATE: anchor only shrink-0, images 32/40px)
    - AGENTS.md (design rule 3: controls are min 56px; --size-control on web)
  </read_first>
  <behavior>
    - The logo link is at least 56px tall and 56px wide (its box, not just the image), with the logo image centered inside.
    - The logo link has an accessible name describing its destination (e.g. "FISH home").
    - app-shell.tsx contains no "/chat" text anywhere.
    - The logo still links to /home (client) or /coach (coach); the two responsive logo images are unchanged in size.
  </behavior>
  <action>
    Update the logo Link className from "shrink-0" to a 56px centered target using tokens only: add min-h-control and min-w-control, make it an inline-flex (or flex) box with items-center and justify-center so the 32/40px image sits centered in the 56px anchor, and keep shrink-0. Do not introduce a raw numeric height/width utility and do not add a tailwind.config.js. Add an aria-label to the logo Link describing the destination (e.g. "FISH home") so the anchor has a deterministic accessible name.

    Remove the stale `/chat` route sentence from the immersive comment (114-118) — keep the D-10 immersive/centered-column rationale but drop the "/chat route was removed" reference. Reword the descriptive comment at line 173 from "immersive channel/chat surface" to "immersive channel surface" so no "/chat" substring remains in the file.
  </action>
  <verify>
    <automated>grep -q "min-w-control" apps/web/components/shell/app-shell.tsx && grep -q "min-h-control" apps/web/components/shell/app-shell.tsx && test "$(grep -c "/chat" apps/web/components/shell/app-shell.tsx)" = "0" && echo SHELL_OK</automated>
  </verify>
  <acceptance_criteria>
    - The logo Link className includes min-h-control and min-w-control plus items-center and justify-center (centered 56px box).
    - The logo Link has an aria-label (accessible name) for its destination.
    - Grep gate: `grep -c "/chat" apps/web/components/shell/app-shell.tsx` returns 0.
    - No raw numeric height/width utility added to the logo anchor; no tailwind.config.js created.
  </acceptance_criteria>
  <done>The shell logo is a 56px accessible target and the stale route reference is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Regression-test the 56px logo target and the /chat cleanup</name>
  <files>apps/web/components/shell/app-shell.test.tsx</files>
  <read_first>
    - apps/web/components/shell/app-shell.test.tsx (render setup with mocked usePathname/@/lib/auth/browser; the readFileSync source-assertion test as a pattern)
    - apps/web/components/shell/app-shell.tsx as edited in Task 1 (logo aria-label + classes)
  </read_first>
  <behavior>
    - A test asserts the logo link carries the 56px control classes (min-h-control and min-w-control) and centers its content.
    - A source assertion confirms app-shell.tsx contains no "/chat".
  </behavior>
  <action>
    Add a test that renders AppShell (reuse the file's existing render helper and mocks) and gets the logo link by its aria-label from Task 1, asserting its className contains min-h-control, min-w-control, items-center, and justify-center. Add a readFileSync source assertion (mirroring the existing D-09/SHEL-01 source test) that the app-shell.tsx source does not match /\/chat/. Keep all existing shell tests intact.
  </action>
  <verify>
    <automated>pnpm --filter @fish/web test app-shell</automated>
  </verify>
  <acceptance_criteria>
    - app-shell.test.tsx passes, including the new logo-target test and the no-/chat source assertion.
    - Behavior assertion: the logo link (by aria-label) has min-h-control and min-w-control classes.
    - Source assertion: readFileSync(app-shell.tsx) does not match /\/chat/.
    - Existing shell tests remain green.
  </acceptance_criteria>
  <done>The 56px logo floor and the route-comment cleanup are locked against regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Presentational shell change; no data or trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-18-01 | Denial of service | sub-56px target excludes motor-impaired/ADHD users | mitigate | Enlarge the logo anchor to the 56px floor; regression-tested. |
| T-09-18-SC | Tampering | npm installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `pnpm --filter @fish/web test app-shell` green.
- `pnpm lint`, `pnpm build` green (no tailwind.config.js; tokens only).
- Grep gate: no "/chat" text in app-shell.tsx.
- HV (visual): the logo tap target is comfortably 56px on the community shell.
</verification>

<success_criteria>
- WR-09 closed: 56px accessible logo target on the canonical chat shell; stale /chat comment removed; regression test in place.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

Modified:
- apps/web/components/shell/app-shell.tsx — logo Link enlarged to a 56px centered target (min-h-control/min-w-control + items-center/justify-center) with an aria-label; stale /chat comments removed.
- apps/web/components/shell/app-shell.test.tsx — logo 56px-target test + no-/chat source assertion.

No new files. No new dependencies. Tokens only.
</artifacts_produced>

<output>
Create `.planning/phases/09-cross-platform-chat-state/09-18-SUMMARY.md` when done.
</output>
