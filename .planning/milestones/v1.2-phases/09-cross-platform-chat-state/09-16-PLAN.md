---
phase: 09-cross-platform-chat-state
plan: 16
type: execute
wave: 10
depends_on: [09-13, 09-15]
files_modified:
  - apps/web/app/(authenticated)/chat/hooks/use-chat-realtime.ts
  - apps/web/app/(authenticated)/chat/chat-client.tsx
  - apps/web/app/(authenticated)/chat/chat-client.test.tsx
  - apps/web/app/globals.css
autonomous: true
gap_closure: true
requirements: [CSTATE-02, CSTATE-06]

must_haves:
  truths:
    - "Switching the mounted client from conversation A to B resets every conversation-scoped realtime transient: participant typing/recording, local recording, local typing, timers, and hasConnected — none bleed from A into B."
    - "B's first realtime connect never reads as 'Reconnecting…' because of A's prior connected state."
    - "The older-page feedback region holds an invariant height across idle, loading, failure, and retry-success, so the transcript never shifts."
    - "A pending optimistic row survives a reconnect reset (web), and a late failure after a realtime-confirmed sent leaves the row sent (web)."
  artifacts:
    - path: "apps/web/app/(authenticated)/chat/hooks/use-chat-realtime.ts"
      provides: "Conversation-scoped reset of participant/local typing + recording transients"
      contains: "conversationId"
    - path: "apps/web/app/(authenticated)/chat/chat-client.tsx"
      provides: "Per-conversation hasConnected reset + invariant pagination-feedback slot"
      contains: "hasConnected"
    - path: "apps/web/app/(authenticated)/chat/chat-client.test.tsx"
      provides: "WR-06 prop-switch resets, WR-07 geometry stability, WR-02/WR-03 web regressions"
  key_links:
    - from: "use-chat-realtime render-time derivation"
      to: "participantTyping/participantRecording/localRecording/localTypingRef"
      via: "previousConversationId comparison (no setState-in-effect)"
      pattern: "previousConversationId"
    - from: "chat-client pagination feedback"
      to: "reserved-height slot"
      via: "single fixed-min-height container wrapping skeleton/error/button"
      pattern: "load-older-slot"
---

<objective>
Close WR-06 and WR-07 on the mounted community client, and add the web-layer
regressions the verifier asked for on top of the 09-13 reducer fixes:

- WR-06: reset every conversation-scoped realtime transient on conversation id
  change — participant typing/recording, local recording, local typing, timers,
  and ChatClient.hasConnected (09-VERIFICATION Review-Finding WR-06 BLOCKING).
- WR-07: reserve invariant pagination-feedback geometry so idle / loading /
  failure / retry-success never move the transcript (WR-07 BLOCKING, D-09
  no-layout-shift, STATE layout-stability contract).
- Web regressions: a pending optimistic row survives a reconnect reset (WR-02),
  and a late failure after a realtime-confirmed sent stays sent (WR-03).

Purpose: these are the last same-mounted-instance and layout-stability defects
for the community room. Output: conversation-scoped transient resets, an
invariant pagination slot, and the matching ChatClient regressions.
</objective>

<execution_context>
Gap-closure round 4 for Phase 09, wave 10 (after 09-13 reducer fixes and 09-15
pagination-ownership). Web hook + component + test layer. Reset state via the
render-time "adjusting state when a prop changes" pattern already used in this
file (previousRealtimeStatus) to avoid react-hooks/set-state-in-effect lint
(STATE decision). On completion, write the SUMMARY in <output>.
</execution_context>

<context>
@.planning/phases/09-cross-platform-chat-state/09-VERIFICATION.md
@apps/web/app/(authenticated)/chat/hooks/use-chat-realtime.ts
@apps/web/app/(authenticated)/chat/chat-client.tsx
@apps/web/app/(authenticated)/chat/chat-client.test.tsx
@apps/web/app/globals.css

<interfaces>
<!-- Contracts the executor edits against. Extracted from source. -->
use-chat-realtime.ts:
- State: participantTyping, participantRecording, localRecording (useState). Refs: localTypingRef, localTypingTimeoutRef, participantTypingTimeoutRef, participantRecordingTimeoutRef.
- Effect at 74-77 already resets seenFirstSubscribeRef/backfillInFlightRef on [chat.conversationId] (refs only). Typing (159-192) and voice (194-223) effects are keyed on [chat.conversationId, chat.currentUserId]; their cleanups clear timeouts but DO NOT reset the state values -> participantTyping/recording persist across a switch (WR-06). localTypingRef is not reset on switch.

chat-client.tsx:
- realtimeStatus from selectRealtimeStatusForConversation (105-107).
- previousRealtimeStatus + hasConnected derived state (171-178): render-time comparison sets hasConnected=true on "connected"; isReconnecting = realtimeStatus === "connecting" && hasConnected (179). hasConnected is NOT reset when chat.conversationId changes (WR-06).
- Pagination feedback region (285-331): sentinel (h-3xs), loading skeleton (two h-2xl rows + gap + pb-md), error alert+Try again (pb-md), and "Load earlier messages" button (pb-md) are separate blocks of DIFFERENT heights that swap in place (WR-07). Base Button gives min-h-control (56px).
- Failed-send row renders "Not sent yet" + Retry; compactSent renders <MessageStatus status={deliveryStatus}> (568-595).

globals.css: Tailwind v4 CSS-first @theme. Add a semantic size token here ONLY if a reserved-height value is genuinely needed; never a raw one-off utility.

chat-client.test.tsx: triggerIntersection(node, isIntersecting) helper; deferred-resolve action-mock pattern (1542+); realtime mock exposing messageHandlers/readHandlers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reset participant/local realtime transients on conversation change</name>
  <files>apps/web/app/(authenticated)/chat/hooks/use-chat-realtime.ts, apps/web/app/(authenticated)/chat/chat-client.test.tsx</files>
  <read_first>
    - apps/web/app/(authenticated)/chat/hooks/use-chat-realtime.ts (state at 52-54, refs at 57-63, conversation effect at 74-77, typing effect 159-192, voice effect 194-223)
    - apps/web/app/(authenticated)/chat/chat-client.tsx (how participantTyping is consumed for the typing indicator)
    - .planning/phases/09-cross-platform-chat-state/09-VERIFICATION.md (WR-06 CONFIRMED BLOCKING: typing/recording/localRefs cross conversation boundaries)
    - .planning/STATE.md (decision: reset via render-time derivation, not setState-in-effect)
  </read_first>
  <behavior>
    - When chat.conversationId changes on a mounted hook, participantTyping, participantRecording, and localRecording all reset to false, and localTypingRef resets to false, before the new conversation's indicators render.
    - Pending typing/recording timers from the previous conversation are cleared (no delayed setParticipantTyping(false) firing into the new conversation).
    - Within one conversation, typing/recording indicators behave exactly as before.
  </behavior>
  <action>
    Add a previousConversationId state to useChatRealtime and, during render, when chat.conversationId !== previousConversationId, set previousConversationId to the new id and reset setParticipantTyping(false), setParticipantRecording(false), setLocalRecording(false), and localTypingRef.current = false — the same render-time "adjusting state when a prop changes" pattern the file/ChatClient already uses for previousRealtimeStatus, so no setState-in-effect lint fires. Keep the existing timeout-clearing cleanups; additionally ensure localTypingTimeoutRef/participantTypingTimeoutRef/participantRecordingTimeoutRef are cleared on the id change (extend the existing [chat.conversationId]-keyed effect that already resets seenFirstSubscribeRef/backfillInFlightRef to also clear these timers). Do not alter the subscription wiring or the within-conversation timeout durations.

    Add a chat-client.test.tsx test: render ChatClient for conversation A, drive a participant typing event so the typing indicator shows, rerender the SAME mounted client to conversation B (new conversationId), and assert the A typing indicator is gone for B. Cover participant recording similarly if the harness exposes the recording handler.
  </action>
  <verify>
    <automated>pnpm --filter @fish/web test chat-client</automated>
  </verify>
  <acceptance_criteria>
    - New WR-06 realtime test passes; existing realtime/typing tests stay green.
    - Behavior assertion: after A->B switch on a mounted client, the participant typing indicator from A is not present.
    - Source assertion: participantTyping/participantRecording/localRecording/localTypingRef are reset via a previousConversationId render-time comparison (no setState inside a useEffect body).
    - `pnpm --filter @fish/web lint` reports no react-hooks/set-state-in-effect violation in this file.
  </acceptance_criteria>
  <done>Conversation-scoped realtime transients no longer bleed across a switch.</done>
</task>

<task type="auto">
  <name>Task 2: Per-conversation hasConnected reset + invariant pagination-feedback geometry</name>
  <files>apps/web/app/(authenticated)/chat/chat-client.tsx, apps/web/app/globals.css, apps/web/app/(authenticated)/chat/chat-client.test.tsx</files>
  <read_first>
    - apps/web/app/(authenticated)/chat/chat-client.tsx (hasConnected/previousRealtimeStatus derivation 165-179; pagination feedback region 285-331)
    - apps/web/app/globals.css (@theme size tokens; the layout-stability convention of reserved rows / out-of-flow notices)
    - .planning/STATE.md (layout-stability contract: no control changes size on state change; reserved message rows, out-of-flow notices)
    - .planning/phases/09-cross-platform-chat-state/09-VERIFICATION.md (WR-07 "Loading -> failure does not move the transcript" FAIL)
  </read_first>
  <behavior>
    - When chat.conversationId changes, hasConnected resets so isReconnecting is false until B genuinely connects; B's initial "connecting" never renders "Reconnecting…".
    - The older-page feedback region occupies the same vertical height in all of: idle (load-earlier button), loading (skeleton), failure (notice + Try again), and after a successful retry — the transcript below does not move between these states.
    - The Try again and Load earlier controls keep the 56px floor.
  </behavior>
  <action>
    hasConnected reset: extend the existing render-time derivation so a conversation change also resets the reconnect derivation. Track a previousConversationId; when chat.conversationId changes, reset hasConnected to (realtimeStatus === "connected") and previousRealtimeStatus to realtimeStatus during render (same pattern already present for previousRealtimeStatus), so B starts from a clean first-connect. Do not introduce setState-in-effect.

    Invariant geometry (WR-07): wrap the loading skeleton, the error+retry affordance, and the "Load earlier messages" button in a SINGLE reserved-height container (e.g. a wrapper with data-testid="load-older-slot") whose height is fixed for all states, so swapping among skeleton / error / button / empty never changes the region height and never shifts the transcript. Use design tokens only for the reserved height: prefer an existing size/spacing token that fits the tallest state; if none fits, add ONE semantic size token to @theme in globals.css (e.g. --size-pagination-slot) and use its generated utility — never a one-off numeric utility. Keep the notice-tone Alert and ghost Try again / Load earlier buttons and their min-h-control. Keep the sentinel as-is.

    Tests: add a WR-06 hasConnected test — mount for A, drive realtimeStatus to "connected" then rerender to B with status "connecting", assert "Reconnecting…" (role="status") is NOT shown for B. Add a WR-07 geometry test — with hasMoreOlder true, assert the load-older-slot wrapper is present in the idle (button) state, remains present during loading (skeleton inside it), and remains present in the error state (via the deferred/failed action mock), i.e. the same reserved slot hosts every state.
  </action>
  <verify>
    <automated>pnpm --filter @fish/web test chat-client</automated>
  </verify>
  <acceptance_criteria>
    - New hasConnected and geometry tests pass; existing chat-client tests stay green.
    - Behavior assertion: after A(connected)->B(connecting) switch, no role="status" "Reconnecting…" renders for B.
    - Structural assertion: a single data-testid="load-older-slot" wrapper hosts the skeleton, error, and button states (present in idle, loading, and error renders).
    - Token check: any new reserved height is a semantic @theme token in globals.css; `grep -nE "h-[0-9]+|min-h-[0-9]+" ` on the changed pagination region finds no one-off numeric height utility.
    - Buttons retain min-h-control (56px).
  </acceptance_criteria>
  <done>The reconnect label resets per conversation and the pagination feedback region is height-invariant.</done>
</task>

<task type="auto">
  <name>Task 3: Web regressions — pending survives reconnect reset (WR-02) and sent stays sent (WR-03)</name>
  <files>apps/web/app/(authenticated)/chat/chat-client.test.tsx</files>
  <read_first>
    - apps/web/app/(authenticated)/chat/chat-client.test.tsx (send lifecycle tests: optimistic pending, failed-send + Retry, realtime mock message/read handlers)
    - packages/core/src/chat-state/reducer.ts (the 09-13 hydrate-preserve + monotonic guard these tests rely on)
    - apps/web/app/(authenticated)/chat/hooks/use-chat-messages.ts (loadNewestMessagesAction reconnect-reset path -> hydrateWindow)
    - .planning/phases/09-cross-platform-chat-state/09-VERIFICATION.md (Executable probes 1-2: hydrate-then-late-failure; confirm-then-late-failure)
  </read_first>
  <behavior>
    - WR-02 web: an optimistic pending row remains visible after a reconnect reset whose loadNewestMessagesAction window omits it; a subsequent send failure marks it failed and restores its body to the draft.
    - WR-03 web: after an optimistic send is confirmed sent (via confirm/realtime), a later send-action failure for that clientRequestId leaves the row rendered as sent (status affordance present, no "Not sent yet").
  </behavior>
  <action>
    Add two regression tests to chat-client.test.tsx. WR-02: render ChatClient with a send action that stays pending (deferred), submit a message so a pending optimistic row renders, then drive the reconnect-reset path (backfill needsReset -> loadNewestMessagesAction returning a bounded window WITHOUT the pending clientRequestId) so hydrateWindow runs; assert the pending row is still present; then settle the send as failure and assert the row shows the failed affordance and the draft is restored to its body. WR-03: submit a message, deliver a confirming server row (via the confirm path or the realtime message handler) so it renders sent, then settle the original send action as a failure for that clientRequestId; assert the row still renders sent (status img present, no "Not sent yet" for it). Use act() around deferred settles and realtime handler calls.
  </action>
  <verify>
    <automated>pnpm --filter @fish/web test chat-client</automated>
  </verify>
  <acceptance_criteria>
    - Both regressions pass on the 09-13 reducer and would fail on the pre-fix reducer (they encode WR-02/WR-03).
    - Behavior assertion (WR-02): pending row survives the reconnect-reset hydrateWindow; later failure restores its body to the draft.
    - Behavior assertion (WR-03): a late failure after a confirmed sent row leaves it sent with no "Not sent yet".
  </acceptance_criteria>
  <done>The WR-02/WR-03 portable fixes are proven at the web/UI layer, satisfying the verifier's web-regression ask.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| previous conversation transient state -> current conversation render | Stale typing/recording/connection state from A is untrusted for B's UI truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16-01 | Spoofing | stale participant typing/recording indicator | mitigate | Conversation-scoped reset clears A's indicators so B never shows a false participant activity. |
| T-09-16-02 | Tampering | layout shift hiding/altering content | accept | Invariant geometry is a UX/stability control; no data trust boundary crossed. Handled via reserved slot. |
| T-09-16-SC | Tampering | npm installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `pnpm --filter @fish/web test chat-client` green (WR-06, WR-07, WR-02/WR-03 web regressions + existing suite).
- `pnpm build`, `pnpm lint`, `pnpm typecheck` green.
- HV-01 (stable notice-tone retry region, no transcript movement) becomes satisfiable via the invariant slot.
</verification>

<success_criteria>
- WR-06 and WR-07 closed; WR-02/WR-03 have web-layer regressions.
- No new client choice, no new primary action, 56px floor preserved, tokens only.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

New symbols / behaviors:
- use-chat-realtime.ts: previousConversationId render-time reset of participantTyping/participantRecording/localRecording/localTypingRef + timer clears on id change.
- chat-client.tsx: per-conversation hasConnected reset; data-testid="load-older-slot" reserved-height pagination container.
- globals.css: optional semantic size token (e.g. --size-pagination-slot) if a new reserved height is required.

New tests (chat-client.test.tsx):
- WR-06 participant-transient reset on A->B switch.
- WR-06 hasConnected/Reconnecting reset on A->B switch.
- WR-07 invariant load-older-slot geometry across idle/loading/error.
- WR-02 pending-survives-reconnect-reset web regression.
- WR-03 sent-stays-sent-after-late-failure web regression.

No new files. No new dependencies.
</artifacts_produced>

<output>
Create `.planning/phases/09-cross-platform-chat-state/09-16-SUMMARY.md` when done.
</output>
