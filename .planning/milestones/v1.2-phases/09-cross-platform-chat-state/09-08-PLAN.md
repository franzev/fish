---
phase: "09-cross-platform-chat-state"
plan: "08"
type: execute
wave: 5
depends_on: []
files_modified:
  - "apps/web/app/(authenticated)/chat/page.tsx"
  - "apps/web/components/shell/app-shell.tsx"
  - "apps/web/components/shell/app-shell.test.tsx"
  - "apps/web/e2e/chat-send.spec.ts"
  - ".planning/REQUIREMENTS.md"
  - ".planning/phases/09-cross-platform-chat-state/09-CONTEXT.md"
  - ".planning/ROADMAP.md"
autonomous: true
gap_closure: true
requirements:
  - CSTATE-02
  - CSTATE-06
must_haves:
  truths:
    - "The `/chat` URL no longer exists: the redirect page `apps/web/app/(authenticated)/chat/page.tsx` is deleted, so `/chat` renders nothing (no page.tsx = no route)."
    - "`/channels/general` (the community room) is the single canonical chat surface, and no application source references the removed `/chat` URL."
    - "REQUIREMENTS.md (CSTATE-02, CSTATE-06), 09-CONTEXT.md (D-09), and ROADMAP.md (Phase 9) carry dated supersede notes describing the community-room target, without deleting the original wording, so re-verification measures the community room and not the dead 1-on-1 route."
  artifacts:
    - path: "apps/web/app/(authenticated)/chat/page.tsx"
      provides: "Deleted — the `/chat` route no longer exists"
    - path: "apps/web/components/shell/app-shell.tsx"
      provides: "Immersive-layout check scoped to `/channels` only (dead `/chat` branch removed)"
    - path: "apps/web/e2e/chat-send.spec.ts"
      provides: "Browser send smoke navigates to the canonical community channel"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Dated supersede note on CSTATE-02 / CSTATE-06"
  key_links:
    - from: "apps/web/components/shell/app-shell.tsx"
      to: "immersive layout"
      via: "channels-only active-path check"
      pattern: "isActivePath\\(pathname, \"/channels\"\\)"
---

<objective>
Resolve Phase 09 route-scope drift (VERIFICATION Gap 4) via the locked user decision: remove the `/chat` route entirely so the URL no longer exists, keep `/channels/general` as the canonical chat surface, and add dated supersede notes to the planning/requirements docs so re-verification measures the community-room experience rather than the removed one-assigned-conversation route.

Purpose: CSTATE-02 and CSTATE-06 currently read against a dead `/chat` route; the docs must describe the real shipped surface before those requirements can be re-verified.
Output: `/chat` deleted, all `/chat` references cleaned, and CSTATE-02/CSTATE-06 (REQUIREMENTS.md), D-09 (09-CONTEXT.md), and the Phase 9 goal (ROADMAP.md) annotated with dated supersede notes.
</objective>

<execution_context>
@/Users/franz/.codex/gsd-core/workflows/execute-plan.md
@/Users/franz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/09-cross-platform-chat-state/09-CONTEXT.md
@.planning/phases/09-cross-platform-chat-state/09-VERIFICATION.md
@apps/web/app/(authenticated)/chat/page.tsx
@apps/web/app/(authenticated)/channels/[id]/page.tsx
@apps/web/components/shell/app-shell.tsx
@apps/web/components/shell/app-shell.test.tsx
@apps/web/e2e/chat-send.spec.ts
@apps/web/lib/channels.ts

<interfaces>
<!-- Extracted from the codebase. Use directly. -->

Removed route (apps/web/app/(authenticated)/chat/page.tsx): currently only `export default function ChatPage() { redirect(generalChannelHref); }`.

Canonical route (apps/web/app/(authenticated)/channels/[id]/page.tsx): renders `ChatClient` with community data; imports shared internals from `../../chat/actions` and `../../chat/chat-client`. The `chat/` directory (ChatClient, hooks/, store/, chat-state.ts, actions.ts, realtime.ts, tests) has NO `page.tsx` besides the one being deleted, so after deletion it is a module folder, not a route.

app-shell.tsx immersive check (line ~116): `const immersive = isActivePath(pathname, "/channels") || isActivePath(pathname, "/chat");`

Channel constants (apps/web/lib/channels.ts): `generalChannelId = "22222222-2222-4222-8222-222222222222"`, `generalChannelHref = "/channels/22222222-2222-4222-8222-222222222222"`.

e2e (apps/web/e2e/chat-send.spec.ts line 13): `await page.goto("/chat");`
</interfaces>
</context>

## Artifacts This Phase Produces

This plan creates no new runtime symbols. It deletes `apps/web/app/(authenticated)/chat/page.tsx`, narrows the `immersive` check in `app-shell.tsx`, updates the e2e navigation target, and adds dated supersede annotations to `.planning/REQUIREMENTS.md`, `.planning/phases/09-cross-platform-chat-state/09-CONTEXT.md`, and `.planning/ROADMAP.md`.

<tasks>

<task type="auto">
  <name>Task 1: Remove the /chat route and its remaining references</name>
  <files>apps/web/app/(authenticated)/chat/page.tsx, apps/web/components/shell/app-shell.tsx, apps/web/components/shell/app-shell.test.tsx, apps/web/e2e/chat-send.spec.ts</files>
  <read_first>
    @apps/web/app/(authenticated)/chat/page.tsx
    @apps/web/components/shell/app-shell.tsx
    @apps/web/components/shell/app-shell.test.tsx
    @apps/web/e2e/chat-send.spec.ts
    @apps/web/lib/channels.ts
  </read_first>
  <action>Decision (locked): remove ONLY the dead route file and its references; do NOT relocate the shared chat internals out of `apps/web/app/(authenticated)/chat/` (ChatClient, hooks/, store/, chat-state.ts, actions.ts, realtime.ts, and tests stay put). This keeps the moved-code surface zero, stays behavior-neutral, avoids churn in `channels/[id]/page.tsx` which imports `../../chat/*`, and does not conflict with the uncommitted Phase-10 WIP in `chat/chat-client.test.tsx` and `chat/hooks/use-chat-messages.ts`.

  1. Delete `apps/web/app/(authenticated)/chat/page.tsx` with `git rm` so the `/chat` URL no longer resolves.
  2. In `app-shell.tsx`, change the `immersive` computation to `isActivePath(pathname, "/channels")` only — drop the `|| isActivePath(pathname, "/chat")` branch. This is dead-route cleanup, NOT a nav change: do not touch nav links, labels, destinations, `clientNavItems`, `coachNavItems`, or the channels sidebar (the user already rewired nav).
  3. In `app-shell.test.tsx`, remove the now-obsolete `it("gives the chat route the full pane instead of the centered column", ...)` case that sets `pathname = "/chat"`; the sibling case that sets `pathname = ` the general channel already covers immersive layout for the canonical route. Do not alter the other shell tests.
  4. In `e2e/chat-send.spec.ts`, replace `await page.goto("/chat");` with navigation to the canonical community channel: `await page.goto("/channels/22222222-2222-4222-8222-222222222222");` (the value of `generalChannelHref`; alternatively import `generalChannelHref` from `@/lib/channels` if the e2e tsconfig resolves the alias). Leave the rest of the send-smoke assertions unchanged.</action>
  <acceptance_criteria>
    - CLI assertion: `test ! -f "apps/web/app/(authenticated)/chat/page.tsx"` (the file is gone).
    - Source assertion: `grep -rn '"/chat"' apps/web/app apps/web/components apps/web/e2e` returns no matches (no application source references the removed URL string).
    - Source assertion: `app-shell.tsx` `immersive` expression contains `isActivePath(pathname, "/channels")` and no `"/chat"`.
    - Source assertion: `e2e/chat-send.spec.ts` navigates to `/channels/22222222-2222-4222-8222-222222222222` (or `generalChannelHref`), not `/chat`.
    - Source assertion: nav link arrays (`clientNavItems`, `coachNavItems`) and their labels/destinations are unchanged from the committed version.
    - Test command: `pnpm --filter @fish/web test "components/shell/app-shell.test.tsx"` passes.
  </acceptance_criteria>
  <verify>
    <automated>test ! -f "apps/web/app/(authenticated)/chat/page.tsx" && ! grep -rn '"/chat"' apps/web/app apps/web/components apps/web/e2e && pnpm --filter @fish/web test "components/shell/app-shell.test.tsx"</automated>
  </verify>
  <done>`/chat` is gone, no source references it, the immersive layout is channels-only, and shell tests pass — nav links untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add dated supersede notes to requirements and planning docs</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/09-cross-platform-chat-state/09-CONTEXT.md, .planning/ROADMAP.md</files>
  <read_first>
    @.planning/REQUIREMENTS.md
    @.planning/phases/09-cross-platform-chat-state/09-CONTEXT.md
    @.planning/ROADMAP.md
    @.planning/phases/09-cross-platform-chat-state/09-VERIFICATION.md
  </read_first>
  <action>Add dated (2026-07-10) supersede notes; do NOT delete or rewrite the original wording (preserve history — annotate in place).

  1. `.planning/REQUIREMENTS.md`, in the "Cross-platform Chat State (CSTATE)" section (immediately after the CSTATE-06 bullet): add a blockquote note dated 2026-07-10 stating that the canonical chat surface is now the community room at `/channels/general` (the `/chat` route was removed), that CSTATE-02's "one-assigned-conversation experience" and CSTATE-06's "existing chat behavior / no new user-facing surface" are now interpreted against the community-room experience, and that Phase 09 re-verification targets `/channels/:id`, not the removed `/chat` route. Leave the CSTATE-02 and CSTATE-06 bullet text intact.
  2. `.planning/phases/09-cross-platform-chat-state/09-CONTEXT.md`, directly under decision `D-09`: add a dated 2026-07-10 supersede note that the "one assigned conversation" visible-behavior target is superseded by the community-room experience at `/channels/general`; `/chat` is removed; the no-lost-draft, no-duplicate, and no-layout-shift guarantees still apply to the community room. Keep the original D-09 text.
  3. `.planning/ROADMAP.md`, in the Phase 9 details/goal area: add a dated 2026-07-10 note that the canonical chat surface for Phase 9 is the community room at `/channels/general` and that any "assigned conversation" phrasing is superseded accordingly. Keep the original goal text.</action>
  <acceptance_criteria>
    - Source assertion: `.planning/REQUIREMENTS.md` contains a 2026-07-10 supersede note referencing `/channels/general` within the CSTATE section, and the original CSTATE-02 and CSTATE-06 bullets are still present.
    - Source assertion: `09-CONTEXT.md` contains a dated supersede note attached to D-09 and the original D-09 text remains.
    - Source assertion: `ROADMAP.md` Phase 9 area contains a dated note about the community-room canonical surface.
    - Behavior assertion: no original requirement/decision wording is deleted (git diff shows additions/annotations only for these three files).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "2026-07-10" .planning/REQUIREMENTS.md .planning/phases/09-cross-platform-chat-state/09-CONTEXT.md .planning/ROADMAP.md && grep -n "channels/general" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>CSTATE-02/CSTATE-06, D-09, and the Phase 9 goal carry dated supersede notes pointing re-verification at the community room, with original wording preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser routing -> authenticated shell | Removing `/chat` removes one navigable surface; `/channels/:id` remains RLS-protected server-loaded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-81 | Information disclosure | Deleted `/chat` route | accept | Deleting a client redirect page exposes no data; `/channels/:id` keeps server-side auth + RLS unchanged. |
| T-09-82 | Tampering | Doc supersede notes | mitigate | Notes are additive and dated; original requirement/decision text preserved so history is auditable. |
| T-09-SC | Tampering | Package installs | accept | No package installs in this plan. |
</threat_model>

<verification>
After both tasks:
- `pnpm --filter @fish/web test "components/shell/app-shell.test.tsx"`
- `test ! -f "apps/web/app/(authenticated)/chat/page.tsx"`
- `grep -rn '"/chat"' apps/web/app apps/web/components apps/web/e2e` returns nothing
- `pnpm typecheck`
- `pnpm lint`
- `pnpm build`
- Note: `e2e/chat-send.spec.ts` is updated for correctness but is NOT run as a gate here (verification is via vitest + build per the phase's verification discipline; Playwright needs a live server).
</verification>

<success_criteria>
- `/chat` no longer exists and nothing in app source references the URL.
- The immersive layout is channels-only; nav links/labels/destinations are unchanged.
- CSTATE-02, CSTATE-06, D-09, and the Phase 9 goal carry dated supersede notes describing the community-room target with original wording intact.
- typecheck, lint, build, and shell tests pass.
</success_criteria>

<closing_notes>
- This plan is independent of the behavior-fix plans (09-07, 09-09, 09-10, 09-11): it shares no code files with them, so it runs in Wave 5 in parallel.
- Working-tree caution: does not touch `chat/chat-client.test.tsx` or `chat/hooks/use-chat-messages.ts`; the uncommitted Phase-10 WIP there is left intact.
</closing_notes>

<output>
Create `.planning/phases/09-cross-platform-chat-state/09-08-SUMMARY.md` when done.
</output>
